# Hermes Experiment Ledger

All metrics below are copied from tool output generated in the same session. Empty cells are preferred over unverified values.

## Summary

| Date | Run | Fixture | Hypothesis / arm | Runtime | Experimental variable | Latency | Output | Verified result | Decision |
|---|---|---|---|---|---|---:|---:|---|---|
| 2026-08-03 | `deepseek-v4-flash-1m-context-20260803` | active orchestrator config + real Sudo router probes through `chat_completions` + Hermes source resolution (`model_metadata.py`, `context_compressor.py`) | DeepSeek V4 Flash's true 1M context window should be configured as 1M (not the stale 128K per-model override) with a 0.6 compression trigger, aligning with kimi-k3's 350K/0.6 policy — unlike K3's KDA, DeepSeek's CSA+HCA attention has no long-context false-stop, so 1M is a real operating range | profile branch `feat/hybrid-routing-portable`; config `~/.hermes/profiles/orchestrator/config.yaml` | per-model `custom_providers.sudo.models[t9s/deepseek-v4-flash].context_length 128000→1000000`; `compression.model_thresholds` + `deepseek-v4: 0.6` (substring-resolved); 21× `t9s/` provider-prefix unification | — | probe 225K tokens + 200K max_tokens accepted; router accepts `reasoning_effort` (none disables thinking, low≈max normalized: 3895 vs 3819 tokens); Hermes `_supports_reasoning_extra_body`=False for custom:sudo → `agent.reasoning_effort` inert without `extra_body` workaround | `hermes config` exit 0; `resolve_model_threshold('t9s/deepseek-v4-flash')` → 0.6; effective 1M × 0.6 = 600K compress trigger; no k3-style long-context false-stop (`needs_reasoning_echo=True`, router does not force) | accept 1M window + 0.6 threshold config; reasoning-effort workaround not yet applied |
| 2026-07-30 | `hermes-nohybrid-upstream-20260730T050428Z-f1da9da0-7737-44b2-9a4c-36d487b4007e` | latest upstream freeze + complete authored/tracked PR convergence + sealed direct-PR/no-Hybrid patch replay + canonical promotion/install/restart | A frozen latest-upstream rebuild can preserve every authoritative non-Hybrid behavior while keeping `coupled-v1`, closing all PR conflicts, and physically excluding the retired Hybrid patch family from the promoted runtime and portable archive | live/shadow tree `44479521952e`: target `7fb10f90f645`, shadow `a20958808010`; prior BASE `d71033a4077a`; PR layer `a881225f181f`; live authority is **26** patches because post-handoff patch `260` is present; workflow `coupled-v1` | upstream BASE; complete PR portfolio; direct PR layer; proof-bound retained/drop accounting; promotion/install/restart/live smoke | — | initial GraphQL scope 31 authored open / 0 external tracked open / 21 DIRTY; 19 unique PRs are focused-PASS and pushed; second-wave base `fa8b959b92c8`; #63298/#63671 hard rebases remain | Phase 0 PASS: `0700` transaction root, four mutation locks unlocked, runtime clean, archive 9,921,459 bytes / SHA-256 `74bb2319…`; handoff's 25-patch count proven stale and no retired `190/200/205/210/215/220/240` file is present | running |
| 2026-07-30 | `hybrid-patch-retirement-47e5ff56` | valid 32-patch manifest + detached pre-Hybrid tail + proof-aware intentional-retirement control plane + deterministic portable export | Retiring the unused Hybrid core/session-workflow family while semantically retaining patches `225`, `230`, and `250` should reduce the authoritative stack from 32 to 25 patches without changing the live runtime or weakening patch publication gates | live `local/hermes-patched@1606b492b` stayed immutable; shadow `d8df618ca916→e2ddfe50a5ed`; base `d71033a4077a`; PR layer `a881225f181f`; tree `1df1fbb1f3d9`; plan `docs/plans/2026-07-30-hybrid-patch-retirement-contract.md` | retire `190/200/205/210/215/220/240`; retain/rebase `225/230/250`; add explicit same-base intentional-retirement mode | fresh shadow-stack audit test run 61.342 s | 25/25 patch stack; candidate Python 732/732; Desktop Vitest 110/110 + typecheck; exporter 39/39; Sol `ACCEPT`, `P0=P1=P2=P3=0` | exact seven-name retirement delta; all 25 retained stable IDs match; zero merges/Hybrid core symbols; active + archived workflow `coupled-v1`; deterministic archive 2,052 files / 9,889,657 bytes / SHA-256 `f31319e241a87ab2bfcafa00790b50e4842d3f5e00f9b221577767a4220e7007`; runtime worktree clean | accept shadow/profile/archive retirement **without runtime activation**; updater, Desktop install, and gateway/Desktop restart remain intentionally skipped |
| 2026-07-30 | `p0-server-overload-retry-b2e61c80` | exact `server_is_overloaded` fixture + retry-budget boundary (`3` retries) + isolated Hybrid GLM implementation / fresh Sol audit + candidate-only test alignment | Explicit overload classification plus bounded provider-overload backoff and the documented retry-budget semantics should recover transient Sudo overloads without credential rotation or provider switching | live `local/hermes-patched@7fb10f90f` (promoted from `1606b492b`); shadow `a20958808`; tree `444795219`; base `d71033a4077a`; PR layer `a881225f181f`; plan `docs/plans/2026-07-29-p0-server-overload-retry-contract.md` | exact error-code classification; 10/20/40/60s capped overload policy; `3 retries = 4 attempts`; `0 retries = 1 attempt`; transport rebuild consumes the same budget; candidate-only test alignment (kanban `origin` kwarg, cron `workdir` kwarg, inflight-turn retention on error path, empty-response excerpt non-persistence) | 7-file runtime verification 6.8 s; full 7-suite focused matrix | 280/280 runtime PASS (0 failed); 39/39 portable PASS; patch `250` SHA-256 `a84bbaf2...` + patch `260` SHA-256 `66cccb8d...`; deterministic archive SHA-256 `f40722c4...`; promotion state `completed/rev25`; tree identity `RUNTIME=SHADOW` | **Sol ACCEPT** — P0/P1/P2=0; runtime promoted and verified; Gateway/Desktop restart pending explicit approval |
| 2026-07-29 | `kimi-k3-promotion-audit-closure-20260729` | target session `20260728_134306_d0bce9` + sealed promotion v9 + active orchestrator profile + deterministic portable export + two-round Hybrid repair/fresh Sol audit | Routing Hindsight patch/check/verification through the promoted repository's live venv, binding Vision to the named K3 provider, and excluding only sealed preparation runtimes should let the queued K3/workflow candidate complete promotion without weakening archive or session authority | `local/hermes-patched@fb950702ef67`; tree `2bc2da33f028`; code artifact `0753492adcdb`; manifest transaction `6220cee353d6089033a4e4f1` | updater live-venv `uv --python`; Vision `custom:sudo-kimi-k3`; exact `promotion-transactions/<tx>/.sealed-promotion-manifest*.preparations/**` exclusion; GLM implement + two exact-UUID repairs + fresh Sol audit | sealed promotion active 976.098 s; GLM implement/repair 452.133 s; final audit 645.339 s | canonical state `completed/complete`; named-provider resolver `sudo-kimi-k3 / t9s/kimi-k3 / OpenAI`; solid-red Vision canary identified red; archive 2,053 files / 10,090,010 bytes / SHA-256 `5eb5bba633b53cbcfb27052e74dcae130700eb9a6ff2f9519c76d5d2e678ba0a` | Hindsight post-sync completed; `uv pip check` 98 packages and runtime verifier PASS; exporter 39/39; K3 registration 7/7; first-row workflow audit tests 4/4; independent export byte-equal; zero preparation members; 31/31 patch stack valid; final reviewer UUID `019fadd2-571d-7000-aa8a-97ba9f3e97a7` returned `ACCEPT`, P0=P1=P2=P3=0 | accept promoted source/profile/archive **before activation**; Desktop install and gateway/Desktop restart remain intentionally skipped pending explicit approval |
| 2026-07-29 | `orchestrator-daily-24h-hardening-20260729` | live orchestrator profile + fake cloud SSH retry + real daily/cloud cron runs + deterministic portable export | Separating topology ownership from process liveness, measuring canonical state/message storage, bounding retained artifacts, and progressively disclosing oversized skills should remove recurring audit false positives without weakening fail-closed durability | live `local/hermes-patched@eafda57b2051`; shadow `fd096358e5d7`; OMP 17.1.7; profile branch `feat/hybrid-routing-portable` | Desktop-only owner/ticker semantics; Hindsight split-runtime env; cloud retry; state.db aggregates; patch-stack durability; MEMORY/USER compaction; high-size skill extraction; generated-state/archive-backup exclusion | — | collector 22,930 bytes; archive 2,039 files / 9,961,234 bytes / SHA-256 `e7c3e7b4888294c31ead1707334b4dc9af1015064158b8e5b761e7ba4b66d612` | profile tests 41/41; OMP wrapper 90/90; exporter 33/33; four-layer 14/14; patch stack 31/31 valid with queued shadow patch `240` one commit ahead of live; archive/runtime/policy parity PASS; generated `*.patch.pre-*` backups excluded; `high_size_count 4→0`; MEMORY 6,967/8,000 and USER 3,847/4,500; real daily/cloud cron `last_status=ok` | accept audit hardening; do not infer promotion of queued patch `240`; remaining medium debt is state.db retention, nine 45–90KB skills, and 56 historical patched-state snapshots |
| 2026-07-29 | `kimi-k3-picker-workflow-persistence-20260729` | active orchestrator profile + real Hermes/OMP K3 tool loops + isolated patched-runtime Desktop create/resume + 31-patch shadow stack + deterministic portable export | A separate Chat Completions K3 provider with a guarded 350K window should be manually selectable without changing Hybrid defaults, and persisting the canonical workflow in the first DB row should prevent explicit Coupled sessions from resuming as the profile-default Hybrid route | live `local/hermes-patched@eafda57b2051`; shadow/candidate `fd096358e5d7`; OMP 17.1.7; profile branch `feat/hybrid-routing-portable` | `custom:sudo-kimi-k3 / t9s/kimi-k3`; context `350000`; compression `.90→315000`; OMP `sudo/t9s/kimi-k3 / max`; patch `240-coupled-workflow-first-row-persistence.patch` | — | exact `K3_HERMES_FINAL_TOOL_OK`, `K3_OMP_NORMAL_TOOL_OK`, `K3_WORKFLOW_CREATE_OK`, and `K3_WORKFLOW_RESUME_OK` | config check exit 0; K3 profile gate 10/10; workflow source suites 54/54; patched real create/resume PASS with `hybrid-v1` profile default and persisted/resumed `coupled-v1`; exporter 33/33; hard-slot regression 1/1; archive 2,039 files / 9,961,234 bytes / SHA-256 `e7c3e7b4888294c31ead1707334b4dc9af1015064158b8e5b761e7ba4b66d612`; patch stack 31/31 and bundle/apply checks PASS | accept manual K3 route and durable workflow fix artifacts; keep Hybrid as default and K3 fallback empty; live runtime activation remains pending explicit restart approval |
| 2026-07-28 | `vision-k3-primary-k27-fallback-20260728` | generated 48×48 solid-red/blue PNGs; isolated primary endpoint at `127.0.0.1:9` for failover | Kimi K3 should serve as primary Vision while Sudo Kimi K2.7 recovers a model-scoped primary connection failure on the same named provider | `local/hermes-patched@eafda57b2051`; profile repo `8c4e04b0cc16` | `auxiliary.vision`: `custom:sudo / t9s/kimi-k3` primary, `custom:sudo / kimi-k2.7-code` fallback; both `chat_completions` | — | K3 direct + `vision_analyze_tool`: exact `RED` and `BLUE`; forced failover response model `kimi-k2.7-code`, direct + tool exact `BLUE` | primary route 2/2 PASS; isolated real failover PASS; focused source tests 36/36; exporter 33/33; deterministic archive 1,981 files / 9,559,192 bytes / SHA-256 `3afd4c2cdbeae1df532c686a364a59285248c73562e1e7bbd6f3ccf7e636a67a` | accept K3 primary with K2.7 automatic fallback for model-scoped capacity/transport failures |
| 2026-07-28 | `hybrid-current-combined-closure-20260728` | active orchestrator profile + sealed portable archive + fake-OMP admission oracle + real OMP executor timeout/resume + fresh hard re-review | Restoring the accepted typed Hybrid wrapper and semantically merging the 272K GPT overlay should close the post-review coupled-only archive regression without changing the explicit-session canary or K3 Vision configuration | runtime `eafda57b2051`; artifact commit `6a72f3839ca6`; OMP 17.1.7; reviewer UUID `019fa8f4-c6a7-7000-8512-387c7d8e0f0f` | wrapper/policy/runtime-skill parity; canonical GPT private auto-reserve overlay; GLM no-overlay; artifact-level behavior oracle | executor 156.023 s over two attempts | live exact `HYBRID_GLM_EXECUTOR_OK`; attempt 1 internal timeout rc124, attempt 2 same UUID rc0; fake probe changed from rc0/spawn=yes to rc2/spawn=no | active/restored Hybrid `298/298 OK`; deployment-safe wrapper `86/86 OK`; artifact+exporter `34/34 OK`; deterministic archive 1,981 files / 9,590,013 bytes / SHA-256 `5da62b8eaf5e208fcfd14f35c474798f41dc451877b7b49e560d8593a63f9943`; fresh re-review `ACCEPT`, P0=P1=P2=P3=0 | accept current combined Hybrid artifact; rollout default was changed by the later `hybrid-profile-default-activation-20260728` run |
| 2026-07-28 | `hybrid-profile-default-activation-20260728` | active orchestrator config + Python/Gateway/Desktop fresh-session contracts + deterministic portable export | After the current Hybrid artifact reached zero-severity acceptance, setting the profile default to `hybrid-v1` should make future sessions start on the fixed Sol controller without changing existing per-session routes or any other profile configuration | runtime `eafda57b2051`; artifact commit `b35df8d1973f`; model default `gpt-5.6-sol` | `coding_workflow.default`: absent/coupled fallback → `hybrid-v1` | — | active config and archived config both resolve `hybrid-v1`; existing current session remains Hybrid | canonical Python resolver/gateway/web-server `129/129 OK`; Desktop fresh Hybrid-default session `1/1 OK`; artifact+exporter `34/34 OK`; exactly one archive member changed (`orchestrator/config.yaml`); deterministic archive 1,981 files / 9,590,020 bytes / SHA-256 `e0b741e34cc3758f987b9b7bbb50c7968d9735636516df3eb9c53ed348741b33` | activate Hybrid as the orchestrator profile default for future new sessions |
| 2026-07-13 | `vision-backend-ab-qwen-20260713` | `Fruits.png` (1055×1491), identical prompt | Existing control: Qwen2.5-VL-32B should provide the reference detailed description | `local/hermes-patched@b7f8bf659` | Vision backend = local vLLM `qwen-vl`; `temperature=0.1`, `max_tokens=2000` | 30.8 s | 5,618 chars; `finish=stop` | Correctly identified the title, 4×4 fruit grid, labels, colors, and layout | retain as opt-in standby |
| 2026-07-13 | `vision-backend-ab-kimi-20260713` | `Fruits.png` (1055×1491), identical prompt | Kimi K2.7 Code should retain useful visual understanding while removing the local port-forward dependency | `local/hermes-patched@b7f8bf659` | Vision backend = Sudo `kimi-k2.7-code`; `temperature=0.1`, `max_tokens=2000` | 16.8 s | 3,706 chars; `finish=length`; 1,033 reasoning tokens | Correctly identified the title, 4×4 fruit grid, labels, colors, and layout | accept as primary vision route |
| 2026-07-13 | `vision-kimi-hermes-smoke-20260713` | generated 96×96 solid-red PNG | Hermes' real `vision_analyze` path should resolve the named Sudo provider using `chat_completions` | `local/hermes-patched@b7f8bf659` | Active `auxiliary.vision` configuration | — | `Red` | `VISION_VERIFY=PASS`; live Desktop tool also returned `Red` | accept configuration |
| 2026-07-13 | `vision-kimi-connection-failure-20260713` | generated red PNG; endpoint forced to `127.0.0.1:9` in an isolated temporary profile | A Kimi connection failure must remain an error and must not invoke Qwen automatically | `local/hermes-patched@b7f8bf659` | Kimi endpoint unavailable; no `fallback_chain` | — | `success=false` | `connection_error=true`, `mentions_qwen=false`, `CONNECTION_FAILURE_VERIFY=PASS` | require user approval before any Qwen switch |
| 2026-07-14 | `memory-p1-durability-promotion-20260714` | P1 13-file matrix + promoted-runtime focused suites + post-restart live services | Session-boundary generation, provider binding, queued mutation, executor failure, and update-survival fixes should remain isolated and portable through the canonical update chain | `local/hermes-patched@87b8d62f0` | `130-memory-session-isolation-durability.patch` from `d785d2bf4` | — | P1 matrix `442 passed`; profile infra `86 passed`; promoted smoke `203 passed`; archive parity `15/15`; post-restart live smoke PASS | Final review `ACCEPT`; updater PASS; Hindsight verifier/health/recall PASS; Basic Memory search PASS; Desktop native deps 3/3 PASS | accept promotion; Desktop and orchestrator gateway restarted and live-verified |
| 2026-07-14 | `sudo-glm52-heavy-context-boundary-20260714` | direct `POST /v1/responses`; deterministic `x ` padding; server-reported usage | Upgraded Sudo `glm-5.2-heavy` should expose a real 1M total context rather than only advertising it | `local/hermes-patched@87b8d62f0197` | Input length and `max_output_tokens` (`1`/`16`); `reasoning=none`; `store=false` | 0.58–170.86 s | HTTP 200 through inferred total budget 1,000,000; next token returns proxy 502 wrapping upstream 400 | 999,993 reported input + 1 output reservation + 6 hidden protocol tokens succeeds cold; 999,994 input fails twice. Independent 16-output boundary gives the same 6-token reserve | accept true context window = 1,000,000 total tokens; retain 900K configured safety cap recommendation |
| 2026-07-14 | `glm52-heavy-900k-config-apply-20260714` | active orchestrator Hermes config + active OMP config + isolated route-aware Heavy smoke | A 900K hard cap paired with an approximately 315K routine trigger should expose the verified capacity without expanding routine histories into the 675K/765K full-window range | `local/hermes-patched@87b8d62f0197`; OMP 16.4.8 | Hermes Heavy `900000/.35`; OMP Heavy `900000` + fixed `315000`; unchanged `.50`, `65536`, `20000`, `120000` | — | Resolver PASS for GLM and both GPT routes; OMP smoke `OMP_GLM52_900K_CONFIG_OK`; semantic diff limited to intended fields | GLM trigger 315K; both GPT routes remain 316.2K. Stale OpenAI Codex 272K cache row removed and resolver rebuilt 372K | accept and persist 900K/315K configuration |
| 2026-07-14 | `aux-glm52-reasoning-none-ab-20260714` | identical harmless smart-approval classifier prompt; four interleaved repetitions per arm | Explicit `reasoning.effort=none` should reduce non-heavy GLM auxiliary latency and hidden output tokens | `local/hermes-patched@87b8d62f0197` | Sudo `glm-5.2` server default vs explicit `none`; timeout 30s | default median 1.134s; none median 1.207s | both arms 4/4 exact `APPROVE`; hidden output 174–251 tokens | No measurable latency/token benefit from explicit `none`. Current log window: 152 approval calls, 2 full 30s timeouts and 2 main-model fallbacks | reject reasoning override; apply `auxiliary.approval.timeout=20`. `approvals.timeout=60` controls later human waiting and is not an outer LLM deadline |
| 2026-07-14 | `omp-model-ab-gpt56-xhigh-20260714` | isolated Python `SingleFlightTTLCache`; identical prompt/public tests; five post-hoc concurrency/LRU tests hidden during implementation | GPT-5.6-sol xhigh should provide the high-quality first-pass control on a normal-tier implementation with subtle concurrency invariants | OMP 16.4.8; `sudo/gpt-5.6-sol`; effective `xhigh` | OMP executor model | 257.73 s | 16 assistant calls; 15 tool-use calls; 46,090 uncached input; 9,853 output; 6,575 reasoning tokens | public tests GREEN; post-hoc hidden 5/5; final full suite 16/16 | accept as first-pass quality control |
| 2026-07-14 | `omp-model-ab-glm52-heavy-firstpass-20260714` | same isolated fixture, prompt, public tests, and hidden tests | GLM-5.2-heavy should trade some first-pass edge-case coverage for substantially lower wall time | OMP 16.4.8; `sudo/glm-5.2-heavy`; session recorded effective `high` although wrapper requested `xhigh` | OMP executor model | 58.64 s | 11 assistant calls; 10 tool-use calls; 51,924 uncached input; 11,955 output | public tests GREEN; initial post-hoc hidden 1/4; missed expired-MRU capacity semantics and post-barrier caller membership | reject one-pass trust for concurrency/lifecycle work; retain as fast implementation arm |
| 2026-07-14 | `omp-model-ab-hybrid-repair-20260714` | GPT orchestrator reviewed GLM implementation, generated adversarial tests, and returned exact findings to the same GLM session twice | GPT orchestration plus GLM execution should recover control quality while retaining a child-runtime advantage | same GLM OMP session resumed by exact UUID | two review/fix iterations | 93.29 s cumulative child wall | repair rounds 22.09 s + 12.56 s; GLM cumulative 24 assistant calls and 21 tool-use calls | final full suite 22/22 after catching a second-order flight-unregistration race introduced by the first repair | accept hybrid for scoped work only with mandatory GPT verification/iteration; 2.76× faster child wall than GPT OMP control |
| 2026-07-17 | `runtime-promotion-pr65388-narrow-20260717` | canonical updater + portable archive + Desktop/gateway restart and live smoke | Latest upstream plus regenerated 040/050/061/070/090/130 patches, #63519 cron-store fixture fix, and narrowed #65388 should promote without conflict and survive export/restart | pre-promotion `local/hermes-patched@77a982087` → promoted `26cc04b8ac` | base `ef9e0c98f5`; #65388 head `9711506d5`; #63519 head `1def0b0a1` | — | applied=47, skipped=4; archive 1554 files, 5848182 bytes, SHA-256 `c92723b7e1…`; durability parity 15/15 | PASS | accept promotion; Desktop PID 83083, gateway PID 81999, backend v0.18.2 port 65295, codesign PASS, native deps 3/3, Hindsight recall PASS, no crash reports |
| 2026-07-19 | `runtime-promotion-frozen-patch-stack-20260719` | frozen canonical updater + 18-patch shadow-stack round trip + portable archive + Desktop/gateway restart/live smoke | Freezing one immutable base and separating runtime promotion from PR mergeability should eliminate moving-base regeneration while preserving all runtime contracts and portable recovery | base `e598cef874` → `local/hermes-patched@9b7257929`; shadow `local/hermes-patch-stack@9b7257929` | `--frozen-base`; builder `--no-fetch --no-discover`; mbox remains active authority, semantic branch is shadow-only | — | applied=46, skipped=4; patch gates 030=250, 040=427, 060=45, 070=208, 090=160+29+2, 110=37, 130 combined=455; archive 1568 files, 6344222 bytes, SHA-256 `7150144585…`; parity 31/31; thin bundle 424865 bytes; 18/18 round-trip tree identity | PASS | accept promotion and frozen mode; Desktop `87134→64436`, gateway `40964→63924→20198`, backend v0.18.2 HTTP 200, config 33/33, codesign/native deps/Hindsight/Basic Memory/Zotero PASS, no new crash reports; retain mbox authority until shadow cutover gates are repeatable |
| 2026-07-19 | `patched-runtime-control-plane-hardening-20260719` | temporary Git repositories, fake command runners, atomic-failure injection, profile/export durability suites | Verified-candidate reuse, a sealed prepare→promote transaction, durable shadow export, exact archive authority, and rollback state should remove redundant rebuilds without weakening fail-closed promotion | tooling-only; live runtime remained `local/hermes-patched@9b7257929` | durable candidate refs/fingerprint/CAS; sealed manifest/offline dependencies; shadow publication transaction; `last_attempt`/`verified_candidate`/`last_good`; Desktop backup plan | — | accepted matrix: 109 non-updater tests + 8 subtests, 12 updater tests, 8 exporter tests + 6 subtests; initial build-only 2 apply/1 verifier, matching promotion delta 0/0 | Independent review: six findings reproduced and closed; final narrow re-review `ACCEPT`; py_compile, shell syntax, whitespace, and `git diff --check` PASS | accept tooling implementation only; do not run a production update/restart; ordered mbox remains active authority pending another real upstream cycle |
| 2026-07-20 | `sealed-runtime-promotion-closeout-20260720` | sealed manifest promotion + frozen canonical install + 19-patch shadow publication + portable archive + Desktop/gateway live activation | Verified-candidate promotion should avoid redundant apply/verification, while a separate canonical frozen install/package gate should reproduce the same live tree without moving live or shadow refs | live and shadow `a49a0972b7056d948ee7d3848b4d8c9791b1d114`; tree `508cf3ddc37b425fd20dd4faf9fd89965ccabfa4` | transaction `bef65a7fb60de2c742b192ce`; manifest SHA-256 `2c9ec556be7bc7e0fa582c16f9683e410e333b7361905de00ec10ae6e0dca2bf`; override SHA-256 `de8285a21c1cac66a126484610365c1da5f5b1f96459620590fa996579ec9d7b` | — | promotion `applied=0, skipped=0`; frozen install `applied=43, skipped=4`; archive 1684 files, 7405088 bytes, SHA-256 `3736f03810e75c5515253da1427eb6fec7d5977f794519877e868e4b22a49d2a`; durability parity 50/50 | candidate/install/live tree identity, 19-patch shadow round-trip, bundle verification, and independent final review `ACCEPT COMMIT AND INSTALL` with no P0/P1; Desktop loaded the installed target; gateway `20198→40760` with clean zero-active drain; backend, memory, codesign, and native-dependency live smoke passed; no new crash after relaunch | accept promotion and full closeout; track recurring renderer SIGTRAP separately |
| 2026-07-20 | `sealed-runtime-update-cycle2-20260720` | authored-PR GraphQL scan + sealed candidate/promotion + 19-patch shadow publication + frozen Desktop install | A fresh sealed cycle on latest upstream should preserve all 19 local patch contracts; GitHub DIRTY alone should not block when exact integration patches replay | BASE `98cadadd84fb1354fa5e999ed0e2f0eb294dbeae`; PR_LAYER `c0733308d8acc0a0bc986ddf2c48084a1d0ed320`; live/shadow/install `d457d27a5dae383da4bc4408316c7780d061c962`; tree `a96b2b0a9a559746f655055abb6b13868d824661` | transaction `16d18c5c699095bdd56c2f2f`; manifest SHA-256 `d68088aad34904cf485709fd6d05e616e5f3cf2616901dbc944901851bdc4356`; 33 authored open PRs, 8 CONFLICTING/DIRTY all exact-pin covered by patches 030/040/060/070/090/110/150 | — | initial candidate `d457d27a`, applied=19/skipped=0; promotion delta=0/0; frozen install applied=43/skipped=4; patch130 commit `fd552bea2`, focused 7/7 + 55/55; archive 1709 files, 7829264 bytes, SHA-256 `6119165a255b1eccba0ac4a4978482fe4644c241d0fc316f5f12d1f0101dc2c8`; parity 50/50 | 0 UNKNOWN; 8/8 DIRTY pins matched; no PR replay blocker; independent review `ACCEPT` with no P0/P1; 19-patch round-trip/bundle, candidate/live/shadow/install tree identity, codesign, native deps, Hindsight, and durability all PASS | PASS — Desktop `39148→96363`, gateway `40760→96196`; zero-active drain (`timed_out=False`, active/cron/API=0); backend v0.18.2 config 33/33, fresh WebSocket, runtime import, Hindsight/Basic Memory, codesign/native deps, and crash delta 0 all passed; both one-shots booted out and production PIDs survived cleanup |
| 2026-07-20 | `terra-high-gui-route-20260720` | live orchestrator profile + route-aware fake OMP harness + real OMP Codex/Sudo smokes + isolated Hermes CLI smoke + direct Sudo Responses boundary/capability probes + portable export | GPT-5.6 Terra can become a third GUI-selected main route on either Codex or Sudo without changing Sol/GLM defaults, while preserving provider-specific context headroom and fail-closed unverified forms | profile wrapper on `fix/slash-worker-lifecycle-comprehensive@1965022a2`; OMP 17.0.5 | Codex Terra 272K/private auto overlay; Sudo Terra nominal 372K/fixed 315K; Hermes provider + OMP catalog + wrapper + portable parity | — | Codex OMP `TERRA_ROUTE_OK`; Sudo OMP `SUDO_TERRA_OMP_OK`; Hermes CLI `SUDO_TERRA_HERMES_OK`; Sudo high/xhigh HTTP 200 + exact `OK`; wrapper 32/32; registration 4/4; exporter 12/12; archive 1721 files, 7865013 bytes, SHA-256 `ce31274ceda2e8c470f599ff1dbe6f404edf3689025232492c8cb28f12722b77` | Sudo boundary: 371850 input + 5 output completed, 371855 + 3 became incomplete/max_output_tokens, next request context_length_exceeded; resolver Sudo `372000/.85/316200`, OMP fixed 315000; first full review `REJECT`, two blockers fixed, exact-session re-review `ACCEPT` | accept provider-distinct Terra base routes; keep Sudo Terra-pro and noncanonical explicit forms fail-closed; GUI-local preset storage remains separate from route integration |
| 2026-07-25 | `patch-replay-mechanical-benchmark-20260725` | three fresh detached worktrees at frozen base `3910ab28c`; exact 39-commit first-parent sequence to `12198c191` | If raw Git cherry-pick is the source of update latency, replaying the exact carried stack should itself take material wall time | `local/hermes-patched@12198c191` | Raw `git cherry-pick` of all 39 carried commits, excluding verification/build/package work | 1.829 s / 1.828 s / 1.639 s | exact target tree identity `3/3` | PASS | reject raw cherry-pick as the latency source; audit dependency restore, verification, packaging, conflict repair, and orchestration |
| 2026-07-25 | `dependency-snapshot-cost-20260725` | live owned dependency trees; exact `immutable_dependency_snapshot` implementation | Immutable dependency copying/hashing contributes measurable overhead but should be separated from raw Git replay | `local/hermes-patched@12198c191` | `.venv` / root `node_modules` / Desktop `node_modules` content hash; exact root `node_modules` owned-tree copy in a detached worktree | hashes 4.400 s / 12.119 s / 0.547 s; copy 31.912 s + cleanup 4.129 s | `.venv` 42,772 files / 553,286,203 bytes; root modules 90,837 / 1,118,299,317; Desktop modules 3,771 / 27,777,625 | PASS | retain immutable identity checks; remove redundant snapshots/hashes only through fingerprint-bound reuse, not by weakening verification |
| 2026-07-25 | `sealed-runtime-update-3910ab28-20260725` | frozen-base canonical updater, then exact-candidate retry after post-promotion dependency failure | A frozen updater should promote the verified 39-commit stack and complete runtime/dependency/Desktop publication without losing rollback authority | BASE `3910ab28c089`; target `12198c191e1`; pre-promotion target `a910b717068` | first PID `2697`; retry PID `8144`; 17 PR commits + 22 portable patches; 4 skips | first launch-to-exit 1131.900 s; retry 270.515 s; retry plan-to-complete 259.925 s | first run promoted exact target then exited 127 because `uv` was absent from PATH; retry reported inputs unchanged and completed dependency/Hindsight/Desktop publication | CONDITIONAL PASS | runtime/install succeeded, but post-CAS failure plus retry replaced the active rollback plan's target with the already-promoted SHA; fix transaction journal, preflight/timeouts, and rollback preservation before the next sealed promotion |
| 2026-07-25 | `updater-transaction-hardening-199f5580-20260725` | synthetic control-plane fixtures only; no live updater, ref promotion, App install, or service restart | Preserve original rollback authority across post-CAS retries and move eligibility/reuse decisions before expensive or irreversible work | tooling-only against frozen upstream `199f55805876`; frozen ref `refs/hermes-patched/frozen-bases/20260725-199f55805876` | append-only stage journal; original-plan retry binding; bounded process groups; pre-CAS tool/Desktop eligibility; read-only candidate reuse probe; transactional Desktop swap | — | frozen base/tree `199f55805876` / `683588e52d9c`; aggregate gate `Ran 144 tests in 1187.108s — OK` (exit 0); py_compile 7 files PASS; bash -n PASS; fresh Sol review `P0=0/P1=6/P2=0 REJECT` | iterate | automated gates passed but gave false confidence; historical P1 #1/#2 remain and four new P1s cover incomplete builder authority, updater serialization, manifest capture, and SIGKILL/power-loss Desktop recovery; no live runtime/ref/App/service touched |
| 2026-07-25 | `updater-hardening-sol-final-review-20260725` | frozen eight-file updater candidate plus aggregate log `77d1e82e667233d55894a5f5994878ce820fadc4a376dd3763f100d5cffe79f9` | A fresh GPT-5.6 Sol review should independently confirm that the final repaired candidate has no P0/P1 gaps before another sealed promotion | OMP `custom:sudo / gpt-5.6-sol` xhigh; live runtime stayed `local/hermes-patched@12198c191e1` | read-only source/contract/adversarial review; targeted historical tests; pre/post SHA-256 and ref verification | — | `VERDICT: REJECT`; `P0=0 P1=6 P2=0`; canonical-venv follow-up `Ran 2 tests in 15.266s — OK` | syntax 7/7 and `bash -n` PASS; 144-test aggregate log hash/result verified; all eight candidate hashes and live/frozen refs unchanged; two historical P1s still present plus four additional P1s | reject prior acceptance; iterate on all six P1s, then rerun targeted semantics, aggregate gate, and fresh independent Sol review |
| 2026-07-25 | `updater-six-p1-repair-20260725` | three sequential TDD batches against the frozen eight-file updater candidate | Closing all six Sol-review P1s with authority-, transaction-, and crash-recovery-specific tests should make the updater eligible for residual P2/P3 audit and a fresh final review | OMP `custom:sudo / gpt-5.6-sol` xhigh; no live updater/ref/App/service operations | Batch A authority #1/#2/#5 → Batch B transaction #3/#4 → Batch C Desktop crash recovery #6 → P2/P3 audit → exact replay and isolated stage-latency double-check | — | Batch A regression union `Ran 19 tests in 163.521s — OK`; Batch B focused gate `Ran 20 tests in 53.894s — OK`; Batch C `15 tests in 37.147s — OK` plus capsule helper `1 test in 7.265s — OK`; syntax/compile PASS | all six known P1s closed in focused gates; Batch C adds durable installer phases, exact stale-authority validation, real SIGKILL recovery, parent fsync, and capsule-owned Python helper; post-C SHA-256 frozen in `/tmp/hermes-updater-six-p1-post-batch-c.sha256`; runtime/frozen refs unchanged; residual P2/P3 audit pending | running |
| 2026-07-25 | `updater-residual-audit-20260725` | read-only post-Batch-C eight-file candidate | A complete residual audit should find only bounded P2/P3 issues before final gates | OMP `custom:sudo / gpt-5.6-sol` xhigh hard/read-only; candidate hash snapshot `/tmp/hermes-updater-six-p1-post-batch-c.sha256` | source/call-path/state-machine audit, focused tests, temporary reproductions, authority-size/per-pass timing probes | — | `ITERATE`; `P0=0 P1=5 P2=5 P3=1`; focused `Ran 5 tests in 5.484s — OK`; report SHA-256 `1c6cbd915a72f094680563598d3f59039c66c3ec9c49c558a0ffa9414b8d5fe7` | all eight candidate hashes and runtime/frozen refs unchanged; reproduced default-repo alias collision, post-promotion started-event wedge, committed-publication cleanup wedge, and unmeasured duplicate work; prior P1 repairs remain incomplete in authority, recovery, serialization, and file-data durability | iterate in three P1 batches, then fix reproduced P2s and add performance instrumentation before aggregate/final review |
| 2026-07-25 | `updater-residual-p1-repair-20260725` | three TDD repair batches for residual audit F1-F5 | Typed authority, ledger-derived recovery, transaction-coupled promotion intent, and file-data durability should close all residual P1s without broad rewrites | OMP `custom:sudo / gpt-5.6-sol` xhigh; no live updater/ref/App/service operations | P1-D typed authority/nested executables → P1-E stage recovery/promotion handoff → P1-F Desktop utilities/file durability | — | P1-D authority suite `11 tests in 69.991s — OK`; P1-E E1 `5 tests in 88.288s — OK`, E2 `4 tests in 79.080s — OK`, retry/state `12 tests in 71.677s — OK`, real builders/P1-D `10 tests in 50.501s — OK`; P1-F installer `21 tests in 51.268s — OK`, rollback durability/publication `10 tests in 0.218s — OK`; syntax/compile PASS | all residual-audit P1s closed in focused gates: typed authority, sealed nested executables, ledger recovery, transaction-coupled ref promotion, sealed Desktop operations, staged App identity/file fsync, and rollback-backup data durability; post-P1 hashes in `/tmp/hermes-updater-post-all-p1.sha256`; runtime/frozen refs unchanged | P1 repairs complete; proceed to reproduced P2 fixes, instrumentation, aggregate/benchmark/final review |
| 2026-07-25 | `updater-residual-p2-repair-20260725` | TDD repair of reproduced residual-audit P2s | Builder serialization, recovery-first installation, installer lifetime locking, phase-aware publication cleanup, and exact backup authority should close all concrete residual correctness/durability P2s | OMP `custom:sudo / gpt-5.6-sol` xhigh; no live updater/ref/App/service operations | P2-G F6/F8/F9 → P2-H F7/F10 → timing instrumentation | — | P2-G agent G1 `9 tests in 33.321s — OK`, updater CAS/intent `7 tests in 71.066s — OK`, installer `24 tests in 63.237s — OK`; independent G1 `9 tests in 32.211s — OK`, installer `24 tests in 57.332s — OK`; P2-H independent installer `29 tests in 68.889s — OK`, committed cleanup/precommit/retry `3 tests in 12.235s — OK`; syntax/compile PASS | P2-G closes F6/F8/F9; P2-H makes committed cleanup roll-forward/idempotent and binds installer backup permission to immutable rollback-plan snapshot with marker v3 + initial/staged/published revalidation; post-P2 hashes in `/tmp/hermes-updater-post-all-p2.sha256`; runtime/frozen refs unchanged | all concrete P2 correctness/durability repairs complete; add F11 instrumentation then final audit/gates |
| 2026-07-25 | `updater-f11-instrumentation-20260725` | final repaired updater candidate | Durable stage timing and operation counts should expose cold/reuse/retry cost without changing authority or recovery semantics | OMP `custom:sudo / gpt-5.6-sol` normal; exact-resume after timeouts; no live updater/ref/App/service operations | terminal-event metrics + builder child summary + updater summary → source-review closure for successful preflight | — | focused instrumentation `11 tests in 54.865s — OK`; post-closure P1-E `9 tests in 151.359s — OK`; P2-G/CAS/backup retry `8 tests in 27.648s — OK`; py_compile PASS | exact schema version 1; per-stage monotonic duration and command deltas; successful/failed/timed-out/skipped stages; current-attempt retry metrics; strict duplicate-key parsing; builder work counts; observational fields never gate execution; final candidate hashes `/tmp/hermes-updater-final-repaired-candidate.sha256`; refs unchanged | instrumentation complete; run optimality audit, benchmark, aggregate gate, and fresh final review |
| 2026-07-25 | `updater-optimality-audit-20260725` | final repaired candidate plus 3× isolated moving/sealed/retry and builder cold/reuse benchmark | After correctness repairs and instrumentation, a fresh hard audit should distinguish safety-required repetition from removable update/cherry-pick cost | OMP `custom:sudo / gpt-5.6-sol` hard/read-only; exact UUID `019f9afb-57d7-7000-8498-9b1a0c25ccc0`; no live updater/ref/App/service operations | full source/call-path audit; repeated temporary-fixture probes; benchmark artifacts under `runs/updater-optimality-benchmark/results/` | moving cold 7.923s; sealed cold 5.486s; retry completion 6.412s; builder cold/reuse 0.467s/0.350s | `ITERATE`; `P0=0 P1=3 P2=5 P3=1`; full report SHA-256 `6ce2b7e6c8e1b2b4f7e365f93c5247f8ae7704eac2e7dbbd9a3d1391990e6be2` | P1: completed history blocks next update; Desktop skip lacks exact installed-artifact authority; malformed Hindsight config fails open. P2: duplicate reuse planning, excessive scalar Git calls, duplicated rollback-plan state, early-path metric gaps, non-stage-aware retry preflight. P3: unbounded local inventory reads. Candidate/refs unchanged by read-only audit. | repair P1 in two batches, then accepted P2 survivors; rerun benchmark, aggregate gate, and fresh final review |
| 2026-07-26 | `updater-optimality-p2-l-20260726` | isolated temporary direct/sealed builder fixtures; exact operation-count measurement | A miss-only cheap gate plus invocation-local immutable Git batching should remove duplicate planning/subprocesses without caching mutable refs or weakening CAS freshness | tooling-only; live runtime stayed `local/hermes-patched@12198c191e1`; exact OMP UUID `019f9b67-49b8-7000-8f47-1ca0b0be07ec` | absent/stale reuse short-circuit; batched ref/object/commit reads; immutable SHA fact cache | direct cold `33/32 → 19/18` external/Git; direct reuse `26/26 → 13/13`; cheap miss `0/0`; sealed child `55/53 → 23/21` | independent decisive `11 tests in 9.548s — OK`; agent gates `37/37`, `4/4`, `2/2`, `2/2` OK; py_compile PASS | malformed/truncated/duplicate/missing/oversized batches fail closed; mutable refs remain freshly read before/after CAS, during rollback, and at final branch/ref validation; hashes `/tmp/hermes-updater-post-p2-l.sha256`; runtime/frozen refs unchanged | accept P2-L; proceed to state-size deduplication and bounded authority closure |
| 2026-07-26 | `updater-optimality-p2-m-20260726` | audit-scale synthetic legacy state with 1.839MB rollback payload plus full retry/state fixtures | Making `transaction.rollback_plan` the single authority and compacting history/root projections should eliminate whole-file write amplification without changing retry semantics | tooling-only; state schema version retained; no live updater/ref/App/service operations | remove plan/descriptor from `last_attempt` and root; explicit 13-field compatibility allowlist; migration on next successful CAS | serialized state `5,519,466 → 1,841,490 bytes` (`−66.636%`); plan occurrences `3 → 1` | independent PromotionState `39/39 OK`; new size/migration `2/2 OK`; post-promotion/dependency/Desktop/gateway retry decisive paths OK; py_compile PASS | legacy duplicated state remains readable; stale revision is byte-exact; successful transition preserves transaction deep equality while compacting compatibility copies; production authority readers use transaction only | accept P2-M; proceed to bounded authority-input closure |
| 2026-07-26 | `updater-optimality-p3-n-20260726` | bounded synthetic authority roots plus current gateway/Hindsight protected-root inventory | Finite shared traversal/JSON budgets should prevent local resource exhaustion while preserving exact valid descriptor/retry bytes | tooling-only; one aggregate budget per updater authority construction; no live updater/ref/App/service operations | regular files/bytes/per-file/depth/path/entries/deadline limits; strict bounded JSON readers; canonical capsule role precedence on source collisions | current selected gateway authority `2,152 entries / 30.7MB`; site-packages sample `6,915 / 190.8MB`; limits `250K files`, `4GiB aggregate`, `512MiB/file`, depth `64`, path `4096B`, `500K entries`, `120s` | independent bounds `8/8 OK`; state readers `5/5 OK`; capsule/retry `14/14 OK`; agent sealed drift `3/3`, PromotionState `44/44`; py_compile PASS | overflow/deadline/malformed JSON fail before effects without leaking contents; aggregate budget cannot reset per root; symlink/special files still fail closed; canonical role entry wins source-map collision; hashes `/tmp/hermes-updater-post-p3-n.sha256`; refs unchanged | accept P3-N; all known optimality-audit findings closed, proceed to final gates/review |
| 2026-07-26 | `updater-optimality-final-benchmarks-20260726` | final zero-P3 source; 3× isolated moving/sealed/retry/builder fixtures plus three detached exact 39-commit replays | Closing all audit findings should reduce repeated work without changing exact replay result or skipping safety-required validation | tooling-only; no live updater/ref/App/service operations; runtime/patch refs sampled before/after | final source benchmark versus frozen pre-repair artifact; raw first-parent cherry-pick from `3910ab28c` to target tree `1a4889fcc` | moving `7.923→6.840s` (`−13.7%`); sealed `5.486→3.758s` (`−31.5%`); retry completion `6.412→5.755s` (`−10.2%`); builder cold/reuse `0.467→0.302s` / `0.350→0.195s` | benchmark 3 repetitions/scenario exit 0; builder cold `19/18` external/Git, reuse `13/13`; exact replay `1.893/1.640/1.684s`, median `1.684s`, target tree `3/3 PASS` | final benchmark SHA-256 JSON `2d9a55cd…bf2f`, Markdown `4b9463d6…7aa5`; replay JSON `ebfc1ded…22b9`, Markdown `27640b4a…7bdc`; runtime HEAD and patch ref unchanged | retain required CAS/durability repeats; raw cherry-pick remains negligible; final five-module aggregate/review evidence follows |
| 2026-07-26 | `updater-final-review-p3-closure-20260726` | fresh-review P3 closure over bounded manifest reader and canonical test coverage | Single-FD bounded reads plus maintained direct lifecycle contracts should close the two review residuals without changing updater authority or stage behavior | tooling-only; exact reviewer UUID `019f9c07-9367-7000-908e-23ae82224113`; no live updater/ref/App/service operations | no-follow regular-file FD, max+1 chunk reads, pre/post identity; generic duplicate-key errors; retire obsolete WrapperTests; modern bounded-runner mocks; direct runtime-sync and first/second Hindsight verifier contracts | final benchmark moving/sealed/retry `6.840/3.758/5.755s`; builder cold/reuse `0.302/0.195s`; exact replay median `1.684s` | strict reader/new lifecycle focused `11/11 OK`; full legacy `54/54 OK`; full sealed `52/52 OK`; final five-module aggregate `331 tests in 1777.340s — OK`; py_compile PASS | aggregate log SHA-256 `cc2ad7bc…47e3`; all ten source/test hashes matched `/tmp/hermes-updater-final-zero-p3-candidate.sha256`; HEAD/patch ref `12198c191e1c`, frozen ref `199f55805876` unchanged | exact reviewer `ACCEPT`; `P0=P1=P2=P3=0`, no new actionable findings; addendum SHA-256 `dbfaa78f…8a88` |
| 2026-07-28 | `sudo-gpt56-272k-route-migration-20260728` | active orchestrator Hermes config, live OMP catalog, route-aware wrapper, focused tests, real Sudo Sol tool loop, and deterministic portable export | Moving every configured Sudo GPT-5.6 route from 372K to 272K while giving all supported 272K GPT routes OMP's automatic 15% reserve should preserve 15% headroom without changing GLM Heavy's 900K/315K policy | `local/hermes-patched@eafda57b2`; OMP 17.1.7; control base `3af596c86` | Sudo Sol/Sol-pro/Terra context `372000→272000`; Hermes remains 0.85; wrapper-owned per-run `thresholdTokens=-1` and `thresholdPercent=-1`; GLM global fixed 315K unchanged | real tool-loop smoke 10.817 s | exact `SUDO_GPT56_272K_TOOL_LOOP_OK`; archive 1,981 files / 9,559,166 bytes / SHA-256 `ccadfb34abefcfabe63b9c83cea773806bfef5f6dbf034b5ed960996ab302d6f` | Hermes resolver: Sudo GPT `272000/.85/231200`, GLM `900000/.35/315000`; registration 4/4; active wrapper 49/49; restored wrapper clean rerun 49/49; exporter 33/33; Hybrid 212/212; deterministic archive/parity/forbidden scans PASS; exact-session review `ACCEPT`, P0=P1=P2=0 | accept 272K migration; historical >272K probes remain evidence only and no longer define active capacity |

## Decisions

- **2026-08-03 — Tri-model MoA accepted as the default dev route:** the Hermes orchestrator runs `t9s/deepseek-v4-flash` (cheap controller, not an OMP route), while all coding goes through OMP with an explicit `--model` (route-following from a ds4 session exits 2 — verified 2026-08-03 canary). Every complex plan/spec/design is proposed in parallel by K3 + GLM-5.2 (via OMP) + the ds4 session itself (blind-sealed before reading the other two); Hermes consolidates to a DESIGN LOCK (never a 5th OMP); coding is a single K3 `--thinking max`; review/audit re-runs the same three and needs ≥2/3 ACCEPT. Trigger is one flat checklist (S1 irreversible / S2 ≥2 architectures / S3 ambiguous done / S4 blast radius / W1 ≥3 files / W2 no template / W3 weak verify-by-run) — ANY hit → three models, none hit → single K3 + Hermes self-review (confirmed 2026-08-03: no STRONG/WEAK split, no 2-model notch; token cost not a concern). Thinking effort is always `max` for K3/GLM-5.2; ds4 (backend `deepseek-v4-flash-dspark`) accepts `low|high|xhigh|max` with no observable difference, so `max` is the semantic ceiling (API probe 2026-08-03). Single source of truth: `coding-agent-delegation/references/tri-model-moa-workflow.md` (merged former `omp-route-model-topology.md`); mirrored in MEMORY, USER, and Basic Memory `agent-memory/hermes/tri-model-mo-a-default-dev-route-runbook`. **Decision: accepted and persisted.**
- **2026-08-03 — DeepSeek V4 Flash adopts true 1M window / 0.6 compression trigger, no long-context false-stop:** the per-model override `custom_providers.sudo.models[t9s/deepseek-v4-flash].context_length` was corrected from 128K to 1M (the stale 128K was overriding the top-level 900K and the model's real 1M spec), and `compression.model_thresholds.deepseek-v4: 0.6` was added so compression triggers at 600K, aligning with kimi-k3's 350K/0.6 guarded-window policy. Unlike K3's KDA linear attention (which false-stops on long context — Hermes stop-detection root cause), DeepSeek V4 uses CSA+HCA attention built natively for 1M, and official guidance recommends ≥384K context for Think Max (reasoning trace itself consumes the window). Sudo router probes: 225K input + 200K max_tokens accepted; `reasoning_effort` none disables thinking (0 tokens) while low≈max normalizes (3895 vs 3819 tokens), so Hermes cannot steer effort without the `extra_body` workaround (`_supports_reasoning_extra_body`=False for custom:sudo). DeepSeek is in the echo-back reasoning family (`needs_reasoning_echo=True`, `reasoning_echo: true` active) but the router does not enforce echo, so no K3-style long-context failure mode. **Decision: accept** — config verified via `hermes config` exit 0 and `resolve_model_threshold()` returning 0.6 / 600K trigger; reasoning-effort workaround left unapplied pending user decision.
- **2026-07-30 — Hybrid core patch family intentionally retired before activation:** the operator will continue with `coupled-v1`, so patches `190`, `200`, `205`, `210`, `215`, `220`, and dependent first-row workflow patch `240` were removed from the authoritative ordered-mbox set. A new same-base `--intentional-retirement` control-plane mode binds the exact current base/PR layer, retained order/stable IDs, optional SHA-256 proof mboxes, round-trip tree, thin bundle, manifest-last publication, and shadow CAS/rollback. The 25-commit candidate/shadow is `e2ddfe50a5ed` with tree `1df1fbb1f3d9`; retained patches `225`, `230`, and `250` preserve their stable IDs. The old 32-patch authority and retired bytes remain preserved under the run evidence directory. Active and archived defaults are `coupled-v1`; deterministic portable archive SHA-256 is `f31319e241a87ab2bfcafa00790b50e4842d3f5e00f9b221577767a4220e7007`. Fresh Sol audit returned `ACCEPT`, `P0=P1=P2=P3=0`. `local/hermes-patched` remains `1606b492ba92`; no updater, Desktop install, gateway restart, or Desktop restart occurred.
- **2026-07-29 — K3 promotion and audit closure accepted before activation:** sealed manifest v9 completed at `local/hermes-patched@fb950702ef672ccad93dfc7cb33b1c346a79f079`, whose tree matches the shadow and prior candidate. The target session's persisted `coupled-v1` route remains intact; its continuation blocker was the post-promotion Hindsight command using the sealed Python without `hindsight_embed`, not a lost session workflow. The updater now retains sealed patcher/verifier scripts and launcher authority while selecting the promoted repository's live venv through `uv --python`; Hindsight post-sync, dependency check, and runtime verification passed. The profile now binds K3 Vision to canonical `custom:sudo-kimi-k3` while preserving `custom:sudo / kimi-k2.7-code` fallback and the `custom:sudo / gpt-5.6-sol / hybrid-v1` defaults. Code artifact `0753492adcdbf22e2a3e8eb42ec0889d4e6ffb85` seals the exact preparation-tree exclusion and deterministic archive. Fresh Sol reviewer UUID `019fadd2-571d-7000-aa8a-97ba9f3e97a7` returned `ACCEPT`, `P0=P1=P2=P3=0`. Desktop installation plus Desktop/gateway restart were intentionally skipped and still require explicit approval.
- **2026-07-29 — K3 manual picker route and initial workflow persistence accepted as artifacts:** the active profile exposes `custom:sudo-kimi-k3 / t9s/kimi-k3` through `chat_completions` at `350000/.90/315000`, while `custom:sudo / gpt-5.6-sol` and `coding_workflow.default=hybrid-v1` remain unchanged. Patch `240-coupled-workflow-first-row-persistence.patch` validates and persists a non-empty canonical workflow in the initial Desktop DB row; a real patched-source create/turn/resume/turn probe preserved `coupled-v1` despite the Hybrid profile default. Existing historical rows with no workflow field are not migrated. `local/hermes-patch-stack` is staged at `fd096358e5d7470f77a525b34d51d6f48d4876dc`, while live `local/hermes-patched` remains `eafda57b2051000212c688e3b72cddcf386c114f`; activation requires a separately approved promotion/restart.
- **2026-07-29 — Daily audit hardening accepted:** the live collector and cron prompt now distinguish Desktop-only ownership, historical cumulative counters, manual cron runs, local patch-stack commits, compact FTS layout, and negligible SQLite reclaimable pages. The sealed patch authority validates 31 patches at shadow `fd096358e5d7`, including `225`/`230` and the independently queued `240`; live runtime remains `eafda57b2051`, so no promotion is inferred. The portable exporter excludes generated `hermes-patched-state.*.json` history while retaining declared durability artifacts. Four previously ≥90KB active skill mains were split into exact reference files with byte-preservation checks, reducing `high_size_count` to zero. No Desktop/gateway restart or destructive snapshot/session cleanup was performed.
- **2026-07-28 — Hybrid activated as the orchestrator profile default:** by explicit user direction, active and portable `coding_workflow.default` are now `hybrid-v1`. Future new sessions resolve to the fixed `custom:sudo / gpt-5.6-sol / xhigh` controller; existing sessions keep their own persisted workflow tuple. Artifact commit `b35df8d1973f5ca4156fa1d6a2e11adcabb6d9fc` changes only `orchestrator/config.yaml` relative to the accepted archive, producing SHA-256 `e0b741e34cc3758f987b9b7bbb50c7968d9735636516df3eb9c53ed348741b33`. No Desktop or gateway restart was performed.
- **2026-07-28 — Current combined Hybrid artifact accepted:** commit `6a72f3839ca6cf9eb55b637c5d9fc7d608838ed3` seals the artifact-level behavioral oracle and portable archive SHA-256 `5da62b8eaf5e208fcfd14f35c474798f41dc451877b7b49e560d8593a63f9943`. At audit time the live Desktop session was `hybrid-v1 / custom:sudo / gpt-5.6-sol / xhigh` while the profile default remained `coupled-v1`; the later user-authorized activation changes only that rollout setting. A real implement run resolved to `sudo/glm-5.2-heavy / max`, timed out internally once, resumed the exact UUID `019fa8d6-16a0-7000-92c7-ee264653cf39`, and completed without Sol fallback. Fresh hard re-review UUID `019fa8f4-c6a7-7000-8512-387c7d8e0f0f` returned `ACCEPT`, `P0=P1=P2=P3=0`. This supersedes the old 272K archive's combined-artifact acceptance while retaining its 272K route semantics.
- **2026-07-28 — K3 promoted to primary Vision with Sudo K2.7 fallback:** the active `orchestrator` profile now resolves `auxiliary.vision` to `custom:sudo / t9s/kimi-k3` over `chat_completions`; its task-specific fallback chain contains `custom:sudo / kimi-k2.7-code` with the same transport and a 120-second candidate timeout. The runtime's model-scoped failure identity permits a sibling model on the same named provider for connection, timeout, rate-limit, model-incompatible, and invalid-response failures; provider-wide auth/payment failures intentionally skip that sibling because both routes share credentials/account capacity. Qwen remains opt-in only.
- **2026-07-28 — Sudo GPT-5.6 272K migration accepted:** active Hermes metadata for Sol/Sol-pro/Terra is 272K and `.85` now triggers at 231.2K. The route-aware wrapper applies a private automatic-reserve overlay to every canonical supported 272K Sudo/Codex Sol/Terra route after caller overlays, while GLM Heavy remains 900K with the global fixed 315K trigger and receives no overlay. This supersedes the 2026-07-20 372K/fixed-315K operational policy; its direct boundary probes remain historical capability evidence. No Desktop or gateway restart was performed, so the current in-flight chat retains its creation-time context object; new sessions use the migrated config.
- **2026-07-26 — Updater optimality campaign accepted:** all original and review-discovered P0–P3 findings are closed; final five-module aggregate is `331/331 OK`, exact 39-commit replay is `3/3` tree-identical, and exact fresh Sol reviewer UUID `019f9c07-9367-7000-908e-23ae82224113` returned `ACCEPT` with `P0=P1=P2=P3=0`. Raw replay is not the dominant cost; retain safety-required CAS, durability, receipt, Hindsight, and Desktop verification repeats.
- **2026-07-13 — Kimi K2.7 promoted to primary (superseded 2026-07-28):** the active `orchestrator` profile used `custom:sudo / kimi-k2.7-code` for `auxiliary.vision` until K3 replaced it as primary.
- **Transport is explicit:** `auxiliary.vision.api_mode=chat_completions` is required because the named Sudo provider defaults to `codex_responses`, which is not the image-input transport used by `vision_analyze`.
- **No automatic Qwen failover:** `auxiliary.vision.fallback_chain` now contains only Sudo Kimi K2.7. Qwen2.5-VL-32B remains available as the local standby only after explicit user confirmation if both Sudo Kimi routes fail.
- **No core-code change:** configuration and persistent routing policy were sufficient; the runtime checkout stayed clean at `b7f8bf659`.
- **2026-07-14 — Memory P1 accepted and promoted:** final independent review returned `ACCEPT`; `130-memory-session-isolation-durability.patch` is part of the required `100/110/120/130` durability chain and promoted at `87b8d62f0`.
- **2026-07-14 — Sudo GLM-5.2 Heavy has a real 1M total window:** two independent output-budget boundaries resolve to exactly 1,000,000 tokens after accounting for six unreported protocol tokens. The `glm-5.2-heavy` alias reports the underlying response model as `glm-5.2`.
- **2026-07-14 — Adopt 900K/315K operational policy:** Hermes and OMP use a 900K Heavy client cap while retaining a roughly 315K normal working set. Capacity metadata increases; routine compression timing remains aligned with both GPT routes.
- **2026-07-14 — Do not add a non-heavy reasoning override:** explicit `reasoning.effort=none` did not improve the approval auxiliary in a controlled smoke A/B. `approvals.timeout` controls only subsequent human waiting, not the smart-approval LLM call; do not raise it to compensate for auxiliary latency.
- **2026-07-14 — Bound smart-approval attempts at 20 seconds:** active `auxiliary.approval.timeout` changed from 30 to 20 while retaining `approvals.timeout=60` for human waiting and the default two transient retries.
- **2026-07-14 — Hybrid OMP policy:** GLM Heavy is a fast implementation executor, not a drop-in one-pass substitute for GPT-5.6-sol xhigh on concurrency/lifecycle/security invariants. Use GPT Hermes to specify acceptance criteria, generate adversarial verification, and iterate GLM until clean; route the hardest architecture/debug reviews directly to GPT OMP.
- **2026-07-19 — Freeze every promotion transaction:** fetch/discover once, seal the 40-character BASE and PR snapshot, then prohibit fetch, live discovery, PR rebase/force-push, and symbolic verifier bases until promotion closes. GitHub `DIRTY` alone is no longer a runtime-promotion blocker.
- **2026-07-19 — Patch workflow verdict is staged REDESIGN:** retain the proven detached/fail-closed builder, but move toward `BASE → PR_LAYER → local/hermes-patch-stack → local/hermes-patched`. The 18 semantic commits, manifest, thin bundle, and mbox round-trip are now shadow-verified; generated mboxes remain authoritative until repeatable cross-cycle parity and rollback gates justify cutover.
- **2026-07-19 — Control-plane tooling accepted, authority unchanged:** verified candidates are durable and reusable by exact fingerprint/tree/ref identity; sealed manifest promotion is deterministic and package-manager-offline; shadow publication and rollback state are transactional. This acceptance covers tooling and synthetic contracts only. It does not authorize shadow-stack cutover or require a canonical update, Desktop install, or service restart.
- **2026-07-20 — Sealed runtime promotion accepted:** exact candidate promotion and the frozen canonical install completed at `a49a0972b7056d948ee7d3848b4d8c9791b1d114`; live/shadow refs and the install tree agree. Desktop relaunched at `15:38:43` after the old renderer's unplanned `SIGTRAP` at `15:35:19` and loaded the already installed bundle; this was not a rebuild. The approved gateway restart changed PID `20198→40760` with a clean zero-active drain, and live backend, memory, codesign, native-dependency, and import smoke passed. Decision: **accept promotion and full closeout**; investigate recurring renderer SIGTRAP separately.
- **2026-07-20 — Provider-distinct Terra routes accepted:** OpenAI Codex Terra remains a 272K route with a private 231.2K auto-compaction overlay and durable Codex cache. Direct Sudo probes establish a nominal 372K endpoint window: 371,850 input + 5 output completed, 371,855 + 3 became incomplete with `max_output_tokens`, and the next deterministic request failed `context_length_exceeded`; separate `high` and `xhigh` requests both completed with exact model echo and `OK`. Hermes therefore registers only Sudo Terra base at 372K/.85 (316.2K), while OMP registers `sudo/gpt-5.6-terra` with fixed 315K compaction. The wrapper accepts base Terra through `custom`, `custom:sudo`, `sudo`, and canonical explicit `sudo/gpt-5.6-terra`; Sudo Terra-pro and noncanonical explicit forms remain fail-closed. Sol/GLM defaults, auxiliary, delegation, cron, and Hindsight remain unchanged.
- **2026-07-25 — Raw cherry-pick is not the slow stage:** three exact isolated replays of all 39 carried commits completed in 1.639–1.829 seconds and reproduced the target tree every time. The perceived latency belongs to surrounding conflict repair, dependency restore/snapshotting, exact-base suites, Hindsight publication, Desktop packaging, and review/orchestration. Immutable dependency work is measurable (root `node_modules` owned-tree copy 31.912 seconds), but the current evidence does not support attributing multi-minute or multi-hour wall time to Git replay itself.
- **2026-07-25 — Canonical updater is not yet safely retryable:** the first frozen run promoted `12198c191e1` and then failed because `uv` was not on PATH; the exact-candidate retry completed, but its newly generated rollback plan treats the already-promoted target as the old target. `last_good.old_target_sha` still records `a910b717068`, while the active retry plan no longer does. Before another sealed promotion, preserve the original transaction/rollback plan across retries and add pre-CAS dependency/toolchain eligibility checks plus bounded stage deadlines.
- **2026-07-25 — Updater transaction hardening requires iteration:** the 144-test aggregate gate (`Ran 144 tests in 1187.108s — OK`) and syntax gates remain valid, but a fresh `custom:sudo / gpt-5.6-sol` xhigh review returned `REJECT`, `P0=0/P1=6/P2=0`. Historical P1 #1 (complete retry authority) and #2 (publish only after durable plan commit) remain open; four additional P1s cover incomplete builder-capsule authority, missing updater-wide serialization/CAS, uncaptured sealed manifest authority, and non-durable Desktop recovery across SIGKILL/power loss. Automated gates gave false confidence, so the previous acceptance is withdrawn. No live runtime promotion, ref mutation, App install, or service restart was performed; frozen base `199f55805876` and runtime HEAD `12198c191e1` remain unchanged.

## Evidence

- Kimi K3 picker and workflow persistence:
  - Target session `20260728_134306_d0bce9` persisted `coding_workflow=coupled-v1`. Its continuation blocker reproduced at promotion stage `hindsight_post_sync`: the execution capsule rewrote the repository venv Python to the sealed runtime, which could not import `hindsight_embed`. The repaired updater keeps patcher/verifier scripts and the `uv` launcher sealed, but binds patch/check/verify commands to the promoted repository's `venv/bin/python` with `UV_PROJECT_ENVIRONMENT`, `UV_PYTHON`, `PYTHONNOUSERSITE=1`, `--no-sync`, and sealed-mode `--offline`. Focused 2/2 and bounded 8/8 updater tests passed.
  - Sealed promotion v9 transaction `6220cee353d6089033a4e4f1` completed all candidate, dependency, and Hindsight stages at `local/hermes-patched@fb950702ef672ccad93dfc7cb33b1c346a79f079`; target/shadow/prior-candidate tree identity was `2bc2da33f028a55f528be3341cf000710078e7c7`. `uv pip check` verified 98 packages and the Hindsight runtime verifier passed. Desktop install, post-Desktop Hindsight, and gateway restart were deliberately recorded as skipped.
  - The audit initially rejected generic Vision provider identity, stale portable updater bytes, and unbounded preparation-tree export. Hybrid GLM implementation UUID `019fadb7-3fef-7000-b71b-6f61fa0ff6c8` then completed two exact-resume repair rounds: exact direct-root path confinement, unrelated/nested same-name preservation, root-symlink rejection, internal-symlink tolerance, and overlay rejection. Independent exporter rerun produced byte equality at 2,053 files, 10,090,010 bytes, SHA-256 `5eb5bba633b53cbcfb27052e74dcae130700eb9a6ff2f9519c76d5d2e678ba0a`; preparation members were zero and all 31 patch-stack members remained valid.
  - Fresh hard-tier Sol audit UUID `019fadd2-571d-7000-aa8a-97ba9f3e97a7` reviewed provider/fallback scoping, Hindsight live-venv authority, exact archive confinement/determinism, portable parity, and first-row workflow persistence. Its fresh checks passed exporter 39/39 and workflow 4/4, and it returned `VERDICT: ACCEPT`, `P0=0 | P1=0 | P2=0 | P3=0`.
  - Active configuration resolved `custom:sudo-kimi-k3 / t9s/kimi-k3` to `https://coding.sudoai.cc/v1` over `chat_completions`; the picker row displayed `Sudo Kimi K3`. The actual `AIAgent` compressor reported context `350000`, threshold `.90`, trigger `315000`, and headroom `35000`. Profile defaults remained `custom:sudo / gpt-5.6-sol` and `hybrid-v1`; automatic model fallback remained empty.
  - Real Hermes tool loop used `read_file` and returned exact `K3_HERMES_FINAL_TOOL_OK` with exit 0. Stored OMP JSONL recorded model `sudo/t9s/kimi-k3`, thinking level `max`, `read` toolCall → toolResult, and exact final `K3_OMP_NORMAL_TOOL_OK`.
  - Runtime TDD RED produced two intended failures: `None != 'coupled-v1'` and `None != 'hybrid-v1'`. GREEN source suites passed 54/54. The real patched-source Desktop probe ran one K3 turn before and after resume and returned exact create/resume markers; with profile default `hybrid-v1`, its first row stored `coupled-v1` and resume restored `coupled-v1`, `custom:sudo-kimi-k3`, `t9s/kimi-k3`, and `chat_completions`.
  - Semantic runtime commit `fd096358e5d7470f77a525b34d51d6f48d4876dc` became shadow patch `240-coupled-workflow-first-row-persistence.patch` (SHA-256 `27951db317b537dc2a3880e0bd59527a32231469616958e135380a9ed09823d8`). The maintainer reported 31 patches and tree `2bc2da33f028a55f528be3341cf000710078e7c7`; clean apply, exact manifest set, and thin-bundle verification passed. Live runtime stayed at `eafda57b2051`.
  - The broad wrapper attempt exposed a known timing-invalid assertion: when two runs intentionally shared one output path, the active first process could legally append `FAKE_OMP_OK` while the rejected second process was being checked. The regression now asserts unchanged fake-OMP start markers and preserves byte equality for independent output paths; the formerly failing node passed 1/1. K3-specific profile contracts passed 10/10.
  - Portable exporter RED proved generated `.patch.pre-*` backups leaked from `scripts/hermes-patches` and overlays did not reject them. GREEN exporter suite passed 33/33; deterministic double export produced 2,039 files, 9,961,234 bytes, SHA-256 `e7c3e7b4888294c31ead1707334b4dc9af1015064158b8e5b761e7ba4b66d612`, with zero generated state snapshots or patch-pre backups and exact 31-patch manifest coverage.

- Sudo GPT-5.6 272K route migration:
  - Active config check: `hermes --profile orchestrator config check` returned config version 33 with exit 0. Live resolver output was Sol/Sol-pro/Terra `context=272000 threshold=0.85 trigger=231200`; GLM Heavy remained `900000/0.35/315000`.
  - OMP catalog: filtered `omp models --json` returned both Sudo `gpt-5.6-sol` and `gpt-5.6-terra` with `contextWindow=272000`; the exact registration contract retained `maxTokens=65536` and `openai-responses`.
  - Wrapper gates: both copies passed `bash -n` and byte identity; registration unittest `Ran 4 tests — OK`; active wrapper unittest `Ran 49 tests — OK`. The wrapper appends a mode-0600 `thresholdTokens=-1` / `thresholdPercent=-1` overlay after caller overlays for canonical supported Sudo/Codex Sol/Terra routes; GLM receives no overlay. Global OMP `thresholdTokens=315000` and the Codex context cache were byte-unchanged.
  - Real route-aware smoke: `custom / gpt-5.6-sol / mechanical` performed `read(probe.txt)`, recorded toolCall → toolResult → final assistant text in OMP JSONL, and returned exact `SUDO_GPT56_272K_TOOL_LOOP_OK` in 10.817 seconds.
  - Independent review: initial 300-second read-only review timed out after evidence collection; exact UUID `019fa88b-a70f-7000-a83a-33cdf6e3770b` was resumed with synthesize-only instructions and returned `VERDICT: ACCEPT`, P0/P1/P2 none.
  - Portable persistence: Python 3.11 exporter suite `33/33 OK`; deterministic double export produced identical 1,981-file archives, 9,559,166 bytes, SHA-256 `ccadfb34abefcfabe63b9c83cea773806bfef5f6dbf034b5ed960996ab302d6f`. All 18 migration overlay files were byte-identical in the archive; forbidden-member and personal-literal scans passed. Restored registration 4/4, Hybrid instructions 10/10, Hybrid policy 202/202, and restored wrapper clean rerun 49/49 passed. One prior restored wrapper run hit its known hard-slot timing flake; the failing node passed 1/1 in isolation before the clean full rerun.

- Terra High GUI route:
  - Catalog: OMP 17.0.5 reported `openai-codex/gpt-5.6-terra contextWindow=272000`; fixed 315K would clamp to 271999, while the selected 15% reserve triggers at 231200.
  - Focused gates: both wrappers passed `bash -n` and byte identity; wrapper unittest `Ran 32 tests — OK`; Sudo registration `Ran 4 tests — OK`; portable exporter unittest `Ran 12 tests — OK`; negative copied-profile isolation failed against the copied broken config as required; full four-model portable OMP provider parity passed.
  - Context check: six canonical GPT-5.6 Codex Sol/Terra/Luna base/pro cache keys equal 272000, no GPT-5.6 trailing-slash duplicate exists, and the Hermes resolver returned 272000 for Terra.
  - Independent review first rejected the explicit-route bypass; the fix made canonical explicit Codex Terra receive the overlay and rejected every other explicit Terra/Terra-pro form. Fresh focused re-review returned `ACCEPT` with no blocking finding or missing test.
  - Real OMP smoke command used the route-aware wrapper with `--hermes-provider openai-codex --hermes-model gpt-5.6-terra --task-tier mechanical`; result `TERRA_LIVE_SMOKE_RC=0` and exact output `TERRA_ROUTE_OK`.
  - Portable export: deterministic double export and archive parity passed; `PASS: exported 1721 files`; size 7865013; SHA-256 `ce31274ceda2e8c470f599ff1dbe6f404edf3689025232492c8cb28f12722b77`.
  - Sudo capability probes: identical Hermes-compatible Responses requests with `instructions`, list-form `input`, `store=false`, and streaming returned HTTP 200 / `completed` / response model `gpt-5.6-terra` / exact `OK` for both `reasoning.effort=high` and `xhigh`.
  - Sudo context boundary: deterministic `x ` padding with `reasoning=none` returned input/output `371850/5` completed, `371855/3` incomplete with `max_output_tokens`, and the next request failed `context_length_exceeded`. Operational policy is nominal context 372000, Hermes threshold `.85` → 316200, OMP fixed trigger 315000.
  - Sudo live route: route-aware OMP smoke used `--hermes-provider custom:sudo --hermes-model gpt-5.6-terra --task-tier mechanical`, returned rc 0 and `SUDO_TERRA_OMP_OK`; isolated Hermes CLI smoke returned `SUDO_TERRA_HERMES_OK`.
  - Independent full review initially returned `REJECT`: candidate-profile tests could pass against live state and the portable OMP template would drop Sol/base GLM. Strict-TDD fixes made the test profile-relative and the template a full four-model semantic mirror; exact-session re-review returned `VERDICT: ACCEPT`.
  - Sudo `/models` returns HTTP 400, so the manually curated Hermes/OMP model lists are not authoritative capability discovery surfaces. Earlier raw string-input probes also returned 400 for both Sol and Terra and were discarded as an invalid request-shape comparison.

- Route smoke command: `HERMES_HOME=~/.hermes/profiles/orchestrator ~/.hermes/hermes-agent/venv/bin/python /tmp/hermes-verify-kimi-vision.py`
  - Result: `provider=custom:sudo`, `model=kimi-k2.7-code`, `api_mode=chat_completions`, `analysis=Red`, `VISION_VERIFY=PASS`.
- Failure smoke command: isolated temporary profile with unreachable `http://127.0.0.1:9/v1`, then `~/.hermes/hermes-agent/venv/bin/python /tmp/hermes-verify-kimi-failure.py`.
  - Result: `success=false`, `connection_error=true`, `mentions_qwen=false`, `CONNECTION_FAILURE_VERIFY=PASS`.
- Active profile config assertions:
  - `provider=custom:sudo`
  - `model=kimi-k2.7-code`
  - `api_mode=chat_completions`
  - per-task `base_url` and `api_key` empty, so the named provider resolves `SUDO_CODING_KEY`
  - no configured `fallback_chain`
- Memory P1 verification:
  - Source commit: `d785d2bf4`; portable patch SHA-256: `77043ede23aa80269f8360999062bb894c5cad9d292e7e83803550341bab10f5`.
  - Final P1 matrix: `442 passed in 71.59s`; promoted-runtime focused smoke: `203 passed in 31.48s`.
  - Profile infrastructure: `86 tests` PASS; exporter: `6 tests` PASS; durability: `7 tests` PASS.
  - Canonical updater promoted `local/hermes-patched@87b8d62f01976018942b5594db08a1bba4a66b85`, `applied=41`, `skipped=2`.
  - Runtime verifier: `hindsight-client=0.8.4`, `hindsight-all=0.8.4`, `min_scores=client+embedded`, `lazy_deps=satisfied`, `embedding_cpu=profile:hermes`.
  - Portable archive: `15/15` parity, SHA-256 `7194961f1a8ff7751efb69ba02bbfac9ba8b704107bf432b7e45c19031647bca`.
  - Post-restart live smoke: Desktop PID `96556`; orchestrator/default serve PIDs `96580`/`96583` on dynamic ports `61305`/`61306`; both `/api/status` endpoints returned HTTP 200 with version `0.18.2`; `gui.log` recorded `ws accepted` at `2026-07-14 17:12:25.646 CST`.
  - Orchestrator gateway restarted independently from PID `69951` to `97471`; shutdown drain reported `timed_out=False` with zero active runs, and the new process started housekeeping plus the embedded Kanban dispatcher.
  - Hindsight `/health` returned `healthy` with database connected; live recall returned the 130 invariants. Basic Memory returned `Hermes memory architecture and recall isolation`. No new Hermes crash report appeared after restart.
- Sudo GLM-5.2 Heavy context boundary:
  - Calibration: 10,000 padding units → `input_tokens=10,030`, HTTP 200 in 0.81 s; response model `glm-5.2`.
  - Large cold-prefill successes: `600,030` input, cache 0, 53.52 s; `999,957` input + 16 output, cache 0, 127.29 s; `999,993` input + 1 output, cache 0, 170.86 s.
  - `max_output_tokens=1`: `input=999,993` succeeds; `input=999,994` fails twice with proxy HTTP 502 wrapping upstream HTTP 400.
  - `max_output_tokens=16`: `input=999,978` succeeds; `input=999,979` fails twice with the same error shape.
  - Both boundaries imply `reported_input + requested_output + 6 hidden protocol tokens <= 1,000,000`.
- GLM-5.2 Heavy 900K/315K config application:
  - Backup: `/tmp/hermes-glm52-context-migration-20260714-175650`, mode `0700`; files mode `0600`.
  - Hermes semantic diff: only `compression.threshold: 0.75 → 0.35` and Heavy `context_length: 340000 → 900000`.
  - OMP model semantic diff: only Heavy `contextWindow: 340000 → 900000`; `compaction.thresholdTokens=315000` added through `omp config set`.
  - Resolver: Heavy `900000/.35/315000`; Sudo GPT-5.6 `372000/.85/316200`; OpenAI Codex GPT-5.6 `372000/.85/316200`.
  - A stale `gpt-5.6-sol@https://chatgpt.com/backend-api/codex: 272000` profile cache row was backed up and removed; the source fallback rebuilt it as 372K.
  - Isolated route-aware OMP smoke: wrapper rc 0; model `sudo/glm-5.2-heavy`; API `openai-completions`; output `OMP_GLM52_900K_CONFIG_OK`.
- Non-heavy GLM auxiliary audit:
  - Active base-model slots: `web_extract`, `skills_hub`, `approval`, `mcp`, `title_generation`, `tts_audio_tags`, `profile_describer`, and `monitor`; all use `custom:sudo / glm-5.2` with empty configured fallback chains.
  - `context_length=1000000` is resolver/fallback metadata and is not sent with isolated auxiliary requests; reducing it to 900K would not improve latency.
  - Sudo auxiliary calls use `CodexAuxiliaryClient`; caller `max_tokens` and temperature are intentionally omitted on the Responses wire. No task currently specifies `extra_body.reasoning`.
  - Current `agent.log` window (`10:47:46`–`18:24:41`): approval 152 calls, compression 37, title generation 1. Approval had two 30s timeouts (1.32%) and two main-model fallbacks (1.32%).
  - Approval A/B: default 4/4, median 1.134s; explicit `none` 4/4, median 1.207s; both produced exact `APPROVE` and 174–251 hidden output tokens.
  - Timeout scopes are independent: `auxiliary.approval.timeout` bounds each smart-approval LLM attempt, while `approvals.timeout=60` starts only if the LLM escalates/denies and Hermes waits for a human. Raising the latter does not help the LLM call.
  - Applied `auxiliary.approval.timeout: 30 → 20`; semantic config diff contained only that field. Backup: `/tmp/hermes-aux-approval-timeout-20260714-184429.yaml`.
  - Live no-explicit-timeout smoke resolved `effective_timeout=20.0`, returned exact `APPROVE` in 3.516s, and reported `AUX_APPROVAL_TIMEOUT_SMOKE=PASS`.
- Controlled OMP executor A/B:
  - Fixture root: `/var/folders/fh/7dlfvrsn5938lw_4z6_pg_th0000gn/T/hermes-omp-model-ab-f6h3go3a`; both arms began from the same RED baseline (`2 failures, 3 errors`) and received the same normal-tier prompt with a 300s budget.
  - GPT control: 257.73s; effective `xhigh`; public GREEN; hidden 5/5; final 16/16. Session usage: 46,090 uncached input, 406,528 cache-read, 9,853 output, 6,575 reasoning tokens.
  - GLM first pass: 58.64s (4.40× faster); session recorded effective `high`; public GREEN but initial hidden 1/4. Its 12 self-authored concurrency tests did not catch three specification-level races/eviction errors.
  - GPT review identified the missing post-barrier membership semantics and expired-entry capacity issue. GLM repair 1 took 22.09s and reached the then-visible 21/21, but GPT code review generated a second-order interleaving where old-flight completion unregistered a live post-barrier flight; the added hidden test failed.
  - GLM repair 2 took 12.56s and fixed identity-guarded flight removal. Final suite: 22/22. Cumulative GLM child wall: 93.29s, 2.76× faster than the GPT OMP first-pass control (63.8% child-wall reduction).
  - This is one normal-tier concurrency fixture, not a universal model score. It measures the current route policy, including the observed GLM `high` vs GPT `xhigh` effective effort difference.

- Patched-runtime control-plane hardening:
  - Candidate benchmark: first build-only `2 apply / 1 verifier`; promotion with identical immutable inputs `0 apply / 0 verifier`.
  - Protected inputs include exact BASE, ordered PR commits/refs, overrides, ordered patch hashes, builder/updater/verifier source hashes, verification commands, and dependency locks. Candidate commit/tree and durable ref are rechecked around target CAS.
  - Sealed manifest mode performs no fetch, GitHub query, live PR discovery, or online package-manager operation; `npm`, `uv sync`, and local-Hindsight restore are forced offline, and the unsealed Desktop installer path is refused.
  - Post-CAS validation restores both target and main with independent CAS, preserves competing writers, and leaves branch/index/worktree unchanged. Shadow publication rechecks target/shadow refs after durable artifact publication and restores artifacts on drift.
  - Patch authority is exact-set, not subset: missing, extra, symlink, or non-regular patch entries fail closed in validator, exporter, and durability review.
  - Desktop-enabled rollback planning requires an atomically prepared complete backup. A permanent regression now injects failure after the old backup is renamed and proves byte-exact restoration with no staging residue.
  - Final split-environment matrix: `109 passed, 8 subtests` (non-updater profile); updater runtime venv `Ran 12 tests — OK`; exporter `8 passed, 6 subtests`; combined `129 tests + 14 subtests`. `py_compile`, `bash -n`, direct whitespace scan, and `git diff --check` PASS.
  - Independent review first returned `NOT APPROVED` with six concrete findings; all six were reproduced and closed. A final test-only MINOR was fixed, and the same reviewer returned `Verdict: ACCEPT`.
  - Safety boundary: no canonical updater, live ref publication, runtime promotion, `/Applications/Hermes.app` install, Desktop/gateway restart, branch operation, commit, or push occurred. Live refs remained `local/hermes-patched@9b7257929`, `local/hermes-patch-stack@9b7257929`, `main@e598cef874`.

- Sealed runtime promotion closeout:
  - The pre-restart closeout commit and its origin tracking branch were both `9e46563ea0bb6a0e3cdc48c29499004f95ff7835`; live and shadow refs were both `a49a0972b7056d948ee7d3848b4d8c9791b1d114`, with runtime tree `508cf3ddc37b425fd20dd4faf9fd89965ccabfa4`.
  - Sealed transaction `bef65a7fb60de2c742b192ce` used manifest SHA-256 `2c9ec556be7bc7e0fa582c16f9683e410e333b7361905de00ec10ae6e0dca2bf` and live override SHA-256 `de8285a21c1cac66a126484610365c1da5f5b1f96459620590fa996579ec9d7b`. Verified-candidate promotion reported `applied=0, skipped=0`; the later frozen install/package gate reported `applied=43, skipped=4`, and its candidate tree equaled the live tree.
  - Shadow publication contained 19 patches: exact upstream replacement drop for 020, append 150, and hash-bound adapted proofs for 030/040/060/090/110. Round-trip tree identity and bundle verification passed; independent final review returned `ACCEPT COMMIT AND INSTALL` with no P0/P1.
  - Regenerated portable archive: 1684 files, 7405088 bytes, SHA-256 `3736f03810e75c5515253da1427eb6fec7d5977f794519877e868e4b22a49d2a`; durability collector `50/50` and overall `PASS`. Archived `runtime-promotion-closed-loop.md` matched the 11269-byte live file exactly at SHA-256 `cb518e1dddc1f6c9ea9a6c0f3c62639897fb82117e4fdfb77f1aea7a3339dca6`; the archive filename audit found zero forbidden cache/environment/dependency directories and zero obvious credential-state filenames.
  - The old Desktop renderer crashed with unplanned `SIGTRAP` at `15:35:19`; Desktop relaunched at `15:38:43` on the already installed `a49a0972…` bundle and WebSocket reconnected. No rebuild occurred. The approved one-shot restart changed gateway PID `20198→40760`; old-process drain completed in 0.01s with `timed_out=False`, `active_at_start=0`, and `active_now=0`, while the new process started housekeeping and the embedded Kanban dispatcher.
  - Final live smoke: gateway CLI reported launchd-supervised PID `40760`; fresh runtime import PASS; Desktop backend `0.18.2` on port `50867` with matching config versions; install stamp `a49a0972…`, `dirty=false`; native dependencies 3/3 and codesign PASS; Hindsight recall and Basic Memory search returned records; no gateway ERROR/CRITICAL/Traceback and no new Hermes crash report after the Desktop relaunch. The KeepAlive=false one-shot ran exactly once, exited 0, and was removed without affecting the production gateway.

## 2026-07-18 — 9 PR DIRTY→MERGEABLE Rebase Campaign

| Field | Value |
|-------|-------|
| **Goal** | Resolve GitHub merge conflicts for all 9 DIRTY PRs against latest origin/main |
| **Base** | origin/main `d59b79fad` (runtime promotion base `ef9e0c98f5`, local/hermes-patched `26cc04b8ac`) |
| **PRs** | #62492, #62676, #62598, #64364, #62663, #63009, #61976, #63671, #57687 |
| **Result** | ✅ DIRTY=0, CONFLICTING=0; all 9 PRs now MERGEABLE (UNSTABLE/BLOCKED = CI pending or review required, not conflict-related) |
| **Method** | Per-PR isolated worktree → `git rebase origin/main` → semantic conflict resolution → focused tests → `git push yingliang <branch> --force-with-lease` |
| **OMP delegation** | #64364 (memory_manager.py 5-region semantic merge) — normal tier 600s, 16 tests passed |
| **Manual resolution** | 8 PRs resolved manually (1–6 conflict regions each) |
| **Test verification** | #62492: 52 pass · #62676: 28 pass · #62598: 380 pass · #64364: 16 pass · #62663: 663 pass · #63009: 131 pass · #61976: 36 pass · #63671: 378 pass · #57687: 7 pass |
| **Key conflicts** | #63671: 8 commits, 51 files, cross Python+TS full-stack (gateway-event.ts reconcileClientTurnState merge, run_agent.py forwarder, tui_gateway/server.py _session_info + turn_snapshot) · #61976: _resolve_command_cwd signature change (session_key→env+prev_owner) + test adaptation · #64364: dual shutdown/drain mechanism semantic merge |

## 2026-07-23 — Frozen Runtime Update and Restart Closeout

| Field | Value |
|-------|-------|
| **Run** | `runtime-update-f13-restart-closeout-20260723` |
| **Base / target** | `f13f845116941ac5616e8df3294f3379a3efeb20` → `local/hermes-patched@767f696e3c2faa6ca0d50cc849cb58af94e6bcac` |
| **Canonical result** | PASS; `45 applied`, `4 skipped`; `23` PR commits + `22` portable patches |
| **Regression repair** | Final candidate-only failures reduced from `10 failed / 74 passed` to `84 passed`; fixes cover compression-boundary persistence ordering, numeric hard-limit guards, and model-switch threshold authority |
| **Desktop install** | install stamp commit `767f696e3c2faa6ca0d50cc849cb58af94e6bcac`, branch `local/hermes-patched`, `dirty=false`; codesign PASS; native deps 3/3 PASS |
| **PR scan** | `33` authored open PRs; `0` `CONFLICTING` / `DIRTY` |
| **Activation** | Approved one-shot restart PASS: Desktop `1314→80101`, serve `1321→80127`, gateway `35035→80072`; both KeepAlive=false one-shots reached `runs=1`, `state=not running`, `last exit code=0`, then were booted out |
| **Live smoke** | Backend `HTTP 200`, version `0.19.0`, config `33/33`, overall `ok`; runtime import PASS; fresh WebSocket accepted; gateway housekeeping + singleton Kanban dispatcher started; Hindsight recall and Basic Memory search non-empty; codesign/native deps 3/3 PASS; crash delta `0`; production PIDs and backend survived one-shot cleanup |
| **Shadow publication** | PASS: `local/hermes-patch-stack@767f696e3c2faa6ca0d50cc849cb58af94e6bcac`; PR layer `dd1b38974191cbcb4b5d1f34aff4318ae2f4f6f0`; 22 ordered mboxes; tree `78afc3666cb882730fe6dc535894d4e541d0c1a7`; manifest SHA-256 `6e0f7aa574bc2ae5c5a05b5ff58b9951f2ad7c180e18685184e7183a03571437`; thin bundle 472645 bytes, SHA-256 `f76365df92b5754e005773b81637cf5fda089a785c05f2ca21c8be197fd8e2a3` |
| **Shadow verification** | Fresh isolated 22-patch `git am --3way` reproduced the exact target tree; strict validator PASS; bundle prerequisite exactly PR layer; focused artifact/exporter contracts `72 passed, 77 subtests`; independent normal-tier OMP review `VERDICT: ACCEPT` |
| **Portable archive** | Deterministic double export PASS: 1872 files, 8296747 bytes, SHA-256 `9b2c4525eba786e9d948816a3aa9e95f53318adab5a8d6fa772e5d34a6d1bc2c`; live collector patch-stack contract `OK (24 manifest-derived)`, archive parity `57/57`, `overall: PASS` |
| **Collector fixture repair** | `test_memory_update_durability.py` now seeds missing patch fixtures from `MEMORY_DURABILITY_PATCHES` and asserts the removed final member dynamically; the focused file returned `14 passed, 2 subtests` before the combined contract run |
| **Post-closeout drift** | Collector fetched `origin/main@e5b83633e`; the frozen runtime is now 105 upstream commits behind. This is the next update cycle, not part of this sealed `f13f8451` publication |
| **Decision** | Accept canonical update, installed artifact, activation, shadow publication, portable archive, and live durability collector; the frozen cycle is fully closed |

## 2026-07-23 — Frozen Runtime Update `dbf4f69b` Closeout

| Field | Value |
|-------|-------|
| **Run** | `runtime-update-dbf4-patch-replay-20260723` |
| **Base / target** | `dbf4f69b728e0cf3c8e43822fea420117e109abc` → `local/hermes-patched@b5e2302a81c5e6fb167aa59d2733a352bb768212`; tree `b3c4ff365c45899a38c991c78ee19b9d715c4b3c` |
| **Patch refresh** | Refreshed `030`, `060`, `062`, `070`, `080`, `090`, and `170` against canonical predecessors; patch `160` retired because upstream `8c745314b` carries the same integration. Final stack: `21` ordered portable patches |
| **Focused evidence** | Patch `090`: `1058 passed` Python, Desktop `12 files / 131 tests`, TUI `3 files / 112 tests`, both typechecks PASS; final cross-patch repair: `4 passed`; each refreshed mbox passed a fresh second-worktree `git am --3way` tree-identity gate |
| **Canonical result** | Python baseline verifier + Desktop verifier PASS; promoted `38 applied`, `4 skipped`. The first post-verification switch exposed a checked-out-main ref/index synchronization bug; bounded recovery synchronized `main@dbf4f69b`, then promoted the same verified tree |
| **Hindsight recovery** | Clean-env verification exposed a previously contaminated dedicated server env. The exact fingerprint env was patched using explicit package roots, `uv pip check` passed for both venvs (`98` core packages, `212` server packages), server marker was canonically resealed, and both Hindsight verifiers PASS after Desktop packaging |
| **Desktop install** | `rebuild-desktop-and-install.sh` exit `0`; install stamp commit `b5e2302a81c5e6fb167aa59d2733a352bb768212`, branch `local/hermes-patched`, `dirty=false`; codesign PASS; native deps `3/3` PASS |
| **Control-plane hardening** | Added checked-out-main synchronization before target switch and stripped `PYTHONPATH` / `PYTHONHOME` / `VIRTUAL_ENV` from all dedicated-server subprocesses; focused control-plane gate: `56 passed, 31 subtests` plus `py_compile` |
| **Final PR audit** | `49` authored PRs total: `32` open, `0` merged, `17` closed-unmerged. Open PRs: `17` CLEAN, `2` MERGEABLE/UNSTABLE, `13` CONFLICTING/DIRTY (`#69393`, `#65388`, `#63671`, `#63298`, `#63292`, `#63009`, `#62598`, `#62552`, `#61518`, `#60662`, `#58632`, `#56034`, `#55707`) |
| **Durability boundary** | Runtime/live patchset is current, but shadow/archive publication was not part of this cycle: `local/hermes-patch-stack` remains at the prior `767f696e3c2faa6ca0d50cc849cb58af94e6bcac` |
| **Activation boundary** | No Desktop or gateway restart. Desktop remained stopped; gateway PID `11032` and serve PID `11186` remained the pre-update processes and therefore still execute the old code until an explicitly approved restart |
| **Decision** | Accept patch refresh, verified runtime promotion, Hindsight recovery, Desktop install, and PR audit. Restart remains a separate approval gate; PR conflict remediation was audited but not executed |

## 2026-07-23 — PR Rebase + Frozen Update `8fc27820` Final Closeout

| Field | Value |
|-------|-------|
| **Run** | `runtime-update-8fc-patch-refresh-20260723` |
| **Base / target** | `8fc278207b0f5b25e567966f9615e1b1737f62af` → `local/hermes-patched@c98e5a39b2ea10271a6a158443e27c4117225ddb`; tree `63618f1bd0e33c70d7a4de7e59ad57ffe5002faa` |
| **PR campaign** | Initial `12` DIRTY PRs rebased in isolated worktrees, independently focused-tested, and pushed with `--force-with-lease`. After `origin/main` advanced from `7168845d`, the full 31-open-PR GraphQL probe's conflict-specific result was `DIRTY=0`; separately reported merge-state counts included `CLEAN=16` and `UNSTABLE=1` (other non-conflict states are not enumerated here). |
| **Patch refresh** | Refreshed `070-queued-prompt-boundaries-integration.patch` and `090-turn-origin-integration.patch` against their exact canonical predecessors. Recorded focused gates: `070` Python `189 + 452`, Desktop `81`; `090` Python `617`, Desktop `110`, Ink `109`; both Desktop/Ink typechecks and two fresh `git am --3way` tree-identity proofs passed. |
| **Canonical promotion** | Frozen manifest base `8fc27820`; candidate verifier passed before CAS. `local/hermes-patched` promoted to `c98e5a39` with `21 applied / 0 skipped`. The sealed updater then correctly refused its non-offline Desktop installer path, leaving a recoverable runtime-installed-artifact split rather than silently claiming full completion. |
| **Desktop install / post-install** | Explicitly approved no-kill installer returned `0`; install stamp `commit=c98e5a39`, `branch=local/hermes-patched`, `dirty=false`; codesign PASS; native deps `3/3`; runtime import PASS; production and dedicated-server Hindsight `uv pip check` + verifiers PASS (`98` / `212` packages). Install log: 10276 bytes, SHA-256 `a8e50ba8e1220f12071c8f3f8bc62fb76509834b1ca211ff69fa20b1075f8c5c`. |
| **Shadow publication** | `local/hermes-patch-stack@c98e5a39`; PR layer `8cd5c88f835ea3db0aebcd9d15cd5293a4c043b1`; 21 ordered mboxes; manifest SHA-256 `09472b53f02f83ac9e8e22c36d0a8627faf7a84a550e0e2a5431f9b40c685e01`; thin bundle 452783 bytes, SHA-256 `291802d544dc3acb2309a53452a7b051eb573524e46b8a48e6bb027527c745c2`; sole prerequisite is the PR layer. Guarded manifest-last CAS changed shadow `767f696e…→c98e5a39…`. |
| **Shadow verification** | Strict artifact validator PASS (`21/21` hashes); two independently rerun fresh-clone `git am --3way` round trips both reproduced tree `63618f1b`; bundle verification PASS; target and shadow refs equal; runtime worktree remained clean and unchanged. |
| **Portable archive** | Python 3.11 exporter focused suite `12/12` PASS. Deterministic double export was byte-identical: 1882 files, 8331597 bytes, SHA-256 `3837e24ffe806fd7aaf2900efa7b3634ae949c0a0393ac84c5dc17a870569a88`; filename audit found zero credential/state/cache paths and all required members. |
| **Durability collector** | Exit `0`: `patch-stack artifact contract: OK (23 manifest-derived)`, archive parity `56/56`, Hindsight policy/main/server checks PASS, native deps PASS, `overall: PASS`. Its canonical-state subsection remained stale at the prior `dbf4f69b→b5e2302a` cycle and must not be used as current-ref evidence; current refs are verified independently above. The local-embedded Hindsight daemon was not running at collection time, reported as a non-blocking warning. |
| **Workflow audit** | Verified two control-plane gaps: frozen verifier paths must resolve from the frozen script directory (fixed and previously gated), and sealed-installer eligibility must be checked before promotion to avoid a runtime/App partial state. Recommended next hardening remains: draft non-promotable checkpoint before dependency prefetch, PR-head provenance in patch fingerprints, and ordered-mbox apply before server/npm/uv prefetch. |
| **Independent closeout review** | Read-only normal-tier OMP review reran the compact gates and returned `VERDICT: ACCEPT`, with `P0=0` and `P1=0`. Its three P2 notes were: stale collector canonical-state narration, incomplete PR-state denominator wording, and Hindsight daemon starting after collection; the first two are explicitly clarified in this ledger, and daemon lineage remains a post-restart smoke item. |
| **Approved one-shot restart** | Gateway one-shot changed PID `1704→69842`; its LaunchAgent reached `state=not running`, `runs=1`, `last exit code=0`. The old gateway drained cleanly in `0.01s` with `timed_out=False`, `active_at_start=0`, `active_now=0`; the production service restarted with housekeeping and the singleton Kanban dispatcher lock. Its production `last exit code=1` is the expected signal-restart contract. Desktop one-shot changed PID `1280→69971` with `runs=1`, `last exit code=0`; a later clean Desktop relaunch established the final stable PID `70603` with backend child `70609`. No new crash report accompanied either launch. |
| **Live backend / WebSocket** | Dynamic orchestrator port `49895`: `/api/status` returned HTTP `200`, `overall=ok`, version `0.19.0`, config `33/33`, correct orchestrator `hermes_home`, and healthy dashboard/gateway/storage components. `gui.log` recorded the Desktop cron scheduler and `ws accepted` after the final launch, with zero new ERROR/CRITICAL/Traceback lines. Gateway PID `69842` remained launchd-supervised; fresh runtime import PASS; its new log window had zero ERROR/CRITICAL/Traceback lines. |
| **Live memory / artifact smoke** | Hindsight semantic recall returned non-empty relevant records; Basic Memory returned the structured `Hermes memory architecture and recall isolation` note; the replacement Hindsight daemon PID `70764` listened on `127.0.0.1:9177`. Installed stamp/source HEAD remained `c98e5a39`, source worktree clean, codesign PASS, native deps `3/3`, and zero new `Hermes*.ips` crash reports since the restart marker. Daily review jobs remained enabled/scheduled and the Desktop cron scheduler was live. |
| **One-shot cleanup** | Both temporary one-shot LaunchAgents booted out successfully; all temporary scripts/plists/status/log artifacts were removed. Production Desktop `70603`, gateway `69842`, backend `70609`, and HTTP `200` remained healthy after cleanup. |
| **Decision** | Final live acceptance: PASS. Accept runtime promotion, Desktop activation, gateway activation, shadow publication, portable archive, memory paths, cleanup, and the complete `8fc27820→c98e5a39` closeout. |

## 2026-07-23 — Authored-PR Merge/Subsumption Retirement Audit

| Field | Value |
|-------|-------|
| **Run** | `hermes-pr-retirement-20260723` |
| **Live upstream / portfolio** | Fetched `origin/main` and `upstream/main` at `91546b8337068891cc0a6b834d89d0d9270fb3ec`; tips matched. Full authored portfolio: `49` PRs = `31 OPEN + 18 CLOSED + 0 directly MERGED`. The frozen deployed runtime remained `local/hermes-patched@c98e5a39`, clean and unchanged. |
| **Merged through successor/salvage PRs** | Verified effective upstream delivery for authored PRs #54612→#55545, #54785→#65419, #55707→#69884, #56084→#57335, #62486→#65896, #62571→#63624, #62657/#62676/#63597→#65919, #63710→#63968, and #68817→#69546. Each successor is `MERGED`, and its merge commit is an ancestor of current upstream; explicit maintainer comments/timeline evidence preserve the relevant authorship or semantic supersession. |
| **Static tracking retirement** | Removed #54785, #55707, and external tracked #56746 from `TRACKED_PRS`. #54785 has exact patch equivalence: all three source commits are `git cherry -` against current upstream and range-map to merged #65419. #55707 is explicitly superseded by merged #69884, which preserves the reviewed credential-warning/keyless-provider policy. #56746's lifecycle portion is upstream via #68817/#69546; local patch 055 retains only the context-breakdown scaling residual. |
| **Remaining authority** | Static list is now exactly `32` rows = all `31` authored OPEN PRs plus one local-only `#0`; live GitHub parity PASS. Every remaining open tracked PR has at least one unique `git cherry +` commit; mixed-sign #53890, #60662, #61976, #63298, and #64364 retain non-empty residual work and remain tracked. The local-only profile-scoped cron fix remains because its duplicate PRs are still open or closed-unmerged. |
| **Immutable-boundary decision** | This was tracking metadata only. Frozen registry records, override guards 055/090, patch bytes, patch-stack manifest/bundle, target/shadow refs, portable closeout archive, installed Desktop, and running services were not mutated. Historical registry/override provenance must be pruned only in a future coordinated patch regeneration against a newer frozen base. |
| **Independent review** | Route-aware OMP read-only audit returned `VERDICT: ACCEPT`, `P0=0`; it independently confirmed the three-row retirement set and found no additional currently tracked PR safe to retire. |
| **Verification** | Python 3.11 AST/parity gate PASS (`32/31/1`), `py_compile` PASS, focused registry/override tests `2/2` PASS. A frozen-ref isolated control-plane dry-run loaded all historical guards and ended `PASS: dry-run plan complete; no repository or registry writes performed`. A live-ref dry-run instead correctly exposed unrelated post-freeze head drift for #65388 (`ca6330ec…→ca94d413…`), which belongs to the next regeneration, not this retirement. |
| **Decision** | Accept static retirement of #54785, #55707, and #56746. Continue tracking all 31 open authored PRs and local `#0`; defer immutable registry/override/patch cleanup to the next sealed update window. |

## 2026-07-24 — Authored-PR Conflict Rescan + #53890 Retirement

| Field | Value |
|-------|-------|
| **Run** | `hermes-pr-rescan-20260724` |
| **Upstream advance** | `origin/main` advanced `91546b833 → 0b17d4d71` (+257 non-merge commits). Notable upstream arrivals: compression todo-snapshot merge (salvaged from our #53890 as `d2bb6cc25`, authored by Yingliang Zhang, closed by teknium1), context-engine quiet compaction status, compression abort/blocked-overflow dedup, credential-pool locking boundary, kanban delegated-child isolation, Windows hidden-console daemons, and update self-heal. |
| **#53890 retirement** | PR #53890 was CLOSED by teknium1 on 2026-07-23T14:25:47Z with commit `d2bb6cc25181`; `git cherry -v` confirmed all 3 PR commits are patch-equivalent (`-` for all). teknium1 cross-referenced successor PR #69860 and closed #53890. Removed from `TRACKED_PRS`; override 110 provenance retained. Static list is now `31` rows = `30` open GitHub PRs + local `#0`; live GitHub parity PASS. |
| **DIRTY/CONFLICTING (7)** | `#56034` (+2 unique, compression safety valve), `#60662` (+4, configurable tail floor), `#62598` (+4, system marker role persistence), `#63292` (+3, interrupted completion metadata), `#63298` (+12, queued prompt boundaries), `#63671` (+10, turn origin), `#64373` (+2, compression rotation lock). All 7 retain non-empty unique commits; none are fully subsumed. Rebase needed. |
| **UNSTABLE (2)** | `#62663` and `#64364` are `OPEN`, `MERGEABLE`, CI `UNSTABLE` — mergeable but CI checks failing/pending. Not conflict-dirty; CI rerun or fix needed. |
| **CLEAN/BLOCKED (21)** | #53936, #55534, #57687, #58632, #58924, #59661, #59942, #60648, #61518, #61976, #62492, #62552, #62587, #62621, #63009 (BLOCKED), #63519, #64421, #64499, #64745, #65388, #69393. |
| **Verification** | Python 3.11 AST/parity gate PASS (`31/30/1`), `py_compile` PASS. Runtime worktree remained clean at `c98e5a39`; no refs or immutable artifacts mutated. All audit refs cleaned up. |
| **Decision** | Accept #53890 retirement. 7 DIRTY PRs require rebase in a future campaign; 2 UNSTABLE PRs need CI attention. No additional PRs are safe to retire at this time. |

## 2026-07-24 — OMP Hard-Tier Dual-Concurrency Read-Only Trial

| Field | Value |
|-------|-------|
| **Run** | `omp-hard-tier-dual-readonly-20260724` |
| **Frozen base** | `fix/slash-worker-lifecycle-comprehensive@1965022a2cdfffcb09ea2135543ab2bed1940f62`; two detached clean worktrees at that exact commit |
| **Isolation** | Separate worktrees, prompt files, output files, and `OMP_SESSION_ROOT` / `--session-dir` roots for arms A and B. Both prompts prohibited writes, tests, rebase, push, and cross-worktree access. |
| **Dispatch** | Both arms used the route-aware wrapper with `custom:sudo / gpt-5.6-terra`, `--task-tier hard`, 900-second internal deadline, and explicit `--allow-parallel-hard`. |
| **Concurrency evidence** | A JSONL: `08:18:37.067Z–08:20:52.813Z` (135.746 s); B JSONL: `08:18:39.947Z–08:20:30.646Z` (110.699 s); verified overlap: 110.699 s. Both wrapper processes exited `0`; one JSONL exists under each distinct session root. |
| **Write-safety evidence** | Both isolated worktrees remained clean; `git diff --check` passed in each. The original checkout retained its pre-existing dirty state only. |
| **Artifacts** | A output SHA-256 `368bb924e2630f6e837cc1e8d200c22f6604ea7dd09728f797944a34c700e2c3`; B output SHA-256 `e5f4990874e36c877fd3e1643a22f25ed3e2cb8b3116b1127083fb8a6691e468`. |
| **Result** | Provider and wrapper completed two isolated read-only hard sessions concurrently. Audit A found `--allow-parallel-hard` is an unbounded bypass, not a capacity-2 mechanism; audit B found portable-export reproducibility gaps. |
| **Decision** | Do **not** raise the default hard-tier concurrency yet. A bounded slot admission mechanism and stricter export/isolation validation are required before enabling routine dual concurrency. |

## 2026-07-24 — OMP Hard-Slot Admission Implementation

| Field | Value |
|-------|-------|
| **Run** | `omp-hard-slot-admission-20260724` |
| **Contract** | Default hard capacity `1`; explicit `--hard-max-concurrent 2` (bounded through `3`); deprecated bypass rejected; same Git common-directory writes serialized; independent repositories may use write slots concurrently. |
| **Evidence** | Runtime + skill wrappers are byte-identical/executable; independently rerun focused wrapper gate: `49 tests` PASS in `72.361s`; shell syntax PASS. Portable profile export PASS: `1897` files, SHA-256 `c8ffb0d71a8fc316c82c6b11e7164f62620398dff66654defe28875a62bafdd8`. |
| **Review/fixes** | Independent review found and fixes closed: deprecated `=...` bypass, empty session root, active-path containment, nested Git without git binary, wrapper/OMP pending-owner handoff, and root-path containment. |
| **Decision** | accept bounded admission implementation; use default capacity `1` unless every concurrent hard dispatch explicitly requests the same capacity and satisfies isolation checks. |

## 2026-07-30 — Frozen Build-Only Updater Hardening + K3 Desktop Activation Closure

| Field | Value |
|-------|-------|
| **Run** | `k3-desktop-activation-20260730` |
| **Control repo** | `feat/hybrid-routing-portable` HEAD `0d5e7cc6158b38029ac50bcbb4f49c13602fcce4` = remote; worktree clean. |
| **Runtime authority** | `local/hermes-patched@1606b492ba9257b6f33a6a1fb936ad2752a31d1f` (tree `2bc2da33f028a55f528be3341cf000710078e7c7`); shadow `local/hermes-patch-stack@e2ddfe50a5edabedf942dc61735f79bd8d4fcf88` (tree `1df1fbb1f3d99dc1cb9a5baba7c7e952dec0ca1f`). Runtime checkout clean. Divergence is the accepted Hybrid retirement result; do not restore to `fb950702` or re-run normal promotion. |
| **Build-only hardening** | Four live profile files updated: `update_hermes_patched.py` (+179/-41), `rebuild_hermes_patched.py` (+94/-9), `test_update_hermes_patched.py` (+387), `test_hermes_patched.py` (+214/-5). Diff SHA-256 `e45cb52fc2b67a4b55e291f792a43aa9b4f157ada5a77e6b6a4ecf95b2bd4db9` (1117 lines). `--build-only` now only valid with `--frozen-base`; moving/manifest fail closed. Build-only skips reuse probe; `--no-discover` without explicit `--gh-bin` omits bare `gh`; coupled build-only is read/build/verify-only (no promotion intent, no target movement, no verified_candidate replacement). Strict single `CANDIDATE_SHA=<40hex>` parse; tree mismatch/target movement/missing/duplicate/malformed all fail closed. Changed Python path enumeration uses `--output=` + NUL-separation (no OUTPUT_LIMIT truncation) with size/traversal/absolute-path guards. |
| **Fixture fix** | `_seed_coupled_transaction()` in `test_hermes_patched.py` now creates `refs/heads/<config.target>` via `git update-ref` before seeding the transaction, fixing the RED where `test_coupled_build_only_does_not_trip_promotion_mode_rejection` could not `rev-parse` a non-existent target ref. 3 lines added; production code untouched. |
| **Tests** | `py_compile` ×4 PASS. Focused 11-test matrix `Ran 11 tests in 297.612s — OK` (with Node 22 in PATH). Full `test_hermes_patched.py` `Ran 63 tests in 29.370s — OK`. Exporter focused suite `40/40` PASS in 4.202s. |
| **Sol review** | Fresh Sol direct review of real diff + test output: P0=0 P1=0 P2=0 P3=0. Explicit ACCEPT. |
| **Portable archive** | Canonical double export byte-equal: 2438 members, 9,902,458 bytes, SHA-256 `8bdd4971b9edcb77ca31cdd0b5483322ffe6972eeea9133720a19c7b4f800d0c`. All 4 script/test members match live SHA-256. Zero capsule members, zero sealed `.preparations` runtime `.py`, zero `.env`/credentials files. 25-patch stack manifest coverage unchanged. |
| **Installed Desktop** | Stamp: commit `1606b492ba9257b6f33a6a1fb936ad2752a31d1f`, branch `local/hermes-patched`, dirty `false`, builtAt `2026-07-29T13:44:12.625Z`. Codesign PASS. Native deps 3/3 PASS. Crash baseline 0. |
| **Gateway** | `ai.hermes.gateway-orchestrator` UNLOADED; 0 `gateway run` process. Maintained throughout. |
| **K3 config** | `auxiliary.vision`: primary `custom:sudo-kimi-k3` / `t9s/kimi-k3` / `chat_completions` / timeout 120; fallback `custom:sudo` / `kimi-k2.7-code` / `chat_completions` / timeout 120. Local Qwen not in auto fallback chain. |
| **Decision** | Accept frozen build-only hardening, portable archive, and canonical export. Desktop restart and post-restart smoke follow below. |
| **Desktop restart** | One-shot LaunchAgent `ai.hermes.desktop-oneshot-restart-20260730T112300` (RunAtLoad=true, KeepAlive=false) bootstrapped at `2026-07-30T03:16:06Z`. Old Desktop PID `16352` quit in `1s`; old serve PID `16372` terminated. New Desktop PID `21612` (start `2026-07-30 11:16:09`), new orchestrator serve PID `21620` (start `2026-07-30 11:16:10`). Gateway remained UNLOADED throughout. LaunchAgent `state=not running`, `runs=1`. Bootout and temp file cleanup PASS. Production Desktop/serve remained alive after cleanup. |
| **Post-restart smoke** | Orchestrator serve port `53391` (dynamic `--port 0`): `/api/status` HTTP `200`, version `0.19.0`, config `33/33`, `gateway_running=false` (expected). Codesign PASS. Native deps 3/3 PASS. Runtime checkout clean. 0 new crash reports since restart. Hindsight recall returned 27 relevant records. Basic Memory MCP search returned 10 structured results. K3 Vision: 8×8 pure red PNG correctly identified as "Red" via `custom:sudo-kimi-k3 / t9s/kimi-k3 / chat_completions`. Config confirmed: primary `custom:sudo-kimi-k3`, model `t9s/kimi-k3`, api_mode `chat_completions`, timeout `120`. K3 named-provider tool loop: `--yolo -z` mode with explicit `custom:sudo-kimi-k3 / t9s/kimi-k3`, read_file tool used, exact marker `K3_TOOL_LOOP_MARKER=20260730T112300` returned. gui.log post-restart: fresh `ws accepted` at `11:16:12`, 0 ERROR/CRITICAL/Traceback. Gateway remained UNLOADED, 0 `gateway run` process. |

## 2026-07-30 — Full Closed-Loop Upstream Rebase + Patch Stack Rebuild on fa8b959b

| Field | Value |
|-------|-------|
| **Run** | `hermes-nohybrid-upstream-20260730T050428Z-f1da9da0` |
| **Old base** | `d71033a4077a6dfdcdb42c9e9eeab4c41e4a7012` |
| **New base (frozen)** | `fa8b959b92c8450e7d66bb659327d932c1a21b79` — "Merge pull request #74630 from NousResearch/bb/update-restart-race" |
| **Candidate** | `local/hermes-patched@0a3a2bdd32c2a103c0a54d21fba735eb9f1cf3cf` (tree `3d2e6df8c8043ff9af27d8c3c8ec6c6085eb4da9`) |
| **Old target** | `7fb10f90f6451b83066f6bcd8943342a6ecf08b4` |
| **Method** | Manual rebuild: cherry-pick 12 direct PRs (23 candidate commits, 11 applied + 12 patch-equivalent skipped) + apply 26 patches (4 non-empty applied, 22 subsumed-by-upstream) in isolated worktree; `git commit-tree` on frozen base. |
| **Direct PRs** | 12: #53936, #54785, #59661, #59942, #60648, #60662, #61518, #61976, #62492, #62587, #62621, #63519 |
| **Rebased PRs** | 21 total rebased + pushed on new base (5 new + 16 re-confirmed + #63298 manual + #63519 manual) |
| **Patches** | 26 total: 4 non-empty (020, 026, 055, 110, 225), 22 subsumed by upstream. Retired: 190, 200, 205, 210, 215, 220, 240. Retained: 225, 230, 250. |
| **Verification** | py_compile 6/6 PASS; focused pytest 876/876 PASS (1 mock-drift skipped: `test_cron_no_agent` workdir kwarg); Desktop typecheck 3/3 PASS. |
| **Desktop build** | `npm run build` + `electron-builder` PASS. `/Applications/Hermes.app` 307M, app.asar SHA-256 `e617bf8bbb526a28ddc534281ac64dc7195ab51258ce8504628c72dc23f69814`, build stamp `0a3a2bdd32`. Codesign with Apple Development identity. |
| **Gateway restart** | Old PID 39567 → New PID 1251 (16:31 CST). Clean startup, no new errors. |
| **Desktop restart** | Old PID 39654 → New PID 1408 (16:32 CST). All helpers + serve backends running. |
| **Live smoke** | Gateway RUNNING on new code. Desktop RUNNING on new build. Hindsight PID 1079 healthy. Serve backends PID 1422 (orchestrator) + 1424 (default) running. 0 new errors post-restart. |
| **Builder modifications** | `rebuild_hermes_patched.py`: added `--empty=drop` to `git am`, cherry-pick `-X theirs --allow-empty` for patch-equivalent commits, `subsumed-by-upstream` fallback for reverse-apply check. `omp_with_timeout.sh`: added `glm-5.2` route mapping. |
| **Workflow** | `coupled-v1` throughout. No Hybrid. |
| **Decision** | Accept full closed-loop upstream rebase + patch stack rebuild on `fa8b959b`. Runtime live. |

## 2026-08-03 — Tri-Model MoA 默认 dev route 固化落地

| Field | Value |
|-------|-------|
| **Run/IDs** | canary `k3`/`glm`/`explicit-k3` (RC=0, `CANARY_OK`, JSONL `sudo/t9s/kimi-k3`); fail-probe `custom:sudo / t9s/deepseek-v4-flash` → exit 2 (unsupported route) |
| **Decision** | Orchestrator (`t9s/deepseek-v4-flash`) ≠ coding model. All OMP from a ds4 session uses explicit `--model`; complex tasks = K3 + GLM-5.2 + ds4 blind-parallel propose → Hermes DESIGN LOCK → single K3 `--thinking max` → 3-way review ≥2/3 ACCEPT. Flat trigger list (S1-S4 + W1-W3), ANY hit → 3-model; none → K3 + Hermes self-review. User confirmed 2026-08-03: no notch, token cost not a concern. |
| **Effort ceiling** | K3 / GLM-5.2 → `max`. ds4 (backend `deepseek-v4-flash-dspark`) API probe: accepts `low|high|xhigh|max`, no error, no observable difference (reasoning_tokens ~9-10 across levels) → `max` is the semantic ceiling. |
| **Artifacts** | source of truth `coding-agent-delegation/references/tri-model-moa-workflow.md` (absorbed former `omp-route-model-topology.md`); MEMORY `[Route]` entry; USER.md tri-model entry (replaced dual-model) + hybrid-terminology line updated; Basic Memory `agent-memory/hermes/tri-model-mo-a-default-dev-route-runbook`. |
| **Review evidence** | three-model review (K3 + GLM-5.2 via OMP + ds4 Hermes) independently converged on explicit-`--model` over patching the wrapper whitelist (2:1). runbook §4 re-verified against sudo endpoint (effort claim) 2026-08-03. |
| **Memory sizes** | MEMORY 7,913/8,000 (98%); USER 4,399/4,500 (97%). Both within limits. |
| **Parallel-session note** | a concurrent Hermes session (PID ~61xxx, 14:57) authored `omp-route-model-topology.md` + a MEMORY entry on the same decision; reconciled by merging into one owner file + one MEMORY entry (user confirmed cleanup 2026-08-03). |
| **Decision** | Accept: tri-model MoA is the default dev route for complex work; single K3 + Hermes self-review for mechanical fixes. |

## 2026-08-26 — 5 PR DIRTY→MERGEABLE Rebase Campaign

| Field | Value |
|-------|-------|
| **Run** | `hermes-pr-rebase-20260826` |
| **Trigger** | Live `gh pr view` scan (mergeable async recompute after per-PR touch) against origin/main `1fe0f2f3ac97`: 25 authored open PRs, 5 DIRTY — `#59942` + `#60648` newly conflicted since the 07-24 rescan; `#63292`/`#63298`/`#63671` known-standing. 3 formerly-DIRTY self-resolved (#56034, #60662, #64373); 9 closed since (`#58632`, `#61518`, `#61976`, `#62552`, `#62598`, `#62621`, `#63519`, `#69393`, `#64364`); 4 new authored all CLEAN (`#78100`, `#78104`, `#78113`, `#80832`). |
| **Method** | Per-PR isolated worktree under `~/.hermes/worktrees/orchestrator/rebase-20260826/prNNNNN` (runtime checkout untouched) → `git rebase origin/main` → semantic conflict resolution → focused tests (runtime `venv/bin/python` for openai-dependent suites; `PATH=~/.hermes/node/bin` Node 22 for tsc/vitest) → `git push fork HEAD:<branch> --force-with-lease`. |
| **#59942** (4 commits) | `hermes_cli/main.py` additive-region conflict: upstream `_macos_codesigning_identity_valid`/`_desktop_macos_setup_tcc_identity` + PR `_app_asar_hash`/`_macos_adhoc_sign_bundle`/`_install_rebuilt_desktop_app` — keep-both union. Test `test_desktop_install_after_update.py` 9/9 PASS. Pushed `8859e5850f`. |
| **#60648** (2 commits) | `test_hindsight_provider.py` add-add test blocks — keep-both union. PR tests 3/3 PASS; 1 failure is the local-env missing `pytest-asyncio` (fails identically on plain origin/main). Pushed `0976bc8244`. |
| **#63292** (4 commits) | `hermes_state.py` schema column union (`interrupted_tool_tail` × `_compressed_summary`): signature keep-both, both INSERT column lists merged to 23 cols, VALUES placeholders 22→23 (both sides independently appended one `?` → identical-edit auto-merge silently dropped one). Python values tuples verified in exact column order. PR tests 9/9 + `test_compressed_summary_metadata.py` 9/9 PASS. Pushed `12172d5d39`. |
| **#63671** (7 commits) | Semantic merge in `use-session-actions/index.ts` (PR `GatewayEventPayload` snapshot/`reconcileClientTurnState` × upstream clarify-projection rewrite, union body + `turnState.needsInput` fallback); silent-merge artifacts found by test/typecheck sweep: `chat-runtime.ts` duplicated `turnStartedAt`, composer-submit test missing `})` closers, `utils.test.ts` dropped `beforeEach` seeding, harness missing new-upstream `onSteer`/`runtimeIdByStoredSessionIdRef` (debugged via instrumented run: unbound `getRuntimeIdForStoredSession: () => null` diverted queue drain to `session.resume` instead of `prompt.submit`). vitest 227/227; python turn-origin/protocol/turn-finalizer suites PASS. Pushed `59163c06dd`. |
| **#63298** (15 commits, hard) | `agent_runtime_helpers.py`: PR deletes repair-side user merging (queued-prompt boundaries) while upstream added summary-carrier/`drop_stale_api_content` — resolved per PR philosophy (wire-copy merge in `drop_thinking_only_and_merge_users` is the single merge point) + PR early-return insertion. `run_agent.py` passthrough union (`_compressed_summary` + `platform_message_id`). Test-file unions for upstream's new adjacent-merge test with expectations re-based on the PR's `[Next user message]` marker. Python focused 505/507 (2 `test_hermes_state.py` failures reproduce identically on plain origin/main — pre-existing env, not merge damage); vitest `use-prompt-actions/index.test.tsx` 127/127 + composer-submit 26/26; tsc clean. Pushed `d7f6ffc047`. |
| **Final state** | All 5 `MERGEABLE`: `#59942` CLEAN, `#60648` CLEAN, `#63671` CLEAN; `#63292`/`#63298` BLOCKED (review required, not conflict-related). DIRTY=0 across the authored portfolio. |
| **Lesson** | Rebases against a fast-moving main produce **silent auto-merge damage** that no conflict marker flags: identical-edit placeholder drops, duplicated declarations, eaten `beforeEach`/`})` closers. Typecheck + full related test sweep on the final tree (not just at conflict points) caught every instance. |

## 2026-08-26 — Local Patch Obsolescence Tri-Model Audit + Retirement

| Field | Value |
|-------|-------|
| **Run** | `hermes-patch-necessity-audit-20260826` |
| **Scope** | Full local patch authority vs upstream `1fe0f2f3ac97`: pick-plan 25 carried PRs + manifest 11 stack patches + hermes-patches-new 4 pending = 40 items |
| **Method** | Orchestrator mechanical evidence bundle (PR states, `git cherry` patch-equivalence, upstream symbol checks, close-comment retrieval) → 3 blind read-only OMP legs (k3 proc_c34725360cd8, glm proc_62abc7bcd1e3, dsf proc_b21e71a47d51, each 1200s, isolated session dirs) → consolidator ≥2/3 with direct-evidence adjudication of 3 pending-patch splits. Output: `/tmp/patch-audit-20260826/{evidence.md,runs/*.txt}` |
| **Unanimous RETIRE (3/3)** | **#76503** (landed via #90193, 8 production files) · **#61518** (landed via #87237, `turn_started_at` + `resumedTurnStartedAt`) · **#61976** (superset via #69396, `_SESSION_CWD` + per-job workdir) · **#64373** (superseded by upstream #76354 — durable lock + already-rotated refusal + `_adopt_live_compression_child` recovery; PR residual log/docstring-only) |
| **Pending verdicts** | **225 RETIRE (2/3)** — `ActiveSessionStoredIdRotation` has no `kind` field, no consumer (consolidator direct-verified). **110 RETIRE (2/3: drop patch)** — production `todo_snapshot` machinery upstream (context_compressor.py + salvage tests); test-only. User directed full drop, no test salvage. **055 KEEP pending (2/3 value exists)** — real `_scale_categories_to_total` residual (+62 lines `agent/context_breakdown.py`); GLM's "byte-identical with 000-lite" claim proven false (000-lite touches only composer/index.tsx + hermes_state.py). **026 KEEP pending (2/3)** — tracks open #57687. |
| **Reversal caught by audit** | **#58632 NOT superseded (3/3)** — recorded author close comment ("upstream deleted setCronJobs/updateCronJobs") is FALSE: both symbols present (cron.ts:73,80), remount spinner still live (cron/index.tsx:875 `useState(null)`). Carry keeps its value. |
| **Scope corrections (KEEP, notes attached)** | **#63009**: codex gpt-5.6 autoraise landed upstream (agent_init:2110/2652); local residual = custom `codex_responses` providers only (our Sudo routing case). **#65388** bundle title wrong — actually "report live fallback runtime" (`_freeze_model_options_request`), still unrecognized upstream. **Stack 400**: all 3 legs independently confirmed manual path for GLM/DSF (`_supports_adaptive_thinking` non-Claude/Kimi → False; `THINKING_BUDGET.get('max',8000)` fallback LIVE) — 400 stays P0. |
| **WATCH (registered)** | **#57687 + pending 026** — K3 cites upstream #85578 (grace-reap→interrupt→60s force-reap) as near-subsumption; 2/3 KEEP now. **Re-verify before next promotion cycle.** GLM's own evidence pointed residual-only daemon-thread join work. |
| **User-approved mutations applied** | pick-plan 25→21 (removed #76503/#61518/#61976/#64373) · patch **350 ghost-reply-final-delivery RETIRED** → `.superseded` + manifest `patch_count` 11→10, `retired_patches`+1 with ROT reason (K3: upstream redesigned `final_content_delivered`/`delivered_final_matches`; matcher-None polarity inversion risk) · pending **225/110 renamed `.superseded`** in place. No builder run, no promotion (separate approval gate). |
| **Live runtime impact** | None — current `local/hermes-patched@552b8f434f` unchanged; retirements take effect at the next regeneration. Pending carry candidates: 026 (+watch), 055 (scale residual). |

## 2026-08-27 — GLM-5.3-Flash Adopted as Global Main Model

| Field | Value |
|-------|-------|
| **Run** | `glm53-flash-main-adoption-20260827` |
| **Router probes (live)** | `POST /anthropic/v1/messages` text → exact `GLM53_AVAILABLE` (20 in / 6 out, model echo `t9s/glm-5.3-flash`); vision via 8×8 solid-red PNG image block → model perceived "solid red" in interleaved thinking (`signature: reasoning_content`) — text + vision both confirmed. `POST /v1/responses` → `GLM53_RESP_OK` (model echo `glm-5.3-flash`); `POST /v1/chat/completions` → `GLM53_CHAT_OK` with `reasoning_content` in message (echo-family signature on that transport). Claimed 1M window configured, not boundary-probed. |
| **Orchestrator profile** | `model.default` `t9s/kimi-k3` → `t9s/glm-5.3-flash`; `reasoning_echo` removed from main block (k3-only trait); fallbacks reordered k3→glm-5.2→sol→dsf; provider models + `t9s/glm-5.3-flash {context_length: 1000000, supports_vision: true}`; API mode `anthropic_messages` on `/anthropic`. |
| **Default profile** | Same default/fallback/metadata change; inherits provider-level `codex_responses` on `/v1` like glm-5.2 (k3/dsf keep `chat_completions` overrides). |
| **Compression policy** | Inherits global `threshold: 0.35` → resolver-verified trigger **350,000 tokens at 1M** in both profiles ("context window 1M, 主要用350K"). No per-model `model_thresholds` key needed (k3=0.9→315K, dsf=0.6, gpt-5.6=0.85 unchanged). |
| **OMP chain** | `~/.omp/agent/models.yml` + `t9s/glm-5.3-flash` entry (`contextWindow: 1000000`, `input: [text, image]`, reasoning, maxTokens 65536); wrapper whitelist + route-follow cases added (`sudo/t9s/glm-5.3-flash`, `custom[t9s/]:glm-5.3-flash` → `is_glm=true`). Both dispatch smokes returned exact `GLM53_OMP_OK` (explicit `--model` 120s MAX + Hermes-route `--task-tier mechanical`). |
| **Authority verification** | `hermes config check` both profiles → `Config version: 39 ✓`; `resolve_model_threshold('t9s/glm-5.3-flash')` → 0.35 both profiles; wrapper `bash -n` OK; profile repo wrapper copy synced (`51120→51438` bytes) + `sync.py push`. |
| **Backups** | `config.yaml.bak-glm53-20260827-100746` (both profiles), `models.yml.bak-glm53-20260827-100752`. Hindsight retain recorded the switch narrative (MEMORY.md tool-call failed at write time — memory entry to be finalized next config touch). |
| **Untouched by design** | Vision route stays `t9s/kimi-k3` primary / `kimi-k2.7-code` fallback (no request to change); glm-5.2 remains registered as fallback; no 1M boundary probe run. |
| **Decision** | Accept `t9s/glm-5.3-flash` as global main for both Hermes profiles + OMP leg availability; k3 demoted to first fallback with its `reasoning_echo` intact. First real sessions/dispatches should watch heading drift between `t9s/` prefix echo (anthropic) and bare `glm-5.3-flash` echo (responses API). |

## 2026-08-27 — GLM-5.3-Flash Canonical-ID Unification (Addendum)

| Field | Value |
|-------|-------|
| **Trigger** | User asked whether to unify on bare `glm-5.3-flash` (matching echo). |
| **Probe (live)** | Bare `glm-5.3-flash` accepted on **/anthropic + /v1/responses + /v1/chat/completions**, and each echo returns exactly `glm-5.3-flash` — **the echo follows the request id**, so `t9s/` is only a spell-variant alias, not a tier routing token. |
| **Mutation** | Both profiles' `model.default` and provider-model metadata key `t9s/glm-5.3-flash` → **`glm-5.3-flash`** (canonical everywhere, echo-matching). Fallback chain untouched (k3 keeps `t9s/` canonical). OMP `models.yml` id → bare (mirrors existing bare `glm-5.2` precedent). |
| **Wrapper** | Explicit whitelist: `sudo/t9s/glm-5.3-flash` accepted → **rewritten to canonical `sudo/glm-5.3-flash` inside EXTRA_ARGS** (fixed: earlier attempt only rewrote the triage log value, alias dispatch failed `Model not found` and exposed the gap); Hermes-route case maps both spellings → canonical. Normalize loop is a no-op for non-alias routes — verified with a k3 control smoke. |
| **Verify matrix** | `hermes config check` → `Config version: 39 ✓` both profiles; resolver `glm-5.3-flash` → 0.35 → 350,000 both profiles; OMP smokes: explicit bare `GLM53_OMP_OK`, explicit t9s alias `GLM53_OMP_OK` (dispatch log confirms canonical rewrite), Hermes-route `GLM53_OMP_OK`, k3 control `K3_OK`. |
| **Backups** | `config.yaml.bak-glm53bare-20260827-102451` (orchestrator), `...-102510` (default profile + models.yml). |

## 2026-08-27 — k3/glm-5.2 Canonical-ID Unification + Router Health Findings (Addendum 2)

| Field | Value |
|-------|-------|
| **Trigger** | User asked whether k3/dsf/gpt-5.6 have the same drift class; fix together if yes. |
| **Probe matrix (live)** | 5 models × 2 spellings × 3 transports. **Bare canonical** (echo matches request everywhere): glm-5.3-flash, kimi-k3, glm-5.2. **t9s REQUIRED**: gpt-5.6-sol/terra (bare request silently redirects to glm-5.3-flash), t9s/deepseek-v4-flash (bare → 502 `no provider available`). sol/terra echo on chat normalizes the prefix to bare but serves correct model — identity intact, no action. |
| **Functional findings (not cosmetic)** | ① dSF broken on /v1 chat (HTTP 500 and earlier silent swap echo=glm-5.3-flash) AND /v1 responses (502) — real health confirmed on /anthropic only (`DSF_LIVE`, thinking+text, 0.8s). Default-profile dSF fallback ran `chat_completions` = live-broken. ② t9s/kimi-k2.7-code on chat + responses silently ALIASED to kimi-k3 (echo=kimi-k3) — the routing backend for k2.7 on those transports is gone; aux vision "fallback chain" is effectively a k3 proxy, not an independent model. |
| **Mutations** | k3 + glm-5.2 → bare canonical in both profiles (fallback lists, ~14 aux references each, provider registry keys; vision primary t9s/kimi-k3→kimi-k3). Default profile: NEW provider `custom:sudo-anthropic` (anthropic_messages /anthropic base) holding t9s/deepseek-v4-flash; fallback dSF repointed there; dSF removed from the codex/chat sudo provider. sol/terra untouched with the documented rule: never register them bare (redirect hazard). OMP kept t9s ids for k3/dsf — OMP speaks /anthropic only (echo exact-matching) and renaming would churn all tri-model skill docs for zero functional gain. |
| **Verification** | `hermes config check` v39 ✓ both profiles ×2; resolver: kimi-k3=0.9→315,000, glm-5.2/glm-5.3-flash=0.35→350,000, dsf=0.6, both profiles; dsf fallback/provider structure dump-verified; registry bilaterals (bare ids exact-match request ids). Template `config.template.yaml` keys k3/glm-5.2→bare (k3 keeps ctx=350000+vision); OMP models.yml/wrapper unchanged. |
| **Left documented (no config change)** | k2.7 vision fallback aliasing (still delivers k3 output on chat — works as transport-hiccup proxy; router-side removal, out of our control); Hindsight env model t9s/glm-5.2 (works on chat, echo-normalized); sol/terra bare-redirect rule. |
| **Backups** | `config.yaml.bak-mtc-20260827-103412` (orchestrator), `...-103429` (default). |


## 2026-08-27 — GLM-5.3-Flash Lane Router Flap 11:05→~11:13 CST (Transient, Recovered)

| Field | Detail |
|-------|--------|
| **Trigger** | User switched this chat's model to glm-5.3-flash @11:05 CST; first prompt (msgs=334, ~220,528 tokens) → UI error `layer: endpoint, code: timeout, retryable: true, Connection error.` |
| **Failure signature** | agent.log session 20260826_175529: 6 streaming attempts (2 retry rounds) 11:05:16→11:05:40, all `Streaming failed before delivery: anthropic.APIConnectionError: Connection error.`; error surfaced to UI at 11:05:57. |
| **Size-gate hypothesis tested & FALSIFIED** | 800K-char /anthropic probe at ~11:12 → `SSL_ERROR_SYSCALL` in 0.13s (TLS killed early); then bisect 200K…800K chars ALL 200 (1.7–3.6s) and 5× repeat 800K + chat-lane 800K = 7/7 green at ~11:13. No deterministic payload limit; behavior flipped within ~1 minute. |
| **Lane-isolation control** | SAME 800K-char body to kimi-k3 /anthropic mid-flap → 200 OK, usage input_tokens=177,908 counted by router. Only the glm-5.3-flash lane was affected. |
| **Conclusion** | Router-side transient (TLS termination / upstream restart) on the glm-5.3-flash lane, ~11:05–11:13 CST window. NOT our config (K3 green inside window; model echo/routing unchanged), NOT size limit (falsified deterministically). Fallback chain engaged as designed (kimi-k3 took over the session). |
| **Action** | None locally. Timeline kept for a Sudo-infra report: fail window 11:05:16–11:05:57, stray TLS kill ~11:12, recovery confirmed 11:13. Watch: if this pattern repeats, consider per-model retry patience bump (current policy retires the turn after ~24s of retryable connection errors). |

## 2026-08-27 — Hermes Promotion: 1894-Commit Upstream Jump (Manual Path, Swap Pending)

| Field | Detail |
|-------|--------|
| **Trigger** | `git fetch` showed upstream/main at `791e2ae325` — 1894 commits ahead of `local/hermes-patched` (`552b8f434f`); checkout had drifted to `main` (gateway running unpatched upstream). |
| **Control-plane repair** | Active patch dir found EMPTY (8/14 archive never restored) → restored 10 patches from `retired/20260814-1258/` (md5-verified). Sync regression `42dcc32` (had reverted `90ae5cb`'s 350-retirement) re-fixed: manifest patch_count=10, 350 in retired_patches w/ ROT reason, pending 110/225 → `.superseded`. |
| **000 rebase** | `000-merged-integration-stack.patch` re-based onto `791e2ae325`: 12 rejects adjudicated → **10 DROP** (verified upstream≡552b, e.g. directive-text, composer-dock `composerFloatingPill`, worktree-dialog epoch remnants, false P5/P6 guards, i18n key deletions vs retained table), **1 PORT** (P4 `hermes_state.py` messages dedup: partial UNIQUE index + repair + ON CONFLICT idempotent no-op on both INSERT sites — upstream absorbed index+repair only), 3 leaked mutex files purged, `hermes_state_common.py` version hunk no-op'd (upstream already at 26). Result: pre_promotion_diagnostic **10/10 APPLY / 0 CONFLICT**; tsc exit 0; desktop vitest 7947 pass / 1 fail (managed-ssh-update — fails identically on pristine upstream). |
| **Sealed updater blocked (Pitfall 20)** | `hermes_promotion_bootstrap.sh` rc=125: capsule command whitelist lacks `uv`; capsule is file-hash sealed (no safe manual edit) → **manual merge fallback** per skill. |
| **Manual merge candidate** | `/tmp/promo-candidate`: `git merge upstream/main` (4 conflicts resolved: agent_init `tail_mode`+`protect_delivered_count` both-kept; compressor signatures same; conversation_loop ×2 verify-loop sites = upstream `force_display=True` per 552b; false-stop block kept with `append_message`). **False-stop family restored** (`agent/false_stop.py` 193L + test 372L + `false_stop_detection:"auto"` default + `_false_stop_synthetic` ephemeral flag — all from 552b, absent upstream). py_compile clean, zero markers. |
| **Gate results** | false_stop 39/39 (after cache-suspend test fix); context_compressor 146/146; session-api+tui+rotation **41+650 pass, 2 fail = gateway-injected `HERMES_*` env (42 vars) poisoning env-parsing tests — clean-env re-run 2/2 PASS, upstream baseline same-fails**; whitewhitespace/conflict-marker/compileall gates clean; candidate desktop tree bit-identical to 000probe (tsc+vitest inheritance valid). Order-dependent OpenRouter-probe flakes (test-composition-only, pristine upstream reproduces) documented as upstream test-hygiene gap. |
| **Survival audit (skill-mandated)** | 10/10 patch probes verified in merged tree; **370 `_emit_tool_call_status` found silently dropped** → restored via clean patch apply, commit `abe3f67eea`. Final branch: `local/hermes-patched` = `abe3f67eea` (merge `ec23cc36eb` + hardening `275c3c443f` + 370 restore). Pre-promo tip preserved: `local/hermes-patched-pre-promo-20260827` = `552b8f434f`. |
| **Pending (user-gated)** | ① Runtime swap: user runs in separate terminal `cd ~/.hermes/hermes-agent && git reset --hard local/hermes-patched` (gateway blocks checkout/reset from live session). ② Desktop rebuild from patched tree (`rebuild-desktop-and-install.sh`) — must follow swap (desktop patches live in 000). ③ Clear `.update_check` caches; ④ Gateway restart (kills session, needs approval). ⑤ Profile-repo `sync.py push` (live dir already correct; push is now safe). |
| **Findings filed** | Sealed capsule whitelist lacks `uv` (control-plane gap, blocks sealed promotion path); builder has no PR cherry-pick layer (`TRACKED_PRS=[]`) — 552b's PR-direct commits (e.g. #63298 features) carried only if they're inside 000/300-400; upstream 1894-commit absorption of local PRs not individually audited. |
| **Swap + Desktop (done post-ledger)** | User ran `git reset --hard local/hermes-patched` (landed on main by mistake → normalized: HEAD→local/hermes-patched via symbolic-ref, main restored to `7a1aafb4e1`); runtime tree verified 4/4 markers (false_stop.py, 370 ×2, dedup ON CONFLICT, config default). Desktop rebuilt from patched tree via launchd-hosted `rebuild-desktop-and-install.sh` (harness kills >420s builds — launchd wrapper `/tmp/desktop-rebuild-wrapper.sh` per manual-desktop-installer-recipe): **FINAL_EXIT:0**, codesign deep-strict rc=0, deployed electron-main.mjs matches source, install-stamp = `abe3f67eea`/local/hermes-patched/dirty=false, `.update_check` caches cleared, rollback backup at `/Applications/Hermes.app.backup`. Only remaining: gateway + desktop app restart (user approval). |


## 2026-08-27 — Promotion 遗留两项收尾（#1 capsule rc=125 修复 + #2 PR 直 commit 审计）

| Field | Value |
|---|---|
| **Hypothesis** | 遗留观察 #1（sealed capsule 缺 `uv` → rc=125）与 #2（builder 无 cherry-pick 层，552b 的 99 个 PR 直 commit 吸收面未审计）当轮可修/可判 |
| **Method** | #1: 溯源 rc=125 出处（`INVOCATION_CONTRACT_RC` in update_hermes_patched.py）→ 重现链（bootstrap `env -i PATH=/usr/bin:/bin` → gate `_minimal_environment` 子 PATH 无 homebrew → `uv --version` by-name 探针 "command failed to start"）→ 最小修复（gate `_discover_tool_bins`：/opt/homebrew/bin、/usr/local/bin、~/.local/bin 有界发现，注入 3 个 `_minimal_environment` 调用点的子 PATH）。#2: 21 家族语义标识符探针 × (HEAD vs 552b) + 逐家上游等效性核实 |
| **Result #1** | 修复四层验证全绿：py_compile ✓；`_minimal_environment` 实测 PATH 含 `/opt/homebrew/bin` 且 `uv`/`gh` 解析 ✓；`bootstrap prepare --help` 清洁 env 全链 rc=0 ✓；`update --_hermes-bootstrap-authority-probe` rc=0 + 完整 handoff 文档 ✓；updater 原样三探针 uv 0.12.6/gh 2.98.0/git 2.54.0 rc=0 ✓。stage1 键集/argv 哈希/capsule 封印零改动 |
| **Result #2** | 99 非 merge 直 commit → 21 家族：12 家族标识符在 HEAD 在场 ✓（reasoning_echo/retain_async/untrusted-framing/tail-floor/MiniMax/turn_started_at/ancestors/dedup-index/end_reason/config-migrate/_SESSION_CWD/hygiene-limit）；9 疑点逐个精判：S=上游 `restore_plugin_prompt_sections` 原生吸收；F=上游 `close_interrupted_tool_sequence`（8 调用点+回归测试）等效；U=截断内联保留（`_do_recall:1885-1887`，sync/async 双路径）；E=上游 in-flight 去重+合并（#84417）+desktop `fromQueue` 27 hits；J=上游 `_rollback_desktop_from_backup` 完整回滚；I=上游 spawn-per-request 无长命 worker；M=`_model_picker_context` 上游版本可导入；B/V=`hindsight-all`+`hindsight_embed.daemon_embed_manager` 机制替换（活进程实证）→ **零丢失，无需移植** |
| **Conclusion** | 下次 promotion 可直接走 sealed 路径（uv/gh 现在可达）；552b PR 直 commit 无一丢失——旧栈语义要么被上游原生吸收（更强形态）要么被机制替换（功能在活）。builder 不需要 cherry-pick 层（PR 内容已由上游吸收覆盖） |
| **Artifacts** | gate 修复 = hermes_canonical_entrypoint.py（含 `_discover_tool_bins` 注释锚点）；审计过程 = 本节 + 终端留痕 |
| **Follow-ups** | 下次 promotion 跑一次完整 sealed 链确认端到端；`~/.local/bin` 中 uv 若装错版本会被优先选中（当前不存在，已入注释） |

## 2026-08-28 — 上游 PR 例行体检（25 open → 24，1 关闭）

| Field | Value |
|---|---|
| **Trigger** | 用户：检查新 comments / merge conflicts / 被其他 PR 吸收者 → close 不再 track |
| **Method** | GraphQL mergeable + head SHA → fork fetch → `git cherry` patch-id 全量 → 高嫌疑 8 家族语义级 grep（上游 grep 判定 + merge-tree 冲突规模） |
| **Conflicts (6)** | #63298 (2f: server.py + gateway 测试), #62663 (1f: cron/scheduler), #62492 (1f: update_cmd), #60662 (2f: compressor+config_defaults), #56034 (1f: compressor) — 均 7 月中-8 月龄，无需抢修；#57687 CONFLICTING 但已吸收→关闭 |
| **Comments** | 无 8/18 后新维护者评论；#62492 与 jrleal10 的 3 条往返 8/18 已全部回应完毕 |
| **Merged-by-others** | patch-id 0/25 精确吸收；语义级 1/25：**#57687 slash-worker 关闭** → 上游 `_finalize_session` 幂等 chokepoint + WS disconnect 经 `_teardown_session` 同路 + on-demand spawn 串行化（含 #38095 leak 注释）→ **已 close**（附证据链说明） |
| **Close candidates cleared** | #80832 上游无 turn-end 终结戳（keep）；#60648 竞争 PR #22836 仍 open（keep）；#62587 竞争 #61403/#62247 仍 open（keep）；#78100 force_display 上游无（keep）；#78104 trajectory-ephemeral 上游无（keep）；#65388 faulthandler watchdog 上游无（keep）；#59942 desktop install rollback 上游无（keep） |
| **Ledger** | 本条 |

## 2026-08-28（下午）— 6 DIRTY PR 全量 rebase + 2 项吸收关闭

| Field | Value |
|---|---|
| **Trigger** | 用户批准：上游 239 commits/2 天，修 conflicts PR |
| **关闭（2）** | #62492（fleet-wide config migration 被 upstream #91277 Phase 2 完整吸收——`_migrate_sibling_profile_configs` + 调用点 + 更强实现）；#57687（前轮已关） |
| **Rebase 完成（5）** | #63292（+2 修复：TS2451 双声明删除、`_CONVERSATION_ROW_COLUMNS` 补回 `_compressed_summary` 列——8/18 旧血统 9 败真因）、#62663（取上游 bounded init，测试 patch 目标迁到 `hermes_state.SessionDB`；修正被原样提交的冲突标记）、#63298（取上游 `a7977771a6` 重实现，#77129 概念已吸收；**1 项设计分歧留言维护者**：replay 层 user;user 边界保持 vs 上游合并语义，测试自 8/26 起在 PR 树红）、#60662（上游 lean-tail 重写后 floor 常量仍非参数——PR 可配置化仍活，签名合并 lean+floor）、#56034（hygiene 阀未吸收，`force` 抗抖动旁路保留） |
| **验证** | #62663 推后 MERGEABLE **CLEAN**（CI 全绿）；其余 4 个推后 MERGEABLE（CI 运行中）；#63292: tsc 0 + thread vitest 170/170 + hermes_state 200/200 + 5 套件 58/58；#62663: tests/cron 1054/1054；#60662: compressor 149/149 + tail 家族 45/45；#56034: compressor 151/151 + preflight 77/77 |
| **方法教训** | rebase 静默列集合并（`_CONVERSATION_ROW_COLUMNS`）= Pitfall 39 的列版；git rerere 的 recorded preimage 会把坏解复用——amend 前必须 marker-scan；推分支前必须 `gh pr view --json headRefName` 核对真名（差点推错 3 个相似名） |
| **Ledger** | 本条 |

### 2026-08-28 CI 复核（推送后首轮）
- #60662 / #56034：CI 全绿 ✅（3/7 全绿）
- #62663：MERGEABLE CLEAN（前轮已确认）
- #63292：Python ✅；TS 红 = `apps/desktop :: check:test:ui` **单测超时**（"Test timed out in 15000ms"，非编译/断言错——TS2451 已愈）；需重跑定性（CI 负载超时 vs 真超时）
- #63298：JS&TS ✅；Python 红 = **正是那个设计分歧测试**（`test_prompt_submit_rebind_map...` 20777 AssertionError）——CI 只挂此 1 个，与留言给维护者的分歧完全吻合，等待裁决

### 2026-08-28 #63292 UI 超时定性（完成）
- 超时测试 = `gateway-settings.test.tsx > loads the machine-level connection config`（618 文件过、仅此 1 个 15s 超时；CI 机 transform 325s vs 本地 38s = CI 负载慢信号）
- 本地复跑：隔离 7.55s 过；负载态（+chat 全目录 101 文件）840/840 过 → **CI 负载 flake，非 PR 引入**
- CI job 无 admin 无法重跑 → 空 commit `61e4783a60` 重触发 → **CI 全绿（23 pass / 12 skip / 0 fail）**
- #63292 终态 MERGEABLE；全部 5 个 rebase PR 至少一轮全绿

### 2026-08-29 #63671 处置（Quad 盲审 + 烟测闭环）
- **四腿盲审**: K3=CLOSE_PR / DSF=CLOSE_PR / GLM53=NEEDS_FIX / SOL=NEEDS_FIX — 唯一事实分歧（GLM53 称 head 非 stale）经一手核实: head 59163c06dd 确为 8/26 rebase、base 1fe0f2f3ac 已含 poller 机制, 但 GLM53 的"试合并只冲突 1 文件"不准 — merge-tree 实测 2 文件冲突（composer/index.tsx + chat/index.tsx, upstream bot-mode 大改区）
- **裁决 LOCK-A+B（用户批准）**: 烟测背书关闭 + 留门条款
- **烟测证据链（全 PASS）**: test_notification_poller_requeues_when_busy（busy requeue 不吞 turn）/ test_internal_event_never_interrupts_busy_session（internal 事件永不 interrupt busy turn, 仅 idle 后以新 turn 级联）/ TestDisplayMetadataPersistence（display_kind 元数据全写路径 round-trip 27/27）
- **上游吸收链**: 前提接缝（old server.py:8817/9422 direct turn start）已被 poller+owner-gate+durable registry 重设计消解; 逐组件吸收映射见 close comment
- **残余价值留门**: (a) desktop turn-origin 元数据（上游 0 概念）(b) ack-at-turn-start 窗口的 mid-turn crash 恢复语义 — 均写进 close comment 作为未来 scoped PR 候选
- **关闭验证**: state=CLOSED; close comment 含完整证据链 + 1759 行测试场景清单的验收价值说明
- tracked PR 数: 23 → 22 open

### 2026-08-30 Promotion 658-commit 轮（sealed 链修复 + 3 层 bug 排障）
- **Base**: upstream `d5fd2e9366`（runtime 从 abe3f67e 落后 658 → 0）
- **链路**: pre-flight 10/10 APPLY → sealed `candidate_rebuild` 762s 完成 → `dependency_sync` ✓ → `local/hermes-patched` promoted 至 `f4dedbab` → hindsight 4 步手动恢复 → 门禁绿
- **排障 3 层**（sealed 全链第一次真跑暴露）:
  1. `gh pr list` JSON 头部丢失 → 根因 = builder `OUTPUT_LIMIT=4000` < 22-PR JSON 4.1KB，bounded buffer 保尾丢头 → discover_open_prs 改 `output_limit=256KB`（rebuild_hermes_patched.py 1091）+ 失败转储探针
  2. registry 5 个 stale closed PR（#57687/#62492/#63671/#74855/#76503）被当 active cherry-pick → 符号审计 5/5 存活后摘除（Pitfall 10）
  3. cherry-pick 级联冲突 → 切 `--merge-stack` 3-way（旧 candidate abe3f67e 为 ref）→ 仅 1 冲突 `agent/context_breakdown.py`（upstream anchored_used vs 我们 scale-to-total）→ 合并两侧语义
- **Hindsight 恢复链**: venv 被 dependency_sync 重建（丢 embed 包）→ 装 hindsight-embed/client 0.8.4；`tools/lazy_deps.py` memory.hindsight 从 0.6.1 对齐到 0.8.4 对（verifier SPECIFIER >=0.8.4 + server lock 0.8.4）；`embedded_runtime.py` adapter 从 552b 恢复（8/27 手动 merge 静默丢，Pitfall 39 再犯）→ verify 全 PASS（client=0.8.4, embed=0.8.4, adapter=lightweight）→ server runtime `39a620d3` 发布
- **门禁**: py_compile 全变更面 ✓ · 冲突标记 0 · 10/10 patch 符号存活 · focused tests 337/338（1 fail = upstream-native `test_failed_read_only_open_does_not_leak_tracked_connection`，纯 upstream 树同挂）· Desktop tsc 0 err
- **遗留**: desktop rebuild（launchd）+ runtime swap 验证 + gateway 重启（须用户）待做；state.json last_good=f4dedbab

### 2026-08-31 Promotion 51-commit 轮（Quad MoA triage + 零冲突 merge + 本地 ctor 缺陷修复）
- **Base → tip**: upstream `26350357d7` → `ff3835a630`（51 commits, 全部 2026-08-30 落地; 主题: compression/compaction 加固 ~26、skills 瘦身 5、gateway capability 5、approval 安全 2、bot-mode 2、photon 2、chore/fmt 8）
- **Quad-model MoA triage**（四腿盲封: k3/glm-5.3/dsf/gpt-5.6-sol, 1100s, 同一 brief, /tmp/triage_20260830/）:
  - 收敛: 单次 merge 为正确载体（3/3 有效腿实证; sol 独判"须分 5 组序列化"全部为无证据推测，被 k3/glm/dsf 的 merged-tree 测试+符号 grep 实证否定 — 3/4 多数 + 实证优先）
  - **glm 腿发现（已一手核实为真）**: 上游 `tui_gateway/server.py` `_apply_live_compression_config` flat-200K fallback 与 `a2af8405d1` None auto-derive 不一致 — 纯上游内部缺陷，仅影响 codex_responses 路由活会话 config save，不阻塞 → 上游 issue 候选
  - **k3 腿发现（已一手核实为真）**: 本地 `agent_init.py:2167` 读 `compression.protect_delivered_count` 但 `:2784` ctor 未传参 → 恒默认 1 ≠ 文档 3（#78100 patch 内缺陷，非合并引入）→ 本轮修复
- **Merge**: `ead1f14660` — merge-tree+worktree dry-run 零冲突（8 个重叠文件全 auto-merge）· 冲突标记 0/83 code files · whitespace 仅文档
- **本地修复**: `311093e326` wire `protect_delivered_count=compression_protect_delivered` into ContextCompressor ctor（1 行，py_compile ✓）
- **门禁**: py_compile 13 热文件 ✓ · Desktop tsc 0 err · patch 符号 11/11 存活（false_stop import/`_emit_tool_call_status`/`_scale_categories_to_total`/hindsight-all==0.8.4/`idx_messages_active_dedupe` 等）
- **测试**（canonical `scripts/run_tests.sh`, venv 环境陷阱: 裸 conda python 跑出 42 假失败 `No module named 'openai'`）:
  - 本地 patch 回归 7 文件 133/133 ✓
  - 上游新契约 6 文件 136/136 ✓（attempt lifecycle/ownership、native compaction 74、lean aux、skill commands）
  - gateway/approval 4 文件 117/118 — 1 fail = `test_approval` macOS realpath('/tmp')→'/private/tmp' 豁免失效（纯上游树同挂, Linux CI 不触发; 上游 issue 候选 #2）
  - 全量 tests/agent+run_agent: 15 fails 全部 pre-merge HEAD 复现（venv 缺 anthropic 包等环境因素; vision_routing 2/2 parity 单文件直跑证实）→ **零合并回归**
- **Desktop rebuild**: launchd 路径; authority 算法修正（须用安装器内嵌 `backup_artifact_snapshot` 结构化哈希, 手写内容哈希会被拒 — backup 实际未变, hash `226ab57130c796af` 与 8/27 原始 authority 逐位一致）; plist 因 harness 拦截改写 `~/Library/LaunchAgents/`; rebuild 进行中
- **遗留**: rebuild 完成后: install-stamp 验证 + gateway/desktop restart（须用户）+ `test_approval` macOS bug 与 flat-200K fallback 两项上游 issue 起草

### 2026-08-31 Promotion 收尾 + 两项上游 bug 直接提 PR（#99069 / #99070）
- **重启生效验证**: gateway（orchestrator+default 双 profile）与 Hermes.app 均 11:42 重启, stamp `311093e326`/`local/hermes-patched`/dirty=false, update 缓存已清 → 51-commit promotion 全链闭环
- **PR #99069** (`fix/approval-verify-cleanup-realpath`, commit `8a662700ec`): `_is_verification_artifact_cleanup` macOS realpath 前缀 bug — 排障深度: 结构分析发现上游两测试 (A `nonrecursive_cleanup` mock `/tmp` vs B `symlinked_temp_dir`) 在 mac 上**互斥不可移植** (pytest tmp_path 自带 /private 前缀故 B 存活, A 必挂); 修复 = spelled_dir ∈ {canon, raw} + realpath 等价 + islink guard, test A 重锚 tmp_path; 118/118 PASS; 真机语义验证 gettempdir 拼写豁免/`/tmp` 别名仍拦
- **PR #99070** (`fix/gateway-codex-threshold-auto`, commit `a77652a798`): `_apply_live_compression_config` flat-200K 覆盖 auto 阈值 — 对齐 agent_init a2af8405d1 None 语义; 顺带修既有 UNSET 测试的旧 200_000 钉死断言; 新增 absent/invalid 两路覆盖; 20/20 PASS
- **流程纪律**: 两 PR 均从纯 `upstream/main` 独立 worktree 分支 (patch-stack 零污染), 各只含 1 commit, MERGEABLE; 隐私审计 diff+body 0 泄漏 (无 yingliangzhang/sudoai/t9s//Users//Mac Studio); 作者=公开 GitHub 身份 yingliang-zhang
- **worktrees 清理**: /tmp/pr-approval + /tmp/pr-gateway 已删; profile runs 下的历史 worktree 未动

### 2026-08-31（下午）PR 池健康检查 + 针对性 rebase 收官
- **真 open PR 池: 24 个**（早先 `gh pr list --author --state open` 返回 40 个是 search-index 陈旧数据; #74855/#76503/#68817 等已被上游吸收关闭, 正确做法 = 逐 PR REST ground truth）
- **状态分布**: MERGEABLE 21（含今日 #99069/#99070），BLOCKED 1（#63009 CI 排队 ≠ 冲突），CONFLICTING 1（#63298 parked 等维护者裁决，自 8/26 已定性）
- **执行动作（共 2 件）**:
  1. **#80832** rebase → `5845d60ae6`（单 commit），1925 tests 通过，MERGEABLE/CLEAN
  2. **#63009** rebase+abort+replay+squash 到 `9587574d20`（单干净 commit，从 5-commit 链整理），287/287 tests 全绿，BLOCKED→MERGEABLE；删掉 absorbed commit 0d31d93482（上游 July-flap 设计已经覆盖 272/372K 问题），保留 api_mode autoraise + 372K/272K gate
- **21 个其余 open PR**: 全部验证 MERGEABLE，zero rebase 需要（GitHub 只是 mergeability 计算慢）
- **教训记档**: rebase 的 target 必须每次都 `git fetch origin main` 之后立即使用（首个 #63009 rebase 误以为 origin/main=`d59b79fadd` 是 latest，实际 fetch 在 rebase 之后到底导致全量重新做；时序 = fetch→rebase→验证，不可拆）

### 2026-08-31（傍晚）#99070 判重关闭（重复 #98863）
- **发现**: alt-glitch AI triage 在 #99070 留言 "Duplicate of #98863"
- **核实**: #98863（nftpoetrist，8-30 21:20 UTC）比我们（8-31 03:54）早 **6.5 小时**；同一函数（`_apply_live_compression_config`）同一 None→auto 语义，diff 几乎逐行一致；对方 invalid-value 测试**参数化 5 种**（garbage/True/-5/1.5/0）比我们（1 种）覆盖更广——**无补充价值**
- **根因复盘**: 我们开 PR 前的 `gh search` 只搜了 config key（`codex_responses_compact_threshold`），对方标题用 "hot-apply ... automatic" 语义词，索引未命中。教训：**开 bug-fix PR 前的查重要同时搜 key 名 + 语义关键词 + 函数名**
- **动作**: #99070 体面关闭，留言指向 #98863（保留独立发现过程说明 + 感谢 triage）；#99069（approval macOS）不受影响仍 OPEN/MERGEABLE
- **净效果**: 今日上游贡献 = 1 个存续 PR（#99069）+ 1 个 bug 确证（重复关闭本身证明 MoA triage 发现的 bug 真实且值得修——被两人独立命中）


### 2026-09-04（下午）PR 池健康检查 + #102117 重构冲击应对（#99069 跨重构移植）
- **触发**: 例行 PR 池检查发现上游 09-04 合入 #102117 `refactor: whole-codebase simplification — −34% source LOC, every god file decomposed`（2655 文件，+436K/−784K 行），叠加 09-03 agent/desktop 修复批——**全 PR 池 rebase 基线一夜失效**
- **真 open PR 池: 22 个**（8-31 是 24；#99070 已判重关闭、#62663 被 teknium1 以 superseded 关闭——上游 #100903 retention sweep 已覆盖，关闭合理，无需异议）
- **merge-tree 全量冲突扫描（origin/main=d3630f8532，vs 8-31 基线）**:
  - **CLEAN (2)**: #53936、#55534 — 无需动作
  - **旁侧冲突（上游文件重组导致，PR 功能正交）**: #99069（approval.py 区域注释冲突，功能已随重构迁移 approval_detection.py）、#60648/#64499/#64745（hindsight `__init__.py` 各 1 处）、#58924（config_schema 1 处）、#62587（cron/scheduler 1 处）、#80832（hermes_state+run_agent 2 处）、#78100/#78113（conversation_loop+run_agent）、#59942（main+update_cmd 2 处）
  - **深冲突（文件被上游重写/函数被迁移）**: #63298（14 处，5 组件）、#63292（6 处）、#56034/#60662（agent_init+context_compressor+turn_context）、#59661（web_routers+hermes_state）、#63009（agent_init 等 3 处）、#64373（conversation_compression+hermes_state）、#65388（web_server+methods_complete+server）、#78104（agent_runtime_helpers）
- **#99069 立即修复（唯一 8-31 新鲜 PR，AI review LGTM）**:
  - 排障: 上游 #102117 把 `_is_verification_artifact_cleanup` 从 `tools/approval.py` 迁到 `tools/approval_detection.py` 并重写为 and-链形态，但 **macOS gettempdir 拼写 bug 在新版依然存在**（`operand == os.path.join(temp_dir, basename)` 严格拼写匹配未放宽）——修复仍适用
  - 移植: 独立 worktree `/tmp/pr-99069-rb`（fix/approval-verify-cleanup-realpath-rb，基于 origin/main），`approval.py` 取 ours（main 重组版），功能块移植到 `approval_detection.py:1122` 新位置，测试锚点改 tmp_path
  - 验证: pytest 116/118 PASS（2 deselect = main 基线同样失败的 `prompt_toolkit` 环境缺失，已对照证实与改动无关）；py_compile OK；隐私审计 0 泄漏；本地 merge-tree CLEAN；GitHub 报 MERGEABLE
  - 推送: `0040817a2f` force-with-lease 到 fork，PR 已回复 rebase 评论 https://github.com/NousResearch/hermes-agent/pull/99069#issuecomment-5541686833
  - **CI 终态: 24 pass / 13 skip / 0 fail，mergeStateStatus=CLEAN（BLOCKED→CLEAN，CONFLICTING→MERGEABLE 全链恢复）**
- **教训**: 上游大重构（−34% LOC）后，PR mergeable=CONFLICTING **不必然意味着功能过时**——需逐 PR 判定"功能块被迁移但 bug 仍在"（→移植）vs "上游已重写覆盖"（→关闭）。merge-tree 批量扫描 + 目标函数 grep 新位置 = 快速分诊法
- **分诊修正（第二轮逐 PR 抽查后）**: #102117 是 god-file 分解（`conversation_loop.py` 8070→1600 行），PR hunks 全面失锚——"机械 rebase"假设作废，约 18/22 个 PR 需按新结构移植。抽查实证: #80832 的 turn-finally terminal stamp 在新 main 不存在（`activity_tracking.py`/`session_activity.py` 拆分后仅 compression 有 `force_persist` 旁路）；#78113 的 false-stop nudge 在新 main 不存在（grep 证伪、`agent/false_stop.py` 为 PR 自建文件）。功能价值不减，但移植成本升一个量级（估 3-5 人日全池）
- **后续（未做，待用户定优先级）**: 其余 19 个 CONFLICTING/UNKNOWN PR 的移植/rebase 队列；其中深冲突 9 个（#63298/#63292/#56034/#60662/#59661/#63009/#64373/#65388/#78104）需逐个判断上游吸收状态（8-31 已做过一轮 "close candidates cleared" 分析，需按 #102117 新基线重做）

### 2026-09-04（晚）Phase-2 批量移植（8 PR）+ Quad-MoA 吸收审计 + 2 PR 有据关闭
- **Quad-MoA 吸收审计（7 深冲突 PR，3 有效腿 100% 收敛，SOL 死腿不计票）**: 输出存档 `/tmp/moa_absorb/out_{k3,glm,dsf}.md`（含每 PR 新落点 file:line）
  - **PORT**: #63292(L, 中断完成落 metadata——main 仍写可见 "Operation interrupted" 行且测试钉死 `test_turn_finalizer_interrupt_alternation.py:157`)、#56034(M, 硬阀仅 gateway 层有)、#60662(S, `_MAX_TAIL_MESSAGE_FLOOR=8` 硬编码)、#59661(S, REST 只读 tip 段)、#63009(M, autoraise 只认 `provider=='openai-codex'` + 切换不重推导)、#65388(PARTIAL: 心跳已吸收, faulthandler dump + model.options 快照 PORT)
  - **ABSORBED**: #64373——durable-lease 机制（`727392b5cb` + `_adopt_if_parent_rotated`）在 merge-base 前已在 main，PR 剩余仅文案 → **体面关闭**（3/3 腿证据留言）
- **移植完成 8 个**（全部 MERGEABLE，每个均: 独立 worktree 基于纯 origin/main + 功能块按新结构落位 + 本地测试全过 + 隐私审计 0 泄漏 + rebase 评论）:
  1. **#80832** terminal activity stamp: `_finalize_activity_after_turn`→`agent/activity_tracking.py`(mixin), `finalize_session_activity`+`_ACTIVITY_FINALIZE_PATIENCE_S`→`hermes_state_sessions.py`/`hermes_state.py`, 调用点→`agent/turn_facade.py` finally 块（8/8 测试过）
  2. **#78113** false-stop nudge: `agent/false_stop.py` 原样 + guard 落 `turn_final_response.py`（dropped-toolcall recovery 后）+ 预算重置落 `turn_tool_round.py` + `_false_stop_synthetic` 注册进 `session_persistence.py` EPHEMERAL 元组（37/37 测试过；测试 import 引用迁新家）
  3. **#78100** force_display: `_emit_interim_assistant_message`→`agent/stream_delivery.py` 加 force_display 参数（dedup/streamed 双旁路），调用点合并进 `_append_interim_answer` helper（`turn_stop_gates.py`，一处覆盖原两处）（25/25 相关测试过；移植中自纠一处布尔式写反）
  4. **#78104** trajectory 过滤: `_is_ephemeral_scaffolding` import 迁 `agent/session_persistence.py`，过滤逻辑原样（语义 assert 验证）
  5. **#60648** hindsight retain_async: 工具 handler 转发配置的 `retain_async`（`_retain_batch` 已有参数只是没传）+ 参数化测试（34 中 33 过, 1 失败=基线环境）
  6. **#62587** cron no_agent 会话: session 记录块移植进新分解的 `_run_no_agent_job`（create→record→close 全退出路径）+ 4 个 run-recording 测试合并进 main 已更新测试文件（22/22 过；Pyright 类型窄化自纠）
  7. **#59942** desktop install: 5 个 helper（`_app_asar_hash` 等 174 行）落新家 `main_desktop.py` + frozen updater surface re-export + `_rebuild_desktop_after_update` 成功尾巴装安装逻辑 + 测试 import/patch 目标全迁 main_desktop（17/17 + cmd_update 47/47 过）
  8. （#99069 见 phase-1）
- **有据关闭 2 个**:
  - **#64373**: ABSORBED（见上）
  - **#58924**: 上游设计取代——main 的 `get_config_schema()` 已含全套 local_embedded LLM 字段（带 `when` 条件渲染），且 desktop declared surface **有意**不暴露该模式（`test_hindsight_config_schema.py:41` 钉死 "intentionally unsupported on desktop"），移植会对抗上游设计决策 → 关闭
- **CI 故障排查（本批 PR 5/8 BLOCKED 根因）**: 上游 #102117 把 workflow 从 `ci.yaml` 改名 `ci.yml`，fork push 的 pull_request 事件打到旧 path 触发 0-job 幽灵 run 记 failure，**真 CI workflow（ci.yml，"All required checks pass" 的来源）在 4 个 PR head 上从未启动**（#99069 早期 push 幸运落在改名生效后窗口）。修法=每分支追加空 commit 重推触发新事件；#78100 重推时又踩分支名坑（PR head=`fix/force-display-interim-message` ≠ 命名直觉），二次修正
- **操作坑（记入待办）**: ① fork PR 的 head 分支名必须每次 `gh pr view --json headRefName` 实查，不可从 commit 主题猜（本日踩 3 次: 78113 `feat/` 前缀、78100 全名不同、78104 恰好直觉一致）；② `git diff origin/main PR` 会包含整个重构差异，必须 `merge-base` 取 PR 自身 delta

### 2026-09-04（深夜）Phase-3 深冲突移植（5 PR）+ ci.yaml 幽灵路由故障二段诊断
- **CI 触发故障深化诊断**: 五连空 commit 重推后 78113/80832 拿到全量 CI（24 pass CLEAN），但 78104/59942/78100 的 ci.yml 始终未触发（仅外围 docker/nix workflow 5 pass）；#59661 更深一层——其 "Detect affected areas fail" 经日志/API 追查实为**幽灵 ci.yaml workflow run 33893853924 被整 run cancelled**（`.concurrency.path=.github/workflows/ci.yaml` 证实；classify 步骤本身 success、`python=true` 输出齐全）。**结论: fork push 的 pull_request 事件间歇性路由到改名死 path**（60662 同批成功→非确定故障）。#59661 已二次空 commit 重推（`0d6e809e2f`）+ 智能轮询（先验证 CI run 存在再盯终态）
- **移植完成 5 个**（累计 12 个 MERGEABLE）:
  1. **#56034** 预检硬阀: `force` 参数链（`should_compress`→`_automatic_compression_blocked[_locally]`）旁路防抖（与 main 新增 `ignore_cooldown` 正交）；决策链落新家 `turn_context_compaction.py:_preflight_compression`；**键改名 `preflight_hard_message_limit`**——main 的 gateway 层已占用 `hygiene_hard_message_limit`(默认5000)，复用会让 agent 阀默认开启改变 TUI/CLI 默认行为；157/157 过 → **CI 24 pass CLEAN**
  2. **#60662** tail floor: config 走 `CompressionSettings` dataclass; 47/47 过 → **CI 24 pass CLEAN**
  3. **#63009** codex autoraise: `_codex_route_bare_model` 加 `api_mode`（spark 同吃、语义无损）; switch 重推导挪进 `update_model`（新家，替代 PR 的 switch_model 外层方案）并尊重 `codex_gpt55_autoraise` 开关重读 config; 语义验证 0.50→0.85(codex_responses 切入)→0.75(sub-512K 地板,非粘滞) 与 PR 测试断言逐值对齐; 31/31
  4. **#59661** REST 祖先: `get_messages` 新家加 `include_ancestors`（复用 `_resume_lineage_ids` 分支豁免 + IN-clause 模式）+ REST/gateway 两端点传 True; 34/34 + 3 新测试; 二次重推中
  5. **#65388** PARTIAL(心跳已被吸收): faulthandler dump(serve 启动布防+心跳 re-arm) + model.options 快照(dispatch 冻结→handler 优先快照)双块; freeze/优先级语义验证过; MERGEABLE
- **CI 全绿 CLEAN 名单(5)**: #99069 #78113 #80832 #60662 #56034
- **全池终态**: 22 open → **14 MERGEABLE**(5 CLEAN) + #63292(L 待移植) + #64499/#64745(hindsight 碰撞待审计) + #63298(parked); 关闭 3(#99070 判重/#64373 ABSORBED/#58924 设计取代)



### 2026-09-05（凌晨）Phase-4 收尾：#63292 移植 + #64499/#64745 三腿审计 + 幽灵路由清障 + CI 修复循环
- **幽灵路由终态清障（重推循环）**: 三 PR 各再推一次空 commit 后 #78104（03e297c696）/#59942（4cdcf3e305）真 CI 触发；#78100（41c6e43525）第三次重推仍命中幽灵（run 33895454700 name=.github/workflows/ci.yaml 0-job 秒级 failure）→ 协议终止，PR 留言请维护者 re-run（证据链附 run 链接 + head_sha 验证空结果）；#59942 的第二推（4cdcf3e305）run 33895394915 又遇 cancel 类幽灵（path=死 ci.yaml、Detect areas cancelled、其余全 skip）→ 第三推 9d4c6c682c 成功触发真 CI
- **#78100 分支名四踩**: worktree 分支 `fix/interim-force-display-rb` 去 -rb 推到 `fix/interim-force-display`（不存在的分支）——实查 headRefName=`fix/force-display-interim-message` 后改推正确分支 + 删孤儿分支。教训强化：**推前必须 ls-remote 或 gh api 实查目标分支已存在**，不能只信本地分支名去 -rb 的推定
- **#63292 移植（最大件 L，Quad-MoA 落点表 3/3 PORT）**: K3 `--thinking max` 1500s 完成，commit `8c959e3e82`（17 文件 +463/−72）→ CI 抓出 2 个**上游旁侧测试**钉旧行为（truncation/partial-exit 路径的可见关闭行）→ 适配 2 测试断言（tail role=tool + `_interrupted_tool_tail` 标记）→ 修正版 `93f0a86689`。移植落点: 重构把 4 个 conversation_loop 调用点 hoist 进 `turn_recovery.abort_turn_on_interrupt` 单点（只改 chokepoint + turn_finalizer/turn_truncation/turn_tool_validation 三处）；持久化 stamping 迁 `session_persistence.py`（`_db_flush_row` 带 marker + `_db_flush_collect` 对已持久化行补打 `mark_tool_tail_interrupted`）；schema `interrupted_tool_tail` 列（`hermes_state_common.py` + `_INSERT_MESSAGE_SQL`/replay 读回）；API-copy 闭包在 `sanitize_api_messages`（interrupted tool→user 前插 assistant 行 + 剥 marker）；desktop `interrupted` flag 三层（types/message-stream/index）。`close_interrupted_tool_sequence` 保留（3 个历史测试场景仍需可见行）。验证: py 12/12 + 新断言 2/2、TS 156/156、typecheck 3 tsconfig clean、隐私审计仅 attribution 文件
- **#65388 CI 失败根因修复**: 移植版 handler 无条件传 `runtime_snapshot=` kwarg 打碎上游 mock（`lambda agent: object()` 单参 → TypeError → @_catch(5033) → KeyError: 'result'，tests/tui_gateway/test_model_options_profile_scope.py:28）→ 改为仅在 snapshot dict 存在时走新路径、否则原样调用（上游 mock 兼容）→ `ff734dc1f0` → **CI 24-pass CLEAN**。教训：改 handler 签名时必须 grep 全库 mock 该函数的测试
- **#64499/#64745 三腿盲审（K3/GLM/DSF 串行，3/3 收敛）**: 审计输入=新结构 main（`__init__.py` 2440→1214 拆分）+ 两 PR delta + 我预核实的 8 条事实
  - **#64499 PARTIAL**（3/3 一致）: enqueue 身份冻结 + FIFO writer + flush-on-switch 已被上游重构独立落地（`_make_turn_retain_job:979` 快照 docstring "Inputs are snapshotted NOW"）；lifecycle 半边缺失——`_build_retain_kwargs:966-968` write-time 重读 `_retain_tags`/`_observation_scopes`、watermark enqueue 即前移（非 commit）、失败 retain 丢弃无序重试、reconnect 裸 swap 泄漏旧 client 无 deep-copy、future 无所有权、`sync_turn(session_id=)` 静默内联重绑
  - **#64745 PORT**（3/3 一致）: prefetch 侧全缺——`queue_prefetch:917` 忽略 session_id kwarg、worker run-time 读 live bank/budget、发布无 epoch fence（main 的 join(3s)+drop 正是 PR 要替换的启发式）、单线程指针覆盖孤儿化 wedged worker、reconnect 无锁裸重建
  - 两 PR 共享同一套 client-lifecycle 机制 → **协调移植决策**（不独立移植避免重复建机制）；审计结论已分别留言两 PR
- **池终态（截至本段）**: CI CLEAN 9 个（#99069 #78113 #80832 #60662 #56034 #63009 #59661 #78104 #65388）；MERGEABLE 待 CI #63292（93f0a86689 跑中）+ #59942（9d4c6c682c 跑中）；#78100 已留言等维护者 re-run；#64499/#64745 审计完成待协调移植（下一步大件）；#63298 parked 不动；关闭累计 3
- **CI 终态补记**: #63292 `93f0a86689` CI success（mergeStateStatus=UNSTABLE 仅为 Docker 外围 workflow 仍在跑，非必需 check；PR 主 CI "All required checks pass" 已绿）+ #59942 `9d4c6c682c` CI success/CLEAN → **池 CI-CLEAN 达 11 个**（99069/78113/80832/60662/56034/63009/59661/78104/65388/63292/59942）；#78100 唯一遗留（等维护者 re-run）。验收四项全达成: ①#63292 MERGEABLE+CI ✓ ②#64499/#64745 出审计结论（PARTIAL/PORT，证据留言）✓ ③三 PR 幽灵路由终态（78104 CLEAN / 59942 CLEAN / 78100 留言）✓ ④ledger phase-4 补记 ✓


### 2026-09-06 PR 池例行体检 + #63298 CI 修复 + #78100 第四推清障
- **池体检（20 open，snapshot 逐一核验 gate + 外部评论全文）**: 17/20 "All required checks pass" 绿（11 个 phase-3/4 CI-CLEAN + #53936 早期 + #64499/#64745 + #62587/#60648/#99069）；无维护者新动作（最新外部互动 07-31 jeff-mettel / 08-31 Enough1122 LGTM）。已核对无需处理：#62587 疑虑 08-04 已正面回复、#80832 措辞已修正、#60648 duplicate 指控对象 #22836 CONFLICTING 停更 07-19（趋势有利）。
- **#63298 CI 修复（测试语义对齐）**: 本地复现真实失败 `assert 2 not in {1,2,3,4,5}` — 上游测试钉旧契约（replay 视图合并 user;user 隐藏第二行 row id），PR 语义为保边界（合并仅在 provider 侧 per-request 副本）。commit `831f8bd3f1` 对齐断言（第二 user turn 是 survivor→rebind 新 row id；truncation 掉的行→None；不存在 id 不进 map）。验证：目标测试 1/1 过；全文件 612 passed / 2 failed——2 个失败在纯 PR base `99c3cad857` 上原样复现（本机 desktop_ui 插件环境噪音，Linux CI 不触发），零回归。
- **#63298 CI 触发故障（比幽灵路由更深）**: `831f8bd3f1` + 空重推 `0221eb3a99` 两个 head 上 upstream runs=0（所有 workflow、含外围 Docker/Nix，check rollup 空、无幽灵 run），等 10+ 分钟无延迟送达。与 #78100 同日对照（75s 内全触发）证实为该 PR 分支的事件静默丢弃。已留言维护者附证据链（issuecomment-5558661744）请求 re-run。
- **#78100 第四推清障成功**: 空 commit `be2cf70019` → 真 CI 27 jobs 启动 → **"Python tests / Run tests" SUCCESS + "All required checks pass" SUCCESS + mergeStateStatus CLEAN**。幽灵路由协议更新：第三次失败后第四次重推（间隔 >24h）成功，说明非永久性限流而是间歇性事件丢弃。
- **池终态**: CI 绿 18/20（+78100 +63298 修复待触发）；#63298 唯一 DIRTY（等维护者 re-run CI）；#64499/#64745 协调移植仍是下一步大件。

### 2026-09-13 PR 池例行体检（上游侧全量快照，gh 实证）
- **池规模**: authored 总量 59 → **18 open / 41 closed / 0 merged**（较 09-06: 20→18 open，全量 state 逐一核对，无 MERGED）
- **自关闭 2 个（审计后证据式关，非被拒）**: #64499（09-10，enqueue 身份冻结核心已被上游 #102117 重构独立落地，`_make_turn_retain_job` 快照 docstring 验证）；#63298（09-11，存留核心被上游 `_handle_busy_submit`+`_enqueue_prompt` 设计取代，关前留言完整审计）
- **09-10/09-11 波次重建 7 个**（#102117 历史重写摧毁 merge-base → 从新 main tip 重放）: #56034 #59661 #60662 #63009 #63292 #64421 #64745 — **全部卡 fork-PR workflow 审批门**：head sha 上 runs 实证 CI/Nix/Docker 三 workflow 均 `completed/action_required`（如 #59661 runs 34457804606/35337/3744，#64421 runs 34554869440/…），check rollup 空、非幽灵路由；仅 #59661 已留言请求批准
- **CI 绿 11 个**（rollup 全 SUCCESS、0 pending）: #53936 #55534 #59942 #60648 #62587 #65388 #78100 #78104 #78113 #80832 #99069
- **外部新互动（09-06 以来）**: ①#59661 ehz0ah review COMMENTED 09-08（阻断发现，09-10 重建已修）+ thatssoheil 09-09（rotation-clone dedupe 疑虑，已交叉回应）②#60662 KeyArgo 技术留言 09-11 — 指出 PR 设计声明 "token budget remains primary" 在当前 main 不成立：`_walk_tail_budget` 对 floor 行无条件计费 + `cut_idx=min(cut_idx, fallback_cut)` token 盲钳制，10K 预算+10×7K 消息终态 6.3x 预算；**待技术回应**
- **下一步**: ①回应 #60662 KeyArgo（修设计声明或建立预算不变量）②其余 6 个 action_required PR 补审批请求留言 ③#64745 单独跟进（搭档 #64499 已关，prefetch 侧 PORT 审计结论仍有效）④维护者零合并持续，CI 绿+MERGEABLE 为全部可控面

### 2026-09-13 #60662 KeyArgo 技术回应（quad-MoA 4/4 ACCEPT_WITH_MODIFICATIONS → 已发帖）
- **事实链（全部本机确定性复现，无网络）**: KeyArgo 留言（09-11）指出 PR body 设计声明 "token budget remains primary" 不成立。在纯 upstream main `bdb5bf96`（他测量的 commit）+ PR head `3a8fef272` 双 worktree 复现：final cut=2/9 msgs/63,081 tok = 6.3x budget/4.2x soft ceiling **精确一致**——违例 pre-existing 于 main；walk 中间值本机 14,020/49,070（与他差 2-7 tok，estimator 舍入；final 精确同）。lean 窗真实对话形状（15 交替对，~7k tok assistant）量化本 PR 放大：floor=8 → 8 msgs/28,076 tok (2.8x)；floor=20 → 20 msgs/70,190 tok (7.0x) = **2.5x 放大**。同日 issue #108647（Tickip，生产 54,818 tail tok = 5.5x，双 compaction 连发）确认同一机制。
- **四腿盲审（900s --thinking max，K3/GLM/DSF/ASTRA 独立 session-dir）**: **4/4 ACCEPT_WITH_MODIFICATIONS**，罕见全票。共识：①两半框架——pre-existing + 主动 own 2.5x 放大，只讲血统=deflection ②token bound 不进本 PR（#108647 三方案是维护者设计裁决 + 既有契约测试 `test_n_guarantee_wins_over_tail_token_budget_and_floor` 允许 anchored tail 超预算，bound 必须与之调和）③承诺 follow-up。测试形态分歧（K3/ASTRA green documenting vs GLM/DSF xfail ratchet）→ 采纳 xfail：repo 先例 `tests/perf_guards/test_pattern_b_scaling.py` 实证存在（"The xfail markers are the ratchet"）。
- **四腿新发现（本 orchestrator 全部核实为真）**: ①diff 声称 rename 实为**复制**——`_MAX_TAIL_MESSAGE_FLOOR` 存活于 :971，`_prune_boundary:2630` 仍用旧常量，`test_protected_tail_pressure_61932.py:22` 还 import 它 ②repo 契约测试 :3394 ③walk 中间值 GLM/DSF 引用 brief 未重跑（K3 重跑了且正确——引用前必须自测，本 orchestrator 实测采纳 K3）。
- **实现（单 K3 900s，commit `2b6177dc6e`，已推送 fork）**: 常量去重（`_DEFAULT_MAX_TAIL_MESSAGE_FLOOR` 单源，`_prune_boundary` 同步，零行为变化，tree-wide grep 零旧引用）+ `test_tail_floor_token_bound_pending_108647`（xfail strict=True，钉期望不变量 `tail_tokens <= 1.5x lean budget`，reason 引 #108647 + 实测数字）+ config_defaults lean 窗 caveat。自验：153 passed + 1 xfailed / protected_tail 2 passed / py_compile OK / 隐私审计双 CLEAN。
- **发布（全部回读验证）**: PR 评论 issuecomment-5653168992（两半框架+四项变更+follow-up 承诺）；PR body Design 段重写（三步机制顺序+7.0x 实测+ #108647 链接，Changes/Validation 表同步）；#108647 交叉留言 issuecomment-5653183345（ratchet tripwire 供 fix 实现者接线）。
- **遗留**: 新 head 同卡 fork-PR `action_required` 审批门（runs 34756371859/1548/1557，与 09-10 状态一致已记录，不重复请求）；#56034/#63009/#63292/#64421/#64745 等 6 个 action_required PR 的审批请求留言仍待补（池体检下一步）。

### 2026-09-13 6 个 action_required PR 补审批请求留言（池体检遗留收尾）
- **核实先行**: 逐一刷新 7 个重建 PR 的 head-sha runs（三 workflow 全 `completed/action_required`），检查历史评论确认仅 #59661 已留过审批请求（不重复），其余 6 个（#56034 #60662 #63009 #63292 #64421 #64745）各补一条：head 短 SHA + 三 run ID + action_required 状态 + 请求维护者批准 workflow。
- **发布**: 6 条全 posted（12:23-12:24Z），回读验证 author/内容一致。**自检抓到自家精度错误**: #56034 三条 workflow 标签全错 + #63009 Nix/Docker 互换（模板参数按顺序硬套，未先核对 run-ID↔name 映射）——立即 REST PATCH 修正（comment IDs 5653241489/5653241937），全 6 条重新回读 + 与 `actions/runs` API 的 name 字段逐一对照，**6/6 映射正确**。教训入库：批量模板参数含「标签-ID 配对」时，先跑 ID→name 映射再生成文案，不靠位置顺序。
- **池状态（本次后）**: 7 个重建 PR 全部有审批请求留言在案；CI 绿 11 个不变；待维护者动作面已全部铺好（审批 + review 响应 + cross-link）。

### 2026-09-13 #64745 单独跟进（池体检遗留最后一大项）
- **核实**: 09-10 重建评论已完整记录 PORT 审计 5 发现的实现（request 快照含 budget——比原 PR 更严、5 因子 currency fence、epoch bump in admission lock、bounded worker pool、client-ownership 机制 + #11923-class leak 关闭）+ 四腿 ACCEPT-WITH-NITS 两 nit 已修；diff 实证 epoch/fence/client_lock/deepcopy 机制在位（+1979/−109）。发现 **PR body 滞后**：Verification 停在 pre-#102117 旧数字（133 passed），未提重建、#64499 机制吸收、CI 审批门——正是 #60662 踩过的 body 滞后坑。
- **动作**: PR body 同步到重建后状态（head `92613bd5e1`、107+434+73 passed 新数字、History 段记录 #102117 重建 + #64499 已关且 enqueue 侧被上游吸收、本 PR 独扛 client-ownership 机制、CI 审批门说明）。隐私审计 CLEAN，回读验证 true。

### 2026-09-18 PR 池全量核查（cherry 证据级判定 + 2 PR 重开）
- **触发**: 09-13 体检记 `0 merged / 17 closed-unmerged`，复核发现该表述严重误导——实际交付走 **salvage/successor PR** 路径（原 PR 以 closed-unmerged 收尾，commit 被 maintainer 以 base 形式并入新 PR 后合入 main，署名保留）。`gh pr list --state merged` 对该仓库恒为空，**不可**作为"内容是否落地"的判据。
- **方法（确定性）**: 对全部 42 个 closed PR 逐一 `gh api issues/N/timeline` 取 close actor + `gh pr view --json comments/reviews` 取最后外部评论 + `git cherry upstream/main <head>` 取 commit 级吸收证据。判定规则:cherry 全 `-` = 已落地;残留 `+` = 未落地。
- **已落地（cherry 全 `-` 证实）16 个**: #59942(09-17 经 #113933 `032cf8438f`/`d49fda1d15`,teknium1 明言 "your commit preserved as the base")、#76503(#90193)、#68817(#69546 `8c745314b`)、#61518(#87237)、#62486(#65896)、#56084(#57335)、#54785(#65419)、#53890(#69860)、#63710(#63968 直 cherry-pick)、#55707(#69884)、#54612(#55545)、#74596、#53928、#53692、#53684、#54637、#61336。
- **明确被拒 4 个**: #62663(teknium1 09-02 "Superseded",与 #100903 retention sweep 重复)、#53928/#53692/#53684(AI-triage 判重后自撤)。#62663 内容确已由 #100903 覆盖,可按 retirement 流程退 TRACKED。
- **自关但 cherry 证伪（关键更正）→ 已重开 2 个**:
  - **#64499** 09-10 自关理由"enqueue 身份冻结已被 #102117 吸收"——**错误**。cherry 显示核心 commit `fadb9b51c9` 仍为 `+`。upstream `_make_turn_retain_job` 只快照 turns/metadata/lineage/bank_id/retain_async/retain_context,job body 仍在运行时调 `self._build_retain_kwargs()` 读 mutable provider state——正是本 PR 所修。已重开 + 留言说明(5726067813)。
  - **#63298** 09-11 自关理由"被 #86134 超集取代"——**错误**。17 commits 中 16 个仍为 `+`。upstream `session_auto_continue.py` 的 `_handle_busy_submit`/`_enqueue_prompt` 是新实现,不含本 PR 的 canonical-history/FIFO 边界语义(adjacent user 保持独立行、仅在 provider copy 合并)。已重开 + 留言说明(5726068112)。
  - 教训入库:**关闭判据只认 `git cherry`,不认评论措辞、 sweeper verdict 或"机制看起来一样"的语义推断**。
- **另 19 个自关 PR 残留 `+`**: 含 #74855(state-db 重复插入 guard,sweeper 明言 main 仍无条件插入)、#58924、#58632、#63519、#55386 等。无 maintainer 定论,性质为"撤回而非被拒",与上述 2 个同类风险——后续池体检应逐一复核是否误关。
- **重开后状态**: 两者均 `CONFLICTING DIRTY`（#102117 历史重写摧毁 merge-base,head 未动),rebase 后方可进 review;#64499 单 commit 低风险,#63298 17 commits 且历史上有深层语义冲突,属重活。
- **17→19 open 快照（mergeable 强制重算）**: MERGEABLE CLEAN 7(#99069 #80832 #78113 #78104 #78100 #60648 #55534) / MERGEABLE BLOCKED 4(#64421 #62587 #56034 + 重开待 rebase 后归类) / CONFLICTING DIRTY 6(#63292 #64745 #63009 #60662 #59661 #65388)。#64421 等 6 个 action_required 审批请求留言已在案,09-13 后无新维护者动作。
- **移植 #64499（09-18,head `bf1460bceb`,已推 fork）**: 重开 + 移植一气呵成。
  - **侦察**: upstream 已吸收的部分 — enqueue 快照 turns/metadata/lineage/bank_id、`_resolve_retain_target` 前置、flush-on-switch 用旧 id。**未吸收的精确漏洞**: `_make_turn_retain_job` 的 `_job()` 仍在 writer 线程运行时调 `_build_retain_kwargs()`,该函数重读 mutable `_retain_tags`/`_observation_scopes`/`_retain_source`/`_METADATA_ATTRS`。
  - **复现（RED）**: 新测试 `TestRetainIdentityIsolation` 用 `threading.Event` gate 卡住 writer 在 dequeue 与执行之间,主线程改 `_retain_tags`/`_observation_scopes`/`_retain_source`,断言 shipped item 保留 enqueue 值。base 上确证失败(tags 变 `new-tag`)。
  - **修复**: 把整个 item 构建前移到 enqueue;writer job 只负责发送。**未引入新状态/新接口**,`_build_retain_kwargs`/`_retain_batch` 签名零改动。
  - **验证**: 文件内 79 passed(+1 新)/1 skipped/8 基线失败;**8 个失败在 untouched base 上原样复现**(hindsight-client 沙箱不可装 → `lazy_deps` ImportError),stash 对比实证零回归。
  - **结果**: PR 从 `CONFLICTING DIRTY` → **`MERGEABLE BLOCKED`**(仅剩 fork-PR CI 审批门)。已发移植说明评论(5726136385)并重写 body 记录重开缘由+cherry 证据。
  - **教训**: 原判断"已被吸收"只核对了 enqueue 快照面,没核对 item 构建面;`git cherry` 的 commit 级证据比语义推断可靠——但 commit 级 `+` 只证明"代码不在",**机制级确认仍须读目标代码**(本例漏洞恰在同一函数的另一半)。
- **#63298 rebase 评估**: 17 commits、历史上有 hermes_state repair 语义分歧,量大;维持 open 待排期,不与 #64499 抢资源。
- **下一步**: ①#64499 CI 审批门跟进(与其他 6 个 action_required 同类)②#63298 移植排期 ③#62663 从 TRACKED_PRS 退栈 ④19 个残留 `+` 自关 PR 的误关复核。

### 2026-09-18 三项收口:#64499 CI 门跟进 / #63298 移植排期 / #62663 退栈
- **#64499 CI 审批门**: 新 head `bf1460bceb` 的三个 run 全部 `action_required`(CI `35315206806`、Nix `35315206477`、Docker `35315206427`),已在 PR 发审批请求留言(5726188438),格式与 09-13 那 6 条一致。GitHub 侧 `mergeable=true` / `mergeable_state=blocked`——纯审批门,无冲突。至此 7 个 action_required PR(#56034 #59661 #60662 #63009 #63292 #64421 #64745)+ #64499 共 8 个全部有审批请求在案。
- **#63298 移植排期评估(dry-run,未动分支)**: 在 `/tmp/port-63298` 对 `0221eb3a99` 跑 `git rebase upstream/main`,**第一个 commit `b2e3e4bcd2` 就在 10 个文件上冲突**:`agent/agent_runtime_helpers.py`、`agent/conversation_loop.py`、`agent/transports/chat_completions.py`、`agent/turn_context.py`、`cron/scheduler.py`、`gateway/mirror.py`、`run_agent.py`、`tui_gateway/{compute_host,server}.py`、`tests/agent/transports/test_chat_completions.py`。根因:#102117 同时把 `tui_gateway/server.py` 拆成 `session_auto_continue.py`/`prompt_turn.py`/`compute_host_bridge.py` 等,与本 PR 在 server.py 上的 913 行改动正面相撞。**结论:这是移植而非 rebase,排到 #64499 有结论之后**;理由三条:①单 commit 的 #64499 尚卡审批门,同资源并行推会分散维护者注意力 ②上游 `session_auto_continue.py` 已有 24 处 queued 机制,移植前必须逐机制比对"同名同义 vs 本 PR 独有",否则重演 #64499 误判 ③量级差 10 倍以上(1458 行插入 vs 26 行)。
- **#62663 从 TRACKED_PRS 退栈(完成)**:
  - **证据链**: teknium1 09-02 关闭理由为"superseded"——cron 完成时已关 session(`650b400c980` `cron_complete`),异常退出由 #100903 的 retention sweep 接管。核实: #100903 **MERGED**(2026-09-02,PR #54189 的 salvage),其 commit `9bc249c7e5` 在 upstream/main 上;机制落在 `hermes_state_maintenance.py:360 maybe_auto_prune_and_vacuum` → `sweep_orphaned_sessions`,关闭 stale-open 的 cron/kanban/subagent 行,带 retention window + `exclude_pinned`,覆盖本 PR 的 reaper 语义。
  - **发现的偏差**: PR 标题承诺的另一半 "persist final_response as session message" 在 4 个 commit 里**从未实现**(全部是 reaper 相关);即该 PR 只交付了标题的一半,被覆盖的也只有那一半。
  - **动作**: `hermes-patched-pick-plan.json` 删除 #62663 条目(25→24,order 重排 0..23),备份 `hermes-patched-pick-plan.json.archive-20260918`;按 immutable-boundary 约定,**不动** `hermes-patched-state.json` 里的 patch 字节,留到下次 coordinated regeneration。
  - **更正一条旧记忆**: 该 PR **不是**被 upstream merge,而是 closed-unmerged(`gh api timeline` 实证 actor=teknium1、event=closed、无 merged 事件);此前记录的"09-02 被 upstream merge 并 close"表述错误。
- **下一步**: ①8 个 action_required PR 等维护者批准(全部已留言,无新动作可做)②#63298 移植待 #64499 有结论后启动,先做逐机制比对再动手 ③19 个残留 `+` 自关 PR 的误关复核(下一个候选 #74855,sweeper 明言 main 仍无条件插入)。

### 2026-09-18 自关 PR 误关复核(全量,三问判据)
- **方法**: 对全部 40 个 closed PR 重算 `git cherry upstream/main <head>`(9 LANDED / 31 RESIDUAL),用 `gh api issues/N/timeline` 取 close actor → 筛出 **17 个自关且带残留 `+`** 的候选。三问判据:①commit 是否在 main ②机制是否在 main ③**用户可见症状**是否在 main(决定项)。①②只定价值,③定对错。
- **A 类:正确关闭(8 个,机制/症状已被上游以别的方式解决)**:
  - **#74855** ①是②否③否。写路径仍是裸 INSERT,但上游加了 `display_identity` + `_dedupe_display_generations` **读路径折叠**重复(compaction 世代归一,`_dedupe_display_generations` 逐 key 取 (active,id) 最大代表)。实测 orchestrator profile 56 万行 / 9.5 万 active = **0 个 ACTIVE 重复组**;61290 个全行重复组全是 archive+live 有意对。用户可见症状已治好,SQL 层去重只是库膨胀。
  - **#58632** remount spinner 已由 `$changeEventsAvailable`/`$cronChangeTick` + 轮询 + visibilitychange 解决(cron/index.tsx:885-925),不依赖缓存。
  - **#60973** 被上游更完整覆盖(submit.ts:227-265 显式 `storedSessionId` 绑定 + 被 reap 的 queued runtime 拒绝继承前台 id,注释明指 cross-session leak)。
  - **#62657** 已吸收:`session_persistence.py:34` 注释写明 "the assistant candidate itself is NOT synthetic",正是本 PR 的改法。
  - **#74597** 已吸收:`error_classifier.py:1070 _from_cause_chain`(深度 5 + 环防护)。
  - **#64364** 已吸收:`memory_manager.py` 的 `_drain_sync_executor()` + `shutdown_drain_state`,自关评论引用的文件/行号与当前 main 一致。
  - **#62571** 后继 #63624 确 MERGED(2026-07-13)。
  - **#69393** 上游改 per-cwd 设计(`$repoStatusByCwd`/`registerRepoStatusCwd`),旧分支已无法构建,关得对。
- **B 类:正确关闭,但自关措辞有事实错误(2 个,自己关的自己,不影响结论)**:
  - **#58924** 自关写 "No behavioral gap remains" — **错**。真实情况:插件 `get_config_schema()` 有完整 `llm_*` 条件字段,但 `config_schema.py` declared 表面**刻意**只有 5 字段 / 2 mode,`test_hindsight_config_schema.py:41` 明文 pin `local_embedded not in mode.allowed_values()`("intentionally unsupported on desktop");`_apply_field_values` 只迭代 schema 字段 → 桌面端填的 `llm_*` 被静默丢弃。结论方向对(与上游设计冲突),但 gap 是**被有意保留**的,不是不存在。
  - **#99070** 自关为让路 #98863 — 该后继 PR 至今 **OPEN 未合**;但本 PR属同时独立发现,#98863 覆盖更广且 CI 全绿,让路本身仍合理。
- **C 类:真误关 / 应重开(3 个)**:
  - **#53684** — **误关,建议重开**。自关理由"duplicate of #48525",但 **#48525 至今 OPEN 未合**(最后活动 08-05 ValentinSergief 评论)。上游 `delete_session`(`hermes_state_sessions.py:1489`)与 `delete_sessions`(:1546)**至今不走 compression lineage**:`DELETE FROM messages WHERE session_id = ?` 只删 tip 的消息,compression 祖先被 orphan 成 `parent_session_id=NULL` 残留——PR 注释预言的"删除后旧压缩根在 session picker 复活"在当前 main 仍成立。两个 PR 修法不同(本 PR Python lineage-walk vs #48525 递归 CTE),可并存或合作。**本地实测**:orchestrator profile 607 sessions / 0 条 `end_reason='compression'`(该 profile 从不压缩),故无法在本机复现残留行,但代码事实无歧义。
  - **#54637** — **误关,建议重开**。自关理由"让路 #49019",但 **#49019 至今 OPEN 未合**(最后评论 07-17)。上游盲点确证:`display.py:924-933` 与 `tool_guardrails.py:224` 对 terminal 一律"非零退出码 = 失败",不看是否有 stdout;`classify_tool_failure` 注释自称"mirrors `_detect_tool_failure`",两处同步盲点。
  - **#62552** — **误关,建议重开**。自关理由"main 已修",但 `_resolve_switch_context_length`(:2011) 只在**custom-provider** 分支读 config,普通 `model.context_length` 仍走老路径;本 PR 修的是**带引号字符串** `"131072"` 被丢弃的解析 bug —— `int("131072")` 现在仍未做,`isinstance(int/float)` 判断会丢掉字符串配置。症状:切模型后上下文窗口被静默回退。
- **D 类:已明确拒/退栈,无需动(6 个)**: #62621(被 d93c905808 取代)、#55386(让路 #62663→被 #100903 MERGED 覆盖)、#62598(与 #78113 架构冲突,自关正确)、#63671/#62676/#63519 等由他人关闭或已在上轮处理。
- **教训入库(方法论修正)**: 三问判据里 **③ 用户可见症状是决定项**:#74855 ①是②否③否 → 正确关闭;#58924 ①否②半③有意保留的缺口 → 正确关闭但措辞错;#64499 ①是②是③是 → 真误关。**另:自关理由里的"让路 X"必须回查 X 的合并状态** —— 本轮 5 个"让路"引用中 3 个(#48525/#49019/#98863)至今未合;若 X 长期停滞,自关等于把唯一活实现也撤了(#53684/#54637 即此类)。

### 2026-09-18 #53684 移植重开(三误关之一)
- **Port 策略**: 不沿用原 PR 的 Python lineage-walk —— 上游已把 compression 边定义中心化为 `_COMPRESSION_CHILD_SQL`(`hermes_state_common.py:154`,被 `get_compression_tip`/`list_sessions_rich`/`hermes_state_titles._is_compression_ancestor` 共用),port 直接复用该权威谓词,递归 CTE 双向走(祖先+后代,seed 可在链中段);`_is_explicit_fork_child_row` 的 `source=='tool'` / `_branched_from` / `_delegate_from` 排除由谓词自带。
- **改动**: `hermes_state_sessions.py` +69 行(`_compression_lineage_ids` + `_LINEAGE_{DESCENDANTS,ANCESTORS}_SQL`);`delete_session`/`delete_sessions` 的 kill 集从单 id 扩为 lineage 集合;bulk 返回值保持 caller-visible 计数(`len(kill_set & existing)`,lineage 扩展算副作用),`expected_delete_ids` 契约与 delegate 级联零改。
- **验证**: 9 新测试 `test_compression_lineage_delete.py` 全绿(tip/root/middle 删除塌缩全链、消息随行、`_branched_from` 子存活并 orphan、`_delegate_from` 子仍级联、无关 session 不受影响、bulk 计数语义)。**全 `tests/hermes_state/` 基线对比**:worktree 隔离跑 pristine `upstream/main` 得同 69 failed/1251 passed(环境性失败,macOS 沙箱),改后 69 failed/1260 passed — `comm -13` **零新增失败**,失败集逐行相同。
- **结果**: PR #53684 已重开,head `d9acdd9193`,**MERGEABLE**(base main 无冲突);三个 workflow 均 `completed/action_required` — 与其余 8 个 fork-PR 同一审批门,非本 PR 特有。重开说明评论已发(5726934748),含与 #48525(仍 OPEN 未合)的对齐声明。
- **下一步**: ①#54637 port(terminal 非零退出码 + stdout → 非失败)②#62552 port(quoted context_length)③9 个 action_required PR 等维护者。
